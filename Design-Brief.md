# Design Brief — Toonflow Web

## 产品概要

- **源 Spec**：Product-Spec.md v3.5（基于既有设计方向补充复核，自动管线运行视图、路径 5 视频生成确认、跨集 IP 资产时间线、质量评估协议，以及 EpisodeDeliveryPackage / ProjectMigrationPackage 边界需要在后续设计稿中补齐）
- **生成日期**：2026-05-11
- **聚焦**：视觉方向（产品详情见 Product-Spec.md）

## 设计方向

### 情绪关键词

- **冷静** — 界面应降低创作管线的混乱感，让剧本、资产、Track、take、成本和审计状态都显得可控。
- **精密** — 视觉层级、状态标签、版本关系和质量门禁要清晰，像专业生产系统，而不是普通 AI 聊天工具。
- **可控** — 用户随时知道当前阶段、缺失依赖、生成策略、风险和下一步操作，避免黑盒抽卡感。

### 参考产品

| 参考产品 | 喜欢的方面 | 不喜欢的方面 |
|---------|-----------|-------------|
| Linear | 高信息密度但有秩序，列表、状态、详情展开和操作反馈清楚 | 不要过于项目管理化，媒体预览不能太弱 |
| Vercel | 冷静、干净、可信，适合设置、模型、后端状态和部署配置 | 不要过度留白，避免压低生产效率 |
| Runway | 视频与生成结果预览有张力，适合 take 审核和媒体对比 | 不采用全局深色与大媒体沉浸布局 |
| Krea | 图片/视频资产卡片直观，适合视觉资产候选与版本比较 | 不采用过强的创作社区感或瀑布流娱乐化表达 |

### 反面参考

- **全局 Runway 深色沉浸**：只适合审片和媒体聚焦，不适合长文本、任务队列、质量门禁和审计日志的长期扫描。
- **Notion 大留白文档页**：适合安静写作，但会削弱 A-E 管线、Track、take、成本和状态的同屏掌控。
- **Ant Design / Element UI 通用后台感**：功能完整但容易显得普通，不能体现 Toonflow 的创作生产系统差异。
- **Miro / Figma 大画布主入口**：空间自由度太高，会冲散章节、剧本、资产、Track 和 RoughCut 的线性生产节奏。

### 目标用户审美优先级

- **第一优先：高频生产操作者** — 制片、导演、AI 视频操作员需要快速判断状态、依赖、成本、风险和下一步动作。
- **第二优先：创作者 / 编剧** — 需要文本可读、版本关系可信、锁定和回滚足够安心。
- **第三优先：技术型配置用户** — 需要模型、供应商、密钥、运行环境和任务恢复可解释，但这些能力不应喧宾夺主。

## 视觉规范

### 色彩方向

- **主题模式**：浅色主工作台，局部深色媒体/预览面板。
- **色彩温度**：中性偏冷。
- **品牌主色方向**：冷色强调，偏理性、技术、可信；只作为导航激活、主操作、选中态和关键状态线索。
- **强调色方向**：警告、失败、成本超额和合规风险使用克制但明确的暖色警示；成功、已锁定和通过门禁使用稳定的绿色方向。

### 品牌资产（独立于色彩）

- **Logo**：无现成 logo。
- **已有品牌色**：无现成品牌色。
- **字体**：无指定字体，交给设计工具选择系统级、清晰、适合中文长文本的无衬线方向。
- **吉祥物/图形 IP**：无。
- **整体品牌调性**：专业、克制、可信，带一点创作工具的媒体质感。

### 信息密度

- **密度方向**：紧凑。
- **参考基准**：接近 Linear。
- **理由**：产品有项目、章节、剧本、资产、Track、take、任务、成本、审计等大量对象；如果信息过松，D/E 阶段会频繁跳转和折叠，影响生产效率。

### 排版方向

- **字体气质**：现代无衬线，偏几何和理性，避免可爱、手写或强装饰字体。
- **中文字体偏好**：系统默认或设计工具推荐的高可读中文字体。
- **标题风格**：小而精致，适合工具界面；避免营销页式大标题。

### 交互风格

- **动画程度**：适中偏少。
- **过渡效果**：快速淡入淡出、轻量展开收起、状态渐变；避免弹跳和强表演型动效。
- **整体节奏**：沉稳但响应明确，生成中、队列中、失败、锁定等状态要即时反馈。

## 核心页面视觉备注

### 全局工作台与项目列表

- **核心交互**：左侧窄导航进入项目列表、设置、任务中心；项目列表展示项目状态、章节数、制作进度和最近活动。
- **视觉方向**：Linear / Vercel 式浅色生产控制台，导航克制，内容区紧凑。
- **特殊要求**：项目列表不能做成营销卡片墙；卡片应服务扫描、比较和继续工作。

### 小说原文与剧本管理

- **核心交互**：导入、章节解析、编辑、版本对比、审核、回滚、锁定和导出。
- **视觉方向**：偏 Notion 的文本可读性，但信息密度和状态处理靠近 Linear。
- **特殊要求**：文本编辑和版本状态要安静，不要用大面积装饰；未保存、stale、locked 等状态必须明显。

### 剧本 Agent / Production Agent 面板

- **核心交互**：紧凑消息流、流式生成、工具调用、快捷指令、阶段上下文和任务进度。
- **视觉方向**：Linear / Slack 式紧凑消息流，更像执行记录和操作台，不像 ChatGPT 大段闲聊窗口。
- **特殊要求**：工具调用、AgentRun、失败原因和下一步按钮应可折叠；面板固定在右侧，不抢占主生产区。

### 塑造与资产详情

- **核心交互**：资产筛选、批量润色、图片生成、AssetVersion 画廊、canonicalVersion 选择、来源与引用查看。
- **视觉方向**：Runway / Krea 式媒体卡片加紧凑元信息；图片清楚，版本、状态、成本和引用压缩展示。
- **特殊要求**：资产卡片不能只展示漂亮图片；必须同时承载类型、版本、锁定、授权、引用和质量状态。

### 制作工作台 A-E 管线

- **核心交互**：集数选择、A-E 节点状态、节点详情展开、TrackPlan、策略依赖、视频任务和 E 阶段粗剪交付。
- **视觉方向**：左侧窄导航 + 中央管线/列表 + 右侧 Agent 面板；中央区域高密度呈现节点、Track 和状态。
- **特殊要求**：管线节点图用于定位，不做巨大画布；节点详情和 Track 列表才是核心信息承载区。

### Track 行与质量门禁

- **核心交互**：Track 行展开后显示 TrackSegment、生成策略、依赖清单、ProductionFrame、take 列表、问题类型和质量门禁。
- **视觉方向**：Linear issue / GitHub checks 风格；每行状态明确，可展开，有检查项、失败原因和操作按钮。
- **特殊要求**：Track 多时仍要可扫描；失败、blocked、stale、needs_regeneration、locked、waived、over_budget、compliance_failed 等状态必须一眼可辨。

### Take 审核与媒体预览

- **核心交互**：预览视频、对比多个 take、标记问题、重试、接受、锁定和进入 RoughCut。
- **视觉方向**：局部深色媒体面板，借 Runway 的审片氛围；周边元信息保持紧凑。
- **特殊要求**：深色只用于媒体聚焦，不把整个工作台改成沉浸式深色创作工具。

### 设置、任务中心与审计日志

- **核心交互**：ProviderConfig、ModelConfig、SecretRef、RuntimeCheck、任务恢复、成本复盘、访问凭据和 AuditLog。
- **视觉方向**：Vercel / Linear 式设置与状态页，清晰、可信、可诊断。
- **特殊要求**：危险操作、密钥、dataRoot、硬删除和导出敏感配置必须有强状态提示和二次确认。

### Legacy Import 导入向导

- **核心交互**：扫描旧目录、预览可导入对象、映射项目/章节/资产关系、确认导入、展示导入后校验结果。
- **视觉方向**：Vercel 式分步检查清单，配合 Linear 式差异预览和状态标签。
- **特殊要求**：明确表达“只读扫描旧数据”和“不会改写 novels 原始目录”；冲突、重复、缺失引用和不可解析项要在确认前集中展示。

### ProjectMigrationPackage 导出检查

- **核心交互**：选择项目迁移/备份范围、检查 manifest、Artifact 完整性、敏感配置排除、运行中任务处理和导出结果。
- **视觉方向**：GitHub checks / Vercel deployment summary 风格，先给总体可导出结论，再展开具体检查项。
- **特殊要求**：导出不是普通下载按钮；缺失素材、运行中任务、SecretRef、访问凭据和成本记录应有清晰排除或阻塞状态。EpisodeDeliveryPackage 属于 E 阶段单集交付，不挂在设置降级页。

### 访问凭据与 SecretRef 安全区

- **核心交互**：配置访问凭据、管理 SecretRef、测试连接、查看使用范围和审计记录。
- **视觉方向**：Vercel 设置页风格，克制、可信、操作边界清楚。
- **特殊要求**：密钥不可见、不可复制明文；测试连接、轮换、失效、权限不足和最近使用记录要有稳定位置。

### RuntimeCheck 与任务恢复

- **核心交互**：运行环境诊断、Provider 可用性、队列状态、失败任务恢复、重复任务阻止和 reconciling 状态查看。
- **视觉方向**：诊断清单式呈现，每项给通过、警告、失败和修复入口。
- **特殊要求**：reconciling、duplicate_blocked、retrying、paused、cancelled 不能只靠文案区分，应有一致的视觉状态语法。

## 状态设计

- **空状态**：极简占位 + 明确 CTA；以“下一步该做什么”为主，不用插画堆情绪。
- **加载状态**：列表和卡片用骨架屏；Agent 输出用紧凑流式消息；批量任务用进度条和数量统计。
- **错误状态**：内联错误为主，配合 Toast；阻塞性错误进入节点/任务详情；危险或不可逆操作使用确认弹窗。

### 状态视觉语法

- **默认 / 草稿**：低强调，适合章节、剧本、资产和 Track 的未推进状态。
- **运行中 / 生成中 / retrying**：轻量动效、进度信息和队列位置并存，让用户知道系统仍在推进。
- **成功 / passed / locked**：稳定的通过状态，强调结果可用、版本已固定或质量门禁已通过，但不做庆祝式视觉。
- **警告 / stale / needs_regeneration / over_budget**：使用克制暖色方向，表示可以继续查看，但推进前需要确认风险。
- **阻塞 / failed / compliance_failed / secret risk**：高优先级危险态，用于阻止推进或触发强确认；错误原因和修复入口必须靠近状态本身。
- **waived**：与 passed 明确区分，表达“人工豁免”而不是“自动通过”；豁免人、原因和时间应可进入详情。
- **reconciling / duplicate_blocked**：任务中心和恢复流程的诊断态，强调系统正在整理或已阻止重复执行。
