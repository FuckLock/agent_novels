# 变更记录

## [v3.5] - 2026-05-14

**变更主题**：
- **第一批修订（落地冲突修正）**：修正 v3.4 进一步审核发现的开发落地冲突，重点拆清 `~sd auto` 与 D 阶段视频生成边界（路径 D 从路径 0 独立），补齐质量评估协议、状态映射、Workspace 用户语言和 AI/后端能力分层。
- **第二批修订（路径 D 独立化连锁影响修复，本次追加）**：第一批的路径 D 独立化是好设计，但连锁影响未传递到位——路径 2 仍自描述 D/E（与路径 D 重复）、路径关系图说路径 D 仅是路径 0 后续（与路径 D 自描述矛盾）、UI 布局缺 D 阶段提交确认页和任务视图、命名风格不统一（路径 D 字母 vs 路径 0-4 数字）。本次第二批清理 10 条连锁问题，并把路径 D 改名为**路径 5**保持命名风格统一。

### 新增

- 🔴 新增 **独立 D 阶段视频生成确认与批量提交路径**（位置：[用户使用流程]；当前命名为路径 5）—— 原因：v3.4 把 D 批量视频生成写进路径 0，和"`~sd auto` 只跑 A→C3"的承诺冲突。
- 🔴 新增 **质量评估协议（最小可执行版）**（位置：[重设计结论] 成片质量测量口径）—— 原因：角色一致性 ≥95%、动作可读 ≥90% 等指标需要评估单位、样本规则和 QualityGate 字段，否则无法验收。
- 🟡 新增 **系统与后端能力需求**（位置：[AI 能力需求] 后）—— 原因：媒体拼接、项目打包、任务对账、环境诊断等是确定性工程能力，不应混入 AI 能力表。
- 🟡 在页面清单补充 **Take/粗剪交付** 页面（位置：[补充说明] 页面清单）。

**第二批新增（本次追加）**：

- 🔴 在路径关系段追加 **Tab ↔ 路径对应表**（位置：[用户使用流程] 路径关系段末尾）—— 原因：第一批描述了 Tab 和路径，但两者对应关系是隐含的，design-maker 和 dev-builder 要自己脑补。新表显式列出剧本 Agent + 剧本管理 Tab ↔ 路径 1、自动管线运行视图 ↔ 路径 0 和路径 5 共享、制作工作台 Tab ↔ 路径 2 和路径 5 入口、Take/粗剪交付 Tab ↔ 路径 4、设置页 ↔ 路径 3。
- 🔴 路径 1 第 11 步加收尾说明（位置：[用户使用流程] 路径 1）—— 原因：路径 1 是路径 0 的前置准备，但第一批第 10 步直接结束，没说"剧本就绪后接入路径 0/2"。读者读完路径 1 不知道下一步。新第 11 步明确"剧本就绪后选路径 0（默认推荐）或路径 2 进入制作链路"。
- 🔴 路径 5 加完整 **关键约束** 块（位置：[用户使用流程] 路径 5 末尾）—— 原因：路径 0 和路径 4 都有"关键约束"块写硬性边界，路径 5 之前没有。新约束块明确：D 必须用户确认、并发默认 3 + 可临时调整、单 Track 失败按 7 类问题各自重试 prompt 变体 ≤ 2 次、单 Track 终止时下游 blocked、暂停时远端任务必须 providerJobId 对账避免"扣费但本地无记录"、所有取消重试失败都写 Task + AuditLog + UsageRecord。
- 🔴 自动管线运行视图扩展为 **5 模式**（位置：[UI 布局] 区域内容）—— 原因：第一批把"路径 D"独立后，UI 布局只描述了 A→C3 跑通 + C3 完成确认两个状态，但路径 5 还需要"D 阶段提交前确认 / D 阶段任务执行 / D 完成进入路径 4 引导"三个新模式。新描述把视图升级为 5 模式按阶段切换（模式 1-5），模式 4 中部由管线流水线大图切换为 Track 任务网格，底部日志变为 Task 事件流；MAJOR 4（缺 D 阶段页面）和 MAJOR 5（视图关系不清）一并解决。
- 🟡 路径 2 加 **关键约束** 块（位置：[用户使用流程] 路径 2 末尾）—— 原因：路径 2 大幅瘦身后需要明确硬约束。新约束块：路径 2 严禁完成 D/E、每阶段必须用户显式确认、跨阶段回退触发下游 stale、Agent 自审失败用户可显式豁免（与路径 0 必须通过自审不同）。

### 修改

- 🔴 将 Spec 版本升级为 v3.5（位置：文档头部）。
- 🔴 修正 `~sd auto` 边界：路径 0 只覆盖 A→C3；C3 完成后生成 D 提交确认页；用户确认后进入独立 D 阶段路径（后续统一命名为路径 5），D 不属于 `~sd auto` 自动范围。
- 🔴 修正路径 0 产物路径说明：B 阶段资产进入 `dataRoot/projects/{projectId}/assets/...`，单集制作产物进入 `dataRoot/projects/{projectId}/episodes/{episodeId}/...`。
- 🟡 将 Workspace 用户口径统一为后端对象：UI 不暴露 Workspace 对象名，只显示"数据目录 / 存储状态 / 环境检查"等用户语言；查看数据目录与存储状态从 P2 调整为 P0。
- 🟡 重写后端状态 → UI 用户语言映射，覆盖 `success`、`succeeded`、`cancelled`、`timeout`、`warning`、`connection_failed`、`capability_unknown`、`imported`、`expired` 等关键状态，并补充辅助标识规则。
- 🟢 同步 Design-Brief.md 与 Design-Delivery-Report.md 的源 Spec 标记到 v3.5，并补充路径 5 与质量评估协议待设计项。

**第二批修改（本次追加）**：

- 🔴 重写 [用户使用流程] **路径关系段**（位置：[用户使用流程] 开头）—— 原因：第一批关系图说"路径 D = 路径 0 的后续必经"，但路径 D 自描述里又承认"从路径 0 的 C3 完成页或路径 2 的手动 C3 完成状态进入"——关系图和详细描述自相矛盾。新关系图明确：路径 5 是**路径 0 和路径 2 共同的后续必经**，主链路 **路径 1 → 路径 0 → 路径 5 → 路径 4**；路径 2 替代路径 0 后仍接 路径 5 → 路径 4。
- 🔴 **路径 D 重命名为路径 5**（位置：[用户使用流程] 全文 + 引用处）—— 原因：第一批用字母 D 命名与路径 0/1/2/3/4 的数字编号风格不统一，读起来突兀。本次全文 replace_all 同步所有引用，CHANGELOG 第一批记录保留"路径 D"作为命名演化历史。
- 🔴 **路径 2 大幅瘦身**（位置：[用户使用流程] 路径 2）—— 原因：第一批路径 D 独立后，路径 2 仍然自描述完整 D→E 流程（第 8-15 步），与路径 5 和路径 4 职责重复且措辞不一致。新路径 2 只保留 A→C3 手动逐步（7 步描述 + 2 步分别接入路径 5 和路径 4），D/E 阶段交给路径 5 和路径 4，避免一份 Spec 描述同一件事两次。
- 🟢 路径 2 标题精确化为"**制作工作台手动逐步（A→C3）**"（位置：[用户使用流程] 路径 2 标题）—— 原因：第一批标题"从剧本进入资产与制作"是旧 v3.0 措辞，与新职责（仅 A→C3）不匹配。

---

## [v3.4] - 2026-05-14

**变更主题**：
- **第一批修订（承诺边界与对象拆分）**：修正 v3.3 继续审核发现的承诺边界问题，把"最终成片质量"作为北极星，把当前核心闭环的直接交付物明确为高质量 RoughCut + AudioSubtitlePlan + EpisodeDeliveryPackage，避免 E 阶段被误解为完整后期/发布母版生成器。
- **第二批修订（流程衔接与去重，本次追加）**：处理审视发现的 8 条剩余流程衔接和治理细节——补齐用户使用流程的路径关系、路径 0 D 阶段衔接、新增路径 4（Take 审核与单集粗剪交付）、产品概述/产品内核去重、Track 策略 ID 到 UI 用户语言映射、自动审核与人工审核两套机制区分。

### 新增

- 🔴 新增 ExportPackage 子类型口径：`EpisodeDeliveryPackage`（单集粗剪交付包）与 `ProjectMigrationPackage`（项目迁移/备份包）—— 原因：v3.3 虽拆了单集交付和项目迁移优先级，但对象说明、场景和权限仍混用"导出包/项目包"，容易误导实现。

**第二批新增（本次追加）**：

- 🔴 新增"路径 4：Take 审核与单集粗剪交付（核心成片质量环节）"（位置：[用户使用流程]）—— 原因：D 阶段后到 EpisodeDeliveryPackage 导出之间的关键环节，过去被路径 0 和路径 2 各用一句话带过，但实际包含 Take 审核 + Retake + 锁定 + RoughCut + RoughCut 级质量门禁 + AudioSubtitlePlan + 导出 7 大动作，是直接决定单集是否可交付的核心环节。新路径完整 10 步 + 4 条关键约束。
- 🔴 重写 [用户使用流程] 路径关系段（位置：[用户使用流程] 开头）—— 原因：v3.3 仅说"路径 0 是核心，路径 1-3 是补充"，读者读完路径 1（剧本生成）和路径 2（手动逐步）后不知道它们与路径 0 怎么衔接。新关系图明确：路径 1 = 路径 0 前置准备 / 路径 0 = 核心使用方式 / 路径 4 = 路径 0 后续必经 / 路径 2 = 路径 0 替代或介入 / 路径 3 = 横切；主链路是 **1 → 0 → 4**。
- 🔴 路径 0 新增 D 阶段并发提交说明（位置：[用户使用流程] 路径 0 第 9 步）—— 原因：v3.3 第 8 步说"用户确认 D"，第 9 步直接跳到"D 完成"，中间发生什么没说。新第 9 步明确：系统并发提交所有 Track 视频任务、按依赖排序（`continue_from_previous` 等前置完成）、自动按问题类型重试 ≤ 2 次、连续失败 blocked、用户可暂停整批或单独终止 Track。
- 🟢 新增"Track 生成策略 → UI 用户语言映射"章节（位置：[UI 布局] 后端状态映射之后）—— 原因：策略英文 ID（multi_reference_text 等）和中文用户语言（多参考直出等）在不同章节双语并存，dev-builder 不知道 UI 显示哪个。新映射表把策略 ID、UI 名称和系统决策典型场景三列对齐。
- 🟢 新增"两套审核机制并存"技术说明（位置：[技术说明] 末尾）—— 原因：~sd auto 内部的 4 套 review Skill/Agent 自动审核与 [辅助功能] "审核生成结果" 用户主动审核是两套机制，但 Spec 没区分。新说明明确两套各自的触发时机、Skill 名称、UI 入口；都写 QualityGate 但 source 字段区分（agent_self_review / manual_review）。

### 修改

- 🔴 将 Spec 版本升级为 v3.4（位置：文档头部）。
- 🔴 重写一句话定义、北极星指标、核心价值和产品内核（位置：[产品概述] / [重设计结论]）—— 原因：v3.3 仍承诺"可发布成片视频"，但核心闭环明确不做完整剪辑、调色、混音、字幕精修。新口径改为"当前核心闭环直接交付高质量可交付粗剪包，持续服务最终发布成片质量"。
- 🔴 修正核心闭环默认路径说明（位置：[重设计结论]）—— 原因：v3.3 写"6 步中的前 4 步默认通过 `~sd auto` 完成"，实际 `~sd auto` 只覆盖制作链路 A→C3，不覆盖 HTTP 进入、小说导入和文本生成。
- 🔴 将质量硬指标中的"嘴型同步"改为"嘴型状态"（位置：[核心闭环验收基线] / [成片质量测量口径] / [核心功能验收标准]）—— 原因：当前核心闭环不默认生成 TTS/口型/混音，要求严格嘴型同步会与"音频字幕后续接入"冲突。
- 🔴 修正 `~sd auto` 验收中的产物路径（位置：[核心功能验收标准]）—— 原因：B 阶段资产不能统一落到 episode 路径，资产应进入 `dataRoot/projects/{projectId}/assets/...`，单集制作产物才进入 episode 路径。
- 🔴 将当前核心闭环的 E 阶段命名从"成片收口"统一改为"粗剪交付"（位置：[重设计结论] / [功能需求] / [UI 布局] / [用户使用流程] / [AI 能力需求] / [技术说明] / [状态枚举]）—— 原因：E 阶段只生成 RoughCut、AudioSubtitlePlan 和 EpisodeDeliveryPackage，不能继续用会暗示发布母版的阶段名。
- 🟡 将 Track 问题类型中的 `lip_sync_error` 改为 `mouth_state_error`，并修正技术栈理由中的 36 对象模型旧口径。
- 🟡 将权限、版权、技术说明和明确不做里的"项目导出包/ExportPackage 导出检查"统一为 `ProjectMigrationPackage` 或 `EpisodeDeliveryPackage`。
- 🟡 修正对象/状态数量标注：核心对象为 37 个，状态枚举为 38 套。

**第二批修改（本次追加）**：

- 🟢 重写 [产品概述] "核心价值" 段去重（位置：[产品概述]）—— 原因：与"北极星指标"段重复表达"接近成片标准的粗剪与交付包，不是一堆需要从头整理的生成片段"。移除重复措辞，只保留"用户角色升级 + 完整成片由外部后期处理"两个独立信息点。
- 🟢 重写 [重设计结论] "产品内核" 段去重（位置：[重设计结论]）—— 原因：与 [产品概述] 头部四段重复表达产品定位。改为纯"边界澄清"段——只说"不是 X / 不是 Y / 只是 Z"和"具体承诺见 [产品概述]"，避免读者读到第 4 遍产品定位时麻木。
- 🟡 重命名应用场景 "Track 粗剪交付" 为 "Take 审核与单集粗剪交付"（位置：[应用场景]）—— 原因：原名称让读者误以为是 Track 单镜头粗剪，实际描述的是 D→E 全流程（Take 审核 + 单集粗剪组装）。

---

## [v3.3] - 2026-05-14

**变更主题**：清理 v3.2 审核发现的 13 处剩余不合理点（4 BLOCKER + 4 MAJOR + 5 MINOR），重点补齐新增 P0/P1 功能验收、统一产品内核与新定位一致、为 dev-builder/code-review 提供可执行验收标准。**不改方向，不动 36 对象模型本体，不动状态枚举具体取值，只清理 v3.2 后残留的内部不一致**。

### 新增

- 🔴 在核心功能验收标准表新增 4 行 P0/P1 验收（位置：[核心功能验收标准]）—— 原因：v3.1/v3.2 新增的"跨集 IP 资产时间线 / 执行一键全自动生产 / 自动管线运行视图 / 多视频供应商能力矩阵"4 个 P0/P1 功能没有验收口径，dev-builder/code-review 无据可依。
- 🔴 在辅助功能验收标准表新增 1 行 P1 验收"网文专用 Skill 包"（位置：[辅助功能验收标准]）—— 原因：v3.1 已列为 P1 辅助功能但缺验收。
- 🟡 在应用场景插入头号场景"一键全自动短剧生产"（位置：[应用场景]）—— 原因：路径 0 是 v3.1 核心差异化路径但应用场景表里完全没体现，所有 24 个场景都是"创作者手动做"，与"用户当导演不当操作员"的核心价值脱节。
- 🟡 在核心闭环开头插入"默认路径"说明（位置：[重设计结论] 核心闭环）—— 原因：核心闭环 6 步都是手动语气，没有声明前 4 步默认通过 `~sd auto` 完成，让 dev-planner 误以为"手动逐步"是主推路径。
- 🟢 在核心对象模型表追加"UI 暴露"列（位置：[重设计结论] 核心对象模型）—— 原因：原表 36 行只列说明不标 UI 暴露情况，读者必须跳到 [UI 布局] 映射表对照才能理解，容易把"不暴露"的对象误当 P0 功能展开。
- 🟢 在缺陷审视表追加"修正优先级"列（位置：[重设计结论] 缺陷审视与修正）—— 原因：30 条缺陷平级列没分级，读者会误以为所有缺陷同等优先；按 v3.1 后的产品定位重新打 P0/P1/P2，有些缺陷在本地模式与私有服务器模式优先级不同。
- 🟢 在 [技术说明] 补充"成本分析是数据汇总能力不是 AI 能力"说明（位置：[技术说明]）—— 原因：v3.2 把成本分析从 AI 能力表删除后，需要在技术说明里明确它是 SQL 聚合而非 LLM 能力。

### 修改

- 🔴 重写 v3.2 头部产品内核段（位置：[重设计结论] 产品内核）—— 原因：v3.0 时代措辞"HTTP 可访问的本地/私有化短剧生产工作台 + Track 粗剪"与 v3.1 改后的"电影级 AI 短剧生产管线 + 可发布成片"自相矛盾，读者读到此处会困惑产品到底做工作台还是做管线。新措辞统一指向"电影级管线 + 可发布成片"。
- 🔴 修改"执行质量门禁"验收标准引用成片质量测量口径（位置：[核心功能验收标准]）—— 原因：v3.2 已经在 [重设计结论] 加了 6 项硬指标的测量口径（角色一致性 ≥ 95% 等），但功能验收行还是 v3.0 旧措辞，没引用过去，dev-builder 实现 QualityGate 不知道具体怎么算分。新验收行明确"RoughCut 锁定前必须按测量口径完成 6 项检查，分项指标和分母必须写入 QualityGate"。
- 🔴 软化重设计原则"成本透明优先于事后追账"措辞（位置：[重设计结论] 重设计原则）—— 原因：v3.1 已撤掉成本透明化作为差异化卖点，[明确不做的事] 也写"不把成本透明化作为差异化卖点"，但 [重设计原则] 仍把成本透明列为产品级原则，自相矛盾。新措辞"成本可追溯：作为生产稳定性支撑，不作为产品对外差异化卖点"。
- 🟡 调整 3 条应用场景措辞（位置：[应用场景]）—— 原因：v3.2 改了策略责任和锚帧主语，但应用场景里"创作者把单集拆成多个 Track / 创作者在正式生成视频前需要把场景人物道具组合成 Track 起始画面 / 创作者批量生成后需要看到..."等措辞仍是用户手动语气。新主语统一为系统决策、创作者审核确认，体现"用户当导演"。
- 🟡 控件规范 4 处与 v3.1 优先级降级口径对齐（位置：[UI 布局] 控件规范）—— 原因：质量门禁控件没引用新测量口径；审计日志控件仍是 v3.0 严要求口径，与本地模式简化矛盾；成本提示/复盘是 P1 但措辞像"用户必看"。新措辞按"高级用户可选 / 本地模式简化 / 按测量口径展示"软化。
- 🟢 移除 [AI 能力需求] 表中"成本分析"条目（位置：[AI 能力需求]）—— 原因：成本分析是数据汇总能力（SQL 聚合 UsageRecord），不是 AI 能力（LLM/多模态），列在 AI 能力表里误导 dev-planner 把它当 AI 任务分配。
- 🟢 统一审计日志术语 UI 文案（位置：[核心功能验收标准]）—— 原因：设置页 UI 区域用"操作记录"，验收标准说"查看审计日志"，控件规范说"审计日志"——同一概念三种说法。新统一：UI 文案用"操作记录"，对象/规则继续用 AuditLog，并明确本地模式简化口径。
- 🟢 修改推荐技术栈理由措辞去除"与现有 Web 兼容"（位置：[技术方向]）—— 原因：v3.1 后产品已经是"重做版电影级管线"，技术栈不需要强调"与现有 Web 兼容"。新理由围绕新定位：Next.js SSR + API Routes 一体化 / TypeScript 类型化 36 对象 / SQLite 本地优先 / 文件产物仓库与数据库分离。
- 🔴 升级 Spec 版本号 v3.2 → v3.3，最后更新日期 2026-05-14（位置：文档头部）。

---

## [v3.2] - 2026-05-14

**变更主题**：修正 v3.1 审核发现的产品文档内部冲突，重点统一数据落盘边界、Track 策略责任、运行环境预检优先级、单集交付包与项目迁移包边界，并补上成片质量指标的测量口径。

### 新增
- 🔴 新增「成片质量测量口径」（位置：[重设计结论]）—— 原因：v3.1 已定义角色一致性、动作可读等硬指标，但缺少评估对象、AI 初筛、人工确认、分母口径和豁免规则，后续 dev-planner/code-review 难以验收。
- 🟡 在页面清单中补充「自动管线运行视图」和「跨集 IP 资产时间线」（位置：[补充说明] 页面清单）—— 原因：它们已是 P0 差异化页面，页面清单不能漏掉。

### 修改
- 🔴 将 Product-Spec 版本升级为 v3.2 + 最后更新日期 2026-05-14（位置：文档头部）。
- 🔴 修正 `~sd auto` 产物路径说明（位置：[用户使用流程] 路径 0）—— 原因：v3.1 仍写“落盘到 `novels/{projectName}/seedance/ep{N}/`”，与新后端 `dataRoot` 边界冲突。新口径改为通过后端写入 `dataRoot/projects/{projectId}/episodes/{episodeId}/...` 对应 Artifact 和数据库记录。
- 🔴 统一 Track 策略为“系统决策，Retake 时用户覆盖”（位置：[核心闭环] / [应用场景] / [核心功能验收标准] / [用户使用流程] / [AI 能力需求] / [技术说明]）—— 原因：v3.1 局部段落仍残留“用户选择策略 / 已选策略”，会误导设计和开发把策略选择器前置。
- 🔴 将「执行运行环境预检」从 P2 调整为 P0（位置：[功能需求]）—— 原因：RuntimeCheck 是 HTTP 入口、批量生成和粗剪导出的前置保障，不是可后置的普通设置页能力。
- 🔴 将 E 阶段单集交付包和项目迁移包拆清楚（位置：[功能需求] / [核心功能验收标准] / [AI 能力需求]）—— 原因：单集粗剪交付是 P0，项目迁移/备份恢复是 P2，不能用同一个“导出项目包”口径混在一起。
- 🟡 补充自动管线 B3 图片生成成本确认（位置：[用户使用流程] 路径 0）—— 原因：v3.1 只强调 D 阶段视频成本，但 B3 生图同样会实际扣费。
- 🟡 删除 UI 映射表中重复的 SecretRef 行（位置：[UI 布局] 后端对象映射）—— 原因：重复行会让设计/开发误以为存在两套密钥展示规则。
- 🟡 更新 Design-Brief.md 与 Design-Delivery-Report.md 的源 Spec 标记，并明确当前设计交付需要补齐 v3.2 新增页面。

---

## [v3.1] - 2026-05-13

**变更主题**：把产品北极星从"AI 短剧生产工作台"升级为"电影级 AI 短剧生产管线"。本次修订基于竞品全景调研（LibTV / LTX Studio / Higgsfield / 万兴剧厂 / LocalMiniDrama 等）与现有项目设计稿审视，重新校准了差异化定位、功能优先级和 UI 暴露口径。**90% 内容保留**，主要改头部、UI 布局和不做的事；36 对象模型、37 状态枚举、5 策略全部不动，只在 UI 层加映射表。

### 新增
- 🔴 新增「北极星指标」段落（位置：[产品概述]）—— 原因：之前没有一句话讲清楚"什么算这个产品成功"，导致功能堆砌缺乏聚焦标准。北极星 = 最终交付物是接近电影制作标准的可发布成片视频。
- 🔴 新增「成片质量（头号验收口径）」基线项（位置：[重设计结论] 核心闭环验收基线）—— 原因：原基线只验"系统能跑通"不验"成片能看"。新基线明确角色一致性 ≥ 95%、构图正确、动作可读 ≥ 90%、嘴型同步、跨镜连续、合规通过六项硬指标。
- 🔴 新增「跨集 IP 资产持续性」基线项（位置：[重设计结论]）—— 原因：跨集复用是行业唯一差异化，应作为成立基线之一。
- 🔴 新增「全自动管线」基线项（位置：[重设计结论]）—— 原因：`~sd auto` 后端已实现，应作为产品成立基线之一。
- 🔴 新增「跨集 IP 资产时间线」核心功能（P0）（位置：[功能需求]）—— 原因：B 阶段已有"复用/变体/新增"标记机制，但缺少项目级跨集可视化入口。这是行业唯一，必须作为差异化主页面。
- 🔴 新增「自动管线运行视图」核心功能和页面描述（P0）（位置：[功能需求] + [UI 布局]）—— 原因：`~sd auto` 是产品核心差异化但旧 Spec 完全没有对应 UI 描述，导致设计稿里没有这个页面。新增项目级独立大屏：管线流水线大图 + 实时日志 + 人工介入面板 + Pause/Resume/Stop。
- 🔴 新增「执行一键全自动生产 `~sd auto`」核心功能（P0）（位置：[功能需求]）—— 原因：自动管线作为差异化必须以独立功能名出现在 Spec 里，便于 dev-planner 拆 Phase 和 design-maker 出页。
- 🔴 新增「多视频供应商能力矩阵」核心功能（P1）（位置：[功能需求]）—— 原因：5 策略 × N 模型的能力适配过去只口头说"读取能力矩阵"，没有显式作为功能列出，导致 dev-builder 实现时容易遗漏。
- 🔴 新增「网文专用 Skill 包」辅助功能（P1）（位置：[功能需求]）—— 原因：网文垂直定位需要专用 Skill 模板（章节切分、爽点识别、付费点设计、女频/男频/古言/玄幻类型化），单独列出便于后续 Skill 开发。
- 🔴 新增「路径 0：一键全自动生产」用户使用流程（位置：[用户使用流程]）—— 原因：自动管线是产品默认推荐使用方式但旧 Spec 没有对应路径，导致 dev-builder 和 design-maker 都按"手动逐步"思维做。新路径明确 `~sd auto` 从启动到 D 阶段确认的完整 10 步流程。
- 🔴 新增「后端对象 → 用户可见概念」映射表（位置：[UI 布局]）—— 原因：36 对象模型保留不动，但 UI 不应全暴露。映射表告诉设计师哪些对象有用户入口（如 Project / Episode / Asset）、哪些半暴露（如 ProviderConfig 翻译成"模型供应商"）、哪些根本不暴露（如 AgentRun / Artifact / SchemaMigration）。
- 🔴 新增「后端状态 → UI 用户语言」映射表（位置：[UI 布局]）—— 原因：37 套状态枚举保留不动，但 UI 应收敛到 6 个用户语言（待规划/准备中/可锁定/已锁/失败/过期）。
- 🟡 新增 5 条「明确不做的事」（位置：[明确不做的事]）—— 原因：把核心边界写死，避免 dev-planner 和 design-maker 误把工程对象/状态全堆 UI 上。

### 修改
- 🔴 重写产品版本为 v3.1 + 最后更新日期 2026-05-13（位置：文档头部）。
- 🔴 重写「一句话定义」为"电影级 AI 短剧生产管线"（位置：[产品概述]）—— 原因：旧定义"本地优先 AI 生产工作台"是工具属性，竞品（LibTV、LocalMiniDrama）已占；新定义聚焦于"成片质量"和"流水线"两个产品锚点。
- 🔴 重写「目标用户」（位置：[产品概述]）—— 原因：旧用户列了 4 类并列，过于发散；新用户聚焦"网文改编创作者 + 短剧团队"，并明确他们的三件痛点（跨集一致性 / 单镜头质量 / 流程碎片化）。
- 🔴 重写「核心价值」（位置：[产品概述]）—— 原因：旧价值讲"流程整合"，新价值讲"用户从操作员升级为导演 + 制片人"，并明确交付物是接近电影标准的成片。
- 🔴 重写「差异化定位」（位置：[产品概述]）—— 原因：旧差异化"本地文件 / 开源可改 / Skill 文件化 / 多 Agent / 可编程供应商"几乎全部被 LibTV / LocalMiniDrama 占。新差异化锚定在"成片质量"本身，靠四件武器：① 导演级 Skill 工业化沉淀（10 方位朝向/台词时长公式/10 项连贯性校验/5 层框架/嘴型/时间轴）② 跨集 IP 持续性 ③ 全自动管线 + 关键节点确认 ④ 网文垂直。
- 🔴 给所有 30 项核心功能和 9 项辅助功能加 P0/P1/P2 优先级标记（位置：[功能需求]）—— 原因：旧 Spec 把所有功能都标"核心"，dev-planner 拿到无法分期。新分级按"是否直接影响成片质量"硬切：P0 直接决定成片成败 / P1 增强质量或支撑稳定 / P2 次要工程能力。
- 🔴 修改「选择 Track 生成策略」功能描述（位置：[功能需求] / [UI 布局] / [技术说明]）—— 原因：旧描述"用户在 D 节点确认策略"把策略选择责任完全甩给用户，把"AI 不听话"翻译成"用户操作累"。新描述改为"系统决策为主 + 用户在 Retake 时可覆盖"，UI 上首次生成不暴露策略选择器，只有不满意发起 Retake 时才在高级区出现。
- 🔴 修改 D 阶段子流程（位置：[技术说明]）—— 原因：D1 从"策略推荐与确认"改为"策略系统决策"，并新增系统策略决策规则（按 5 优先级匹配命中即停）。
- 🔴 修改「multi_reference_text」定位（位置：[技术说明]）—— 原因：旧描述允许它作为正式制作选项，新描述限定它仅用于草稿预览或时长极短场景。
- 🔴 重写 UI 整体布局描述（位置：[UI 布局]）—— 原因：旧布局列了 8 个并列 Tab/页面，没有主次。新布局明确分两类：生产主线（Tab 形式按 A→E 排列）+ 项目级独立大屏（自动管线运行视图、跨集 IP 资产时间线）。
- 🔴 修改设置页描述（位置：[UI 布局]）—— 原因：旧描述用 ProviderConfig / SecretRef / RuntimeCheck / AuditLog 等工程对象名作为 UI 区域名，对用户不友好。新描述要求用"模型供应商 / 语言模型 / 图像模型 / 视频模型 / 模型密钥 / 环境检查 / 操作记录"等用户语言。
- 🔴 修改剧本 Agent Tab 描述（位置：[UI 布局]）—— 原因：旧描述把聊天 + 故事骨架 + 改编策略 + 剧本三栏并列堆一屏，信息密度过高。新描述改为"对话 + 当前产物主区 + 顶部摘要条"。
- 🔴 修改项目卡片描述（位置：[UI 布局]）—— 原因：旧卡片只显示章节/剧本/Track 三个数字，看不出当前在 A/B/C/D/E 哪个阶段。新卡片加流水线进度条，让用户在卡片层就感受到"流水线"。
- 🟡 修改估算并记录生成成本（P1，明确不作差异化卖点）（位置：[功能需求]）—— 原因：用户明确撤回"成本透明化"作为产品故事主轴。
- 🟡 修改配置可编程供应商（P2，降级）（位置：[功能需求]）—— 原因：LibTV 已占该方向，不作主轴。
- 🟡 修改导入旧 novels 项目（P2，挂设置子菜单不作顶级页面）（位置：[功能需求]）—— 原因：重做版定位下，旧数据导入是一次性运维动作，不该占主导航。
- 🟡 修改 Operator 多角色 / AuditLog 完整审计（P2，本地模式简化）（位置：[功能需求]）—— 原因：本地单机模式下完整审计是过度工程，仅在私有服务器模式启用。

---

## [v3.0] - 2026-05-08

### 新增
- 🔴 新增「核心闭环验收基线」（位置：[重设计结论]）—— 原因：v2.9 的核心功能范围过大，缺少判断产品是否真正成立的最低通过线。
- 🔴 新增「核心功能验收标准」和「辅助功能验收标准」（位置：[功能需求]）—— 原因：原功能描述缺少可测试通过条件，后续 dev-builder 和 code-review 难以判断完成度。
- 🔴 新增「输入来源与解析边界」（位置：[补充说明]）—— 原因：原文只明确 `.txt` 小说导入，没有接住已有剧本文本、解析失败和外部输入扩展边界。
- 🔴 新增「权限与访问矩阵」（位置：[补充说明]）—— 原因：局域网/私有服务器访问、Operator 和审计日志已有对象，但缺少谁能做什么的最小权限规则。
- 🔴 新增「素材版权与合规边界」（位置：[补充说明]）—— 原因：Artifact 已记录 license 和 source，但缺少导出前风险提示、AI 商用条款和合规检查规则。
- 🔴 新增「成本预算与止损规则」（位置：[补充说明]）—— 原因：已有 UsageRecord 和成本估算，但缺少预算上限、超额确认、重试止损和失败成本区分。
- 🟡 新增 Operator 角色、Artifact 授权状态和预算状态枚举（位置：[补充说明] 状态枚举）—— 原因：权限、版权和成本规则需要状态承接。

### 修改
- 🔴 将产品版本升级为 v3.0（位置：文档头部）—— 原因：本次补齐验收、权限、输入、版权和成本等产品级边界，属于大版本修补。
- 🔴 将资产类型从 `character / scene / prop` 扩展为 `character / scene / prop / costume`（位置：[核心对象模型] / [功能需求] / [状态枚举]）—— 原因：服装/妆造对角色连续性和镜头复现是核心资产，不应含糊塞进道具。
- 🔴 将 QualityGate 验收覆盖扩展到 StoryOutline、AdaptationPlan 和 ScriptVersion（位置：[核心功能验收标准]）—— 原因：文本阶段脏输入会污染资产、分镜和视频，不应只在视频阶段做门禁。
- 🟡 将旧数据层文档 `docs/data-layer.md` 标注为旧 Web / Legacy Import 参考—— 原因：避免其“novels/ 是唯一数据源”的旧规则和 Product-Spec.md v3.0 的后端化边界冲突。

---

## [v2.9] - 2026-05-08

### 新增
- 🔴 新增 `SkillVersion` 和 `AgentRun` 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：产品差异化依赖 Skill 文件化提示词和 Agent 调度，AI 产物必须能追溯到具体 Skill、模型、输入和工具调用。
- 🔴 新增 AgentRun 状态和 SkillVersion 状态（位置：[补充说明] 状态枚举）—— 原因：Agent 执行过程和提示词版本生命周期不能混在 Task 或 ModelConfig 里。
- 🔴 新增 AgentRun / SkillVersion 数据产物路径（位置：[补充说明] 数据产物）—— 原因：AI 运行快照和 Skill 快照需要可读、可追溯 Artifact。
- 🔴 新增 Agent 运行编排和安全边界判断能力（位置：[AI 能力需求]）—— 原因：所有 AI 生成、修复、审核和工具调用都需要记录血缘，同时防止用户文本提示注入。

### 修改
- 🔴 明确每次 AI 生成、审核、修复、提示词编译和工具调用都必须写入 AgentRun（位置：[技术说明]）—— 原因：只记录最终对象和模型不足以复现生成过程。
- 🔴 明确 SkillVersion 变更不会自动让已有内容失效，但新生成必须记录当前 SkillVersion，并在重新生成时提示版本差异（位置：[技术说明]）—— 原因：避免提示词升级导致旧产物被误判或新产物不可解释。
- 🔴 明确小说、剧本、网页内容和用户消息只能作为内容上下文，不能覆盖系统 Skill、工具权限、访问控制或密钥策略（位置：[技术说明]）—— 原因：导入文本和用户提示可能包含提示注入。
- 🟡 将“读取和编辑 Skill”改为管理 SkillVersion（位置：[辅助功能]）—— 原因：Skill 也需要版本、回滚和审计。

---

## [v2.8] - 2026-05-08

### 新增
- 🔴 新增 `SecretRef` 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：ProviderConfig 已引用密钥，但密钥引用本身也必须有对象边界，避免密钥重新混入数据库、前端响应或导出包。

### 修改
- 🔴 将资产提示词润色和图片生成改为创建 `AssetVersion` 候选，而不是写入资产记录或展示“最新结果”（位置：[功能需求]）—— 原因：避免绕开资产版本体系，导致 Track 引用浮动图片。
- 🔴 将剧本导出改为生成 `ExportPackage` 或 Artifact 导出记录（位置：[辅助功能]）—— 原因：所有面向用户的导出都应可审计、可追溯，不能直接写本地文本文件绕过 manifest。
- 🔴 明确 `SecretRef` 只保存引用、用途、作用域、过期时间和审计信息，不保存可导出的密钥明文（位置：[技术说明]）—— 原因：项目导出、日志、审计和前端响应都不能泄露供应商密钥。
- 🟡 将通用“生成状态”控件说明改为 UI 映射 Task 状态，不新增底层任务状态枚举（位置：[控件规范]）—— 原因：避免和 v2.6 的 Task 状态表分裂。
- 🟡 将剩余“第一版”措辞改为“当前核心闭环/核心闭环”（位置：[应用场景] / [明确不做的事]）—— 原因：Product Spec 不拆开发 Phase，只表达产品边界。

---

## [v2.7] - 2026-05-08

### 新增
- 🔴 新增 `AudioSubtitlePlan`、`ExportPackage`、`UsageRecord`、`ProviderConfig` 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：E 阶段音频字幕、导出交付、成本复盘和供应商网关不能继续只是散文件或 ModelConfig 附属字段。
- 🔴 新增交付包导出和成本复盘应用场景（位置：[应用场景]）—— 原因：单集完成后需要交给剪辑/配音/迁移，生成后也需要看实际消耗和失败成本。
- 🔴 新增供应商状态、成本记录状态、ExportPackage 状态（位置：[补充说明] 状态枚举）—— 原因：供应商连接、成本对账和导出包生命周期都需要明确状态。
- 🔴 新增 AudioSubtitlePlan、ExportPackage、UsageRecord 数据产物路径（位置：[补充说明] 数据产物）—— 原因：E 阶段和成本记录需要稳定 Artifact 位置。

### 修改
- 🔴 将“生成音频与字幕计划”明确为生成 `AudioSubtitlePlan`，将“导出和恢复项目包”明确为生成 `ExportPackage`（位置：[功能需求]）—— 原因：避免后续接 TTS、字幕、剪辑工程和迁移包时重写对象模型。
- 🔴 将“估算生成成本”扩展为“估算并记录生成成本”，任务结束后写入 `UsageRecord`（位置：[功能需求] / [技术说明]）—— 原因：只做事前估算无法复盘策略成本和失败成本。
- 🔴 将设置页从单纯模型配置扩展为 ProviderConfig + ModelConfig 两层（位置：[UI 布局] / [技术说明]）—— 原因：供应商网关、能力探测和密钥引用不应塞进单个模型对象。
- 🟡 将“第一版不做完整非线编软件”改为“核心需求不做完整非线编软件”（位置：[功能需求]）—— 原因：Product Spec 不拆开发 Phase，只表达产品边界。

---

## [v2.6] - 2026-05-08

### 新增
- 🔴 新增 Task 状态枚举（位置：[补充说明] 状态枚举）—— 原因：技术说明已要求任务支持暂停、取消、恢复、重试和对账，状态表必须明确这些执行状态。
- 🔴 新增依赖有效性状态 `current / stale / needs_regeneration / blocked_by_missing_input`（位置：[补充说明] 状态枚举）—— 原因：生成结果的业务状态不能和上游是否过期混在一起。

### 修改
- 🔴 明确 Task 状态、Track/Take 业务状态、依赖有效性三者分离（位置：[技术说明]）—— 原因：一个 locked take 可能仍然因为上游剧本或资产变化而 stale，不能把它粗暴改成 failed。
- 🔴 明确默认 host 绑定 localhost，局域网访问必须显式开启，private_server / 公网 / 跨网段反代必须使用 HTTPS（位置：[技术说明]）—— 原因：HTTP 访问本地小说、模型密钥和视频素材时，只靠密码或 token 不够。

---

## [v2.5] - 2026-05-08

### 新增
- 🔴 新增 `DirectorAnalysis`、`DirectorPlan`、`Storyboard`、`PromptPack`、`TrackPlan` 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：A/C1/C2/C3/D 阶段不能只靠散文件串联，必须有可版本化、可追溯的制作产物对象。
- 🔴 新增制作产物状态，并将 `Seedance 阶段` 改为中性的 `制作阶段`（位置：[补充说明] 状态枚举）—— 原因：重做版不应被 Seedance 命名绑定，制作管线也需要表达 stale/locked 等状态。
- 🔴 新增 DirectorAnalysis、DirectorPlan、Storyboard、PromptPack、TrackPlan 的数据产物路径（位置：[补充说明] 数据产物）—— 原因：每个制作阶段都需要稳定 Artifact 位置和版本 ID。

### 修改
- 🔴 扩展统一 API 和数据库结构说明，覆盖 A/C/D 阶段制作产物对象（位置：[技术说明]）—— 原因：前端、Agent 和任务队列需要读写同一套生产对象，不能各自解析 Markdown/JSON 文件。
- 🔴 明确 `ScriptVersion` 变更会影响 `DirectorAnalysis`、`DirectorPlan`、`Storyboard`、`PromptPack` 和 `TrackPlan`（位置：[技术说明]）—— 原因：剧本变化会让下游分镜和视频计划过期，必须进入依赖失效检查。
- 🔴 将后端化数据结论中的旧对象列表扩展为文本、资产、制作管线、Track、任务、审计和 Artifact 全对象边界（位置：[重设计结论]）—— 原因：避免新后端只围绕 Project/Episode/Track 建模，漏掉文本和 A/C 阶段。
- 🟡 将 C2/C3 技术说明改为 `Storyboard` / `PromptPack` 向 `TrackPlan` 提供起点、终点、关键状态和连续性要求（位置：[技术说明]）—— 原因：D 阶段不能从长 prompt 反推生产图需求。
- 🟡 将 `prompts.md` 调整为 `exports/prompts-readable.md` 可读导出，不作为主数据源（位置：[补充说明] 数据产物）—— 原因：PromptPack/TrackPlan 才是主对象，Markdown 只能用于阅读和交付。

---

## [v2.4] - 2026-05-08

### 新增
- 🔴 新增 `SourceDocument`、`Chapter`、`StoryOutline`、`AdaptationPlan`、`ScriptVersion` 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：文本阶段不能只落成零散 Markdown 文件，否则剧本版本、下游依赖和回滚都会断。
- 🔴 新增 `Operator`、`AuditLog`、`RuntimeCheck` 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：局域网/私有服务器访问、敏感操作、运行环境预检都需要可审计和可诊断的对象。
- 🔴 新增文本版本管理、启动与生成前预检、敏感操作审计应用场景（位置：[应用场景]）—— 原因：真实使用中会频繁改剧本、切数据目录、重启服务、解锁和导出，不能只考虑生成按钮。
- 🔴 新增运行环境预检、剧本版本管理、审计日志查看等功能（位置：[功能需求] / [UI 布局]）—— 原因：把后端化、安全和版本规则落实到用户可见能力。
- 🔴 新增文本产物状态、RuntimeCheck 状态、Artifact 来源状态，以及任务中心页面（位置：[补充说明]）—— 原因：状态机和页面清单需要接住 v2.4 新对象。

### 修改
- 🔴 将硬编码 `02-prompts.md` / `track-plan.json` 的 Track 生成描述改为读取 TrackPlan 对象和 Artifact（位置：[功能需求]）—— 原因：重做版不能继续被旧 Seedance 文件名绑住。
- 🔴 将“系统写入剧本文件”改为创建 `ScriptVersion` 和可读 Artifact（位置：[用户使用流程]）—— 原因：剧本是版本化对象，不是一次覆盖的文件。
- 🔴 明确统一 API 覆盖文本、资产、Track、质量门禁、审计和 Artifact 对象（位置：[技术说明]）—— 原因：前端、Agent、任务队列不能各自读写不同数据源。
- 🔴 补充 HTTP 文件安全、上传限制、CSRF/CORS、会话过期、访问令牌撤销和路径白名单要求（位置：[技术说明]）—— 原因：HTTP 直接访问本地素材和密钥时，安全边界必须产品化。
- 🔴 移除状态枚举中“视频生成模式”和“Track 生成策略”的重复命名，只保留 `Track 生成策略`（位置：[状态枚举] / [控件规范]）—— 原因：重复枚举会导致实现时字段名分裂。
- 🟡 修正 A-D 管线状态为 A-E，并把旧“可审计文件”表述改为“数据库记录 + Artifact”（位置：[用户使用流程] / [技术说明]）—— 原因：E 成片收口是核心闭环，后端化后不能只靠文件路径审计。

---

## [v2.3] - 2026-05-08

### 新增
- 🔴 新增 `AssetVersion` 和 `QualityGate` 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：资产多版本、主参考图和锁定前质量检查必须成为一等需求，否则生成结果不可复现、成片审核会失控。
- 🔴 新增资产版本、任务中断恢复、误删恢复与清理等应用场景（位置：[应用场景]）—— 原因：真实制作中会反复生成候选素材、刷新页面、重启服务和误删内容，需求必须提前覆盖。
- 🔴 新增「管理资产版本与主参考图」「上传和导入参考素材」「执行质量门禁」「恢复和对账生成任务」「使用回收站和安全删除」功能（位置：[功能需求]）—— 原因：补齐资产版本、素材来源、任务幂等、质量检查和删除恢复闭环。
- 🔴 新增资产版本状态、删除状态、质量门禁状态和任务恢复状态（位置：[补充说明] 状态枚举）—— 原因：这些不是 UI 文案，而是后端状态机和验收条件。
- 🔴 新增 AssetVersion 产物目录、导出 manifest 和回收站产物（位置：[补充说明] 数据产物）—— 原因：项目迁移、恢复和清理需要稳定落点。

### 修改
- 🔴 明确 Track、Take、RoughCut 必须引用确定的 `assetVersionId` / `productionFrameId`，不能引用“最新资产图”（位置：[重设计原则] / [技术说明] / [明确不做的事]）—— 原因：浮动引用会导致旧镜头无法复现。
- 🔴 明确 Task 必须保存 `idempotencyKey`、`providerJobId`、`requestHash`、`attemptNo` 和远端对账状态（位置：[技术说明]）—— 原因：避免刷新、重复点击或 Worker 重启造成重复扣费和远端结果丢失。
- 🔴 明确素材来源与授权记录要求（位置：[技术说明]）—— 原因：上传、抽帧、生成、复用素材在商用、迁移和审计时需要 provenance。
- 🔴 明确导出必须使用一致性快照，manifest 记录 DB revision、schemaVersion、Artifact hash、运行中任务和敏感配置排除项（位置：[技术说明]）—— 原因：防止项目包恢复后数据库和文件不一致。
- 🔴 明确关键对象采用软删除优先，硬删除需二次确认并触发 Artifact 清理（位置：[技术说明]）—— 原因：保护误删恢复，同时避免大文件垃圾长期堆积。
- 🟡 修正“关键 Track”措辞为“策略确实需要锁定起止画面的 Track”（位置：[明确不做的事]）—— 原因：首尾帧、多关键帧不是升级档位，而是按镜头目标选择的生成策略。

---

## [v2.2] - 2026-05-08

### 新增
- 🔴 新增「缺陷审视与修正」章节（位置：[重设计结论]）—— 原因：v2.1 已定义后端化方向，但缺少保证系统长期不乱的数据一致性、迁移、任务依赖、安全和 Artifact 生命周期规则。
- 🔴 新增 `AccessCredential`、`SchemaMigration` 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：HTTP 私有访问和后端数据结构演进都需要一等对象。
- 🔴 新增「变更影响检查」「项目导出与恢复」应用场景和功能（位置：[应用场景] / [功能需求]）—— 原因：上游修改会使下游 Track、take、粗剪过期，项目迁移也需要 manifest、数据库切片和 Artifact 校验。
- 🔴 新增依赖分析、项目打包与校验能力（位置：[AI 能力需求]）—— 原因：需要自动判断哪些下游产物 stale，并生成迁移/导入兼容报告。

### 修改
- 🔴 明确 `schemaVersion`、`artifactManifest`、`SchemaMigration` 和 `revision` / 乐观锁要求（位置：[技术说明]）—— 原因：避免后端 schema 变化、多标签页编辑和局域网访问造成数据损坏。
- 🔴 明确上游变更必须触发下游 `stale` / `needs_regeneration` 检查（位置：[技术说明]）—— 原因：剧本、资产、TrackSegment、模型配置变化后，旧视频不能继续被误认为有效。
- 🔴 明确 Task 必须支持依赖 DAG、取消、暂停、恢复、重试、超时、并发限制和失败清理（位置：[技术说明]）—— 原因：长任务与前后 Track 依赖不是简单状态字段能解决。
- 🔴 明确 Artifact 生命周期管理和 locked 产物只读规则（位置：[技术说明]）—— 原因：避免大文件垃圾、数据库悬空、已锁定 take 被覆盖。
- 🔴 明确访问凭据与模型密钥不进入默认项目导出（位置：[技术说明]）—— 原因：防止迁移/共享项目时泄露敏感配置。

---

## [v2.1] - 2026-05-06

### 新增
- 🔴 新增后端化数据结论（位置：[重设计结论]）—— 原因：重新做产品时，`novels/` 不能继续作为业务边界，只能作为旧数据目录和迁移来源。
- 🔴 新增 Workspace、Artifact、LegacyImport 核心对象（位置：[重设计结论] 核心对象模型）—— 原因：新产品需要后端工作空间、文件产物索引和旧项目导入记录来支撑完整项目形态。
- 🔴 新增「后端项目数据仓库」应用场景、「管理后端工作空间」「导入旧 novels 项目」核心功能（位置：[应用场景] / [功能需求]）—— 原因：需要把文件夹工具升级为有 API、状态机、任务队列和数据契约的后端产品。
- 🔴 新增后端数据设置区域（位置：[UI 布局]）—— 原因：用户需要知道 `dataRoot`、数据库、素材仓库、旧数据导入和存储占用。
- 🔴 新增后端存储模式与 Legacy 导入状态枚举（位置：[补充说明] 状态枚举）—— 原因：本地混合存储、私有服务器部署和旧数据导入需要明确状态。

### 修改
- 🔴 将数据存储从“当前 Web 文件系统 + 可选 SQLite”改为“混合后端存储”（位置：[技术方向]）—— 原因：结构化状态进入数据库，大文件进入 Artifact 仓库，前端不能直接读写 `novels/`。
- 🔴 明确现有 `path.join(process.cwd(), '..', 'novels')` 规则只适用于旧 Web 兼容和 legacy import，不应作为新对象模型长期约束（位置：[技术说明]）—— 原因：重做版需要可配置 `dataRoot` 和后端 API 边界。
- 🔴 将数据产物表从旧 `seedance/...` 路径改为 `dataRoot/projects/{projectId}/...` 后端仓库结构（位置：[补充说明] 数据产物）—— 原因：新产品不能继续被旧 `novels/` 路径和项目名目录绑住。
- 🔴 在明确不做事项中加入“不把 `novels/` 作为新产品核心数据模型”和“不让前端直接读写项目文件”（位置：[明确不做的事]）—— 原因：避免重做版继续退化成文件夹读写工具。

---

## [v2.0] - 2026-05-06

### 新增
- 🔴 新增「重设计结论」章节（位置：[重设计结论]）—— 原因：用户确认产品需要重新设计，相当于重新做，需求文档必须从增量补功能切换到重新定义产品内核和边界。
- 🔴 新增核心闭环定义（位置：[重设计结论]）—— 原因：明确产品必须从 HTTP 访问、剧本、资产、Track 生产到 E 成片收口形成完整闭环，而不是只做若干生成按钮。
- 🔴 新增核心对象模型表（位置：[重设计结论]）—— 原因：重做版应先稳定 Project / Episode / Asset / Track / TrackSegment / ProductionFrame / Take / RoughCut / ModelConfig / Task 等对象，再设计页面。
- 🔴 新增重设计原则（位置：[重设计结论]）—— 原因：避免旧页面结构、无限画布或 Toonflow-app 完整能力反向绑架新产品。

### 修改
- 🔴 明确现有 Web、CLI Seedance pipeline 和 Toonflow-app 只是需求来源与经验参考，不是兼容目标（位置：[重设计结论] / [技术方向] / [技术说明]）—— 原因：重新做产品时，不能被旧 Tab、旧 API、旧页面结构锁住。
- 🔴 明确新产品信息架构应围绕 Project / Episode / Asset / Track / Take / RoughCut 重新设计（位置：[技术说明]）—— 原因：页面可以重画，数据对象不能反复推翻。
- 🔴 在明确不做事项中加入“不以旧页面/旧 API 兼容为目标”和“不把无限画布作为核心入口”（位置：[明确不做的事]）—— 原因：控制重做版边界，避免需求继续膨胀。

---

## [v1.9] - 2026-05-06

### 新增
- 🔴 新增「HTTP Web 直接访问」应用场景和「通过 HTTP 访问工作台」核心功能（位置：[应用场景] / [功能需求]）—— 原因：产品必须支持通过浏览器地址直接访问，不能只依赖桌面壳或命令行。
- 🔴 新增访问入口说明（位置：[UI 布局] 区域内容）—— 原因：服务启动后需要展示本机地址、可选局域网地址、端口和安全提示。
- 🔴 新增 HTTP 访问模式枚举 `localhost / lan / private_server`（位置：[补充说明] 状态枚举）—— 原因：本机、局域网、私有服务器三种访问方式的安全要求不同。

### 修改
- 🔴 将产品类型和部署方式明确为 HTTP Web 服务优先，Electron 仅作为可选桌面壳（位置：[技术方向]）—— 原因：Web HTTP 入口是一等交付形态，不是后续包装选项。
- 🔴 新增 HTTP 访问技术约束：host/port/base URL、局域网访问控制、反向代理兼容、素材 HTTP 预览和视频范围请求（位置：[技术说明]）—— 原因：生成素材、SSE、任务轮询和视频预览都必须在 HTTP 访问场景下可用。
- 🔴 明确不做“只能通过 Electron 打开的桌面应用”，也不默认开放公网访问（位置：[明确不做的事]）—— 原因：既要支持直接 HTTP 访问，又不能把本地小说、模型密钥和生成素材裸露到网络。

---

## [v1.8] - 2026-05-05

### 新增
- 🔴 新增「Track 成片收口」和「音频与字幕准备」应用场景（位置：[应用场景]）—— 原因：只生成 T01/T02/T03 文件不是成片，必须考虑锁定 take、粗剪、字幕和音频交付。
- 🔴 新增「审核和锁定 Track take」「组装单集粗剪」「生成音频与字幕计划」「估算生成成本与排队风险」功能（位置：[功能需求]）—— 原因：视频生成后的接受、重试、导出、成本控制是生产系统必须有的闭环。
- 🔴 新增 E 成片收口节点（位置：[功能需求] / [UI 布局] / [用户使用流程] / [状态枚举]）—— 原因：D 阶段输出的是 Track take，不是单集成片。
- 🔴 新增 Track take 状态、Track 问题类型、成片收口状态枚举（位置：[补充说明] 状态枚举）—— 原因：需要记录每次生成结果是否接受、为何重试、粗剪是否可导出。
- 🔴 新增 take、selected-takes、audio-plan、rough-cut、exports 数据产物（位置：[补充说明] 数据产物）—— 原因：成片收口需要保存锁定视频、音频字幕计划、粗剪清单和导出结果。

### 修改
- 🔴 明确 D 阶段输出不等于成片，只有 locked take 才进入 E 阶段（位置：[技术说明]）—— 原因：同一 Track 可能有多次生成结果，必须先选择和锁定。
- 🔴 明确 Track take 必须记录生成血缘（位置：[技术说明]）—— 原因：没有模型、参数、输入素材、提示词、成本和父 take，重试与问题定位会变成猜谜。
- 🔴 明确重试应按问题类型处理，不只是重新生成（位置：[技术说明]）—— 原因：角色漂移、构图错误、动作错误、嘴型错误、道具缺失需要不同修复策略。
- 🔴 明确音频/字幕是独立轨道，不硬塞进 D 视频生成（位置：[技术说明] / [明确不做的事]）—— 原因：不同视频模型对同步音频和口型能力差异很大，不能默认支持。
- 🔴 明确 E 阶段只是轻量粗剪收口，不做完整非线编（位置：[技术说明] / [明确不做的事]）—— 原因：避免产品范围膨胀成剪辑软件。

---

## [v1.7] - 2026-04-29

### 新增
- 🔴 新增「视觉锚帧制作」应用场景与「生成 Track 视觉锚帧」核心功能（位置：[应用场景] / [功能需求]）—— 原因：正式制作不能只靠多张参考图和 prompt 直出，需要一张具体起始画面来锁定构图、人物站位和道具空间关系。
- 🔴 新增 Track 制作目标枚举 `draft_preview / production_final`（位置：[补充说明] 状态枚举）—— 原因：草稿试跑和正式成片的控制成本不同，不能混成一套规则。
- 🔴 新增 Track 生产图来源枚举 `generated / uploaded / extracted / reused`（位置：[补充说明] 状态枚举）—— 原因：视觉锚帧、尾帧、关键帧可能来自生成、上传、视频抽帧或复用，必须可审计。
- 🔴 新增 `seedance/ep{N}/frames/{TNN}/start.png` 数据产物（位置：[补充说明] 数据产物）—— 原因：正式制作 Track 需要独立保存视觉锚帧。

### 修改
- 🔴 将 `multi_reference_text` 定位为草稿预览、快速试跑或模型能力验证（位置：[技术说明]）—— 原因：多张参考图能提供身份和风格，但不能稳定定义具体构图、人物站位和道具空间关系。
- 🔴 将 `start_frame` 调整为大多数正式制作 Track 的基础策略（位置：[技术说明]）—— 原因：视觉锚帧更适合作为可控成片的起点。
- 🔴 明确视觉锚帧不提前进入 B 资产管理阶段，而是在 D2 按具体 Track 生成（位置：[技术说明]）—— 原因：B 阶段只管理可复用资产，Track 锚帧是具体镜头生产图，提前批量生成会制造大量无用图片。
- 🔴 在 D 阶段流程中加入制作目标选择与正式制作锚帧准备（位置：[用户使用流程] 路径 2）—— 原因：不同制作目标对应不同成本和控制要求。

---

## [v1.6] - 2026-04-28

### 新增
- 🔴 新增「策略驱动的视频制作」应用场景（位置：[应用场景]）—— 原因：`start_frame`、`first_last_frame`、`multi_keyframe`、`continue_from_previous` 会改变制作步骤，不是一个 UI 下拉框就能解决。
- 🔴 新增「准备策略所需视觉输入」核心功能（位置：[功能需求] / [用户使用流程]）—— 原因：选择策略后必须先补齐参考资产、首帧、尾帧、中间关键帧、上一段尾帧和供应商能力依赖。
- 🔴 新增 D 阶段策略驱动子流程 D0-D4（位置：[技术说明]）—— 原因：将 Track 检查、策略确认、素材准备、视频提交、预览审核拆清楚，避免 `start_frame` 等策略反向污染 A/B/C 全链路。
- 🔴 新增 Track 策略依赖状态枚举（位置：[补充说明] 状态枚举）—— 原因：D 节点必须知道当前 Track 是缺素材、等待上一段、生成中、已 ready、被阻塞还是失败。
- 🔴 新增 `seedance/ep{N}/frames/{TNN}/previous-tail.png` 数据产物（位置：[补充说明] 数据产物）—— 原因：`continue_from_previous` 需要保存从上一段视频抽取的尾帧。

### 修改
- 🔴 明确 C2/C3 需要为 D 阶段提供 Track 起点视觉状态、终点视觉状态、关键中间状态和连续性要求（位置：[技术说明]）—— 原因：否则 D 阶段无法合理生成首帧、尾帧或多关键帧。
- 🔴 明确策略素材分为“项目资产图”和“Track 生产图”（位置：[技术说明] / [明确不做的事]）—— 原因：角色/场景/道具参考图可跨集复用，首帧/尾帧/关键帧只服务具体 Track，不能混进资产库。
- 🔴 修改 Track 控制状态，加入 `preparing_inputs`（位置：[补充说明] 状态枚举）—— 原因：策略素材准备是视频生成前的独立状态，不应和 ready/generating 混在一起。
- 🔴 扩展 `track-plan.json` 说明，加入生成策略与策略依赖（位置：[补充说明] 数据产物）—— 原因：后续任务队列、失败重试和供应商适配都需要读取这部分结构化数据。

---

## [v1.5] - 2026-04-28

### 新增
- 🔴 新增 `continue_from_previous` 生成策略（位置：[技术说明] / [补充说明] 状态枚举）—— 原因：连续 Track 之间常需要用上一段尾帧作为下一段首帧，否则拼接后人物站位和构图容易跳变。
- 🔴 新增 Track 生成策略推荐能力（位置：[AI 能力需求]）—— 原因：用户不应该每次凭感觉决定是否用首帧、首尾帧或多关键帧，系统需要基于动作复杂度、时长和连续性给出推荐理由。

### 修改
- 🔴 将“关键帧层级”改为“Track 生成策略选择”（位置：[应用场景] / [功能需求] / [UI 布局] / [技术说明]）—— 原因：首帧、首尾帧、多关键帧不是高低层级，而是不同镜头目标对应的生产方法。
- 🔴 明确每个 Track 都必须显式记录生成策略（位置：[技术说明] / [状态枚举]）—— 原因：后续 UI、任务队列、供应商适配和失败重试都依赖这个字段，不能只存在于 prompt 文本里。
- 🔴 将供应商不兼容处理从“静默降级”改为“提示换模型、换策略或拆分 Track”（位置：[用户使用流程] 路径 2）—— 原因：静默降级会让用户误以为系统按首尾帧生成了，实际却变成普通多参考生成。

---

## [v1.4] - 2026-04-28

### 新增
- 🔴 新增「关键 Track 关键帧控制」应用场景和功能（位置：[应用场景] / [功能需求] / [用户使用流程]）—— 原因：外部 AI 视频工作流普遍用首帧、首尾帧或关键帧提升关键镜头可控性，但不会要求每个短片段都先做首尾帧。
- 🔴 新增 Track 关键帧模式与状态枚举（位置：[补充说明] 状态枚举）—— 原因：D 节点需要记录每个 Track 是无关键帧、仅首帧、首尾帧还是多关键帧，以及关键帧是否已确认和锁定。
- 🔴 新增 `seedance/ep{N}/keyframes/{TNN}/...` 数据产物（位置：[补充说明] 数据产物）—— 原因：首帧、尾帧、多关键帧及其提示词需要独立保存，不能混在资产参考图或最终视频里。

### 修改
- 🔴 将视频生成模式从 `multi_reference_text / storyboard_image_reference / first_last_frame` 调整为 `multi_reference_text / start_frame / first_last_frame / multi_keyframe`（位置：[补充说明] 状态枚举）—— 原因：当前真正需要区分的是 Track 控制等级，而不是抽象的“分镜图辅助模式”。
- 🔴 明确“不得默认为每个 Track 生成首尾帧”（位置：[技术说明] / [明确不做的事]）—— 原因：全量首尾帧会增加成本、审核和人工确认负担，且关键帧差异过大时会导致视频过渡异常。
- 🔴 在 D 阶段流程中增加关键帧确认与供应商降级逻辑（位置：[用户使用流程] 路径 2）—— 原因：不同视频模型对首帧、首尾帧、多关键帧支持不同，产品需要按能力适配而不是硬编码一种流程。

---

## [v1.3] - 2026-04-28

### 新增
- 🔴 新增「Track 内镜头段控制」核心功能（位置：[功能需求] / [UI 布局] / [用户使用流程] / [技术说明]）—— 原因：仅用 `T01 + 参考图 + 长 prompt + 生成按钮` 会把 10-15 秒视频变成黑盒抽卡，无法控制叙事节奏、人物动作和镜头落点。
- 🔴 新增 `track-plan.json` 数据产物（位置：[补充说明] 数据产物）—— 原因：D 阶段需要保存 Track 与 Track 内时间段控制数据，不能只保存最终 `02-prompts.md` 和视频任务。
- 🔴 新增 Track 控制状态枚举（位置：[补充说明] 状态枚举）—— 原因：D 节点需要区分草稿、待检查、可生成、生成中、已生成、已锁定和失败状态。

### 修改
- 🔴 将 D 节点从简单 Track 列表调整为“Track 行可展开 + 秒级时间段表格/卡片”（位置：[UI 布局] 控件规范）—— 原因：生成单位可以是 Track，但控制单位必须更细，否则用户无法处理 15 秒内的多段叙事。
- 🔴 在视频生成前增加 Track 检查与拆分步骤（位置：[用户使用流程] 路径 2）—— 原因：复杂 Track 应先拆清楚画面目标、动作、镜头、台词/嘴型和参考资产，再提交视频模型。
- 🔴 明确 Track 是视频 API 生成单位，Track 内时间段/镜头段是用户控制单位（位置：[技术说明]）—— 原因：避免把“Track 级生成”误解为“一段不可编辑长 prompt 直接生成”。

---

## [v1.2] - 2026-04-28

### 修改
- 🔴 明确产品主流程只承诺 `multi_reference_text`，不并列支持三套视频生成流程（位置：[技术说明] / [补充说明] 状态枚举）—— 原因：避免把供应商适配能力误读成产品默认功能范围。
- 🔴 明确无限画布不是当前主流程必要条件，当前先用管线节点图串流程（位置：[技术说明]）—— 原因：无限画布是生产视图，不应替代底层流程状态机。

---

## [v1.1] - 2026-04-28

### 新增
- 🔴 新增「Track 级多参考视频片段生成」核心功能（位置：[功能需求]）—— 原因：明确当前系统的视频主路径是“场景/人物/道具参考图 + 叙事提示词”生成短视频，不是首尾帧生成。
- 🔴 新增视频生成模式枚举（位置：[补充说明] 状态枚举）—— 原因：区分默认多参考模式、分镜图辅助模式和首尾帧兼容模式。
- 🔴 新增 D 节点 Track 视频任务数据产物（位置：[补充说明] 数据产物）—— 原因：视频生成需要记录 `video-tasks.json` 与 `T{NN}.mp4` 片段。

### 修改
- 🔴 将 D 阶段从笼统“视频生成”细化为“Track 视频生成”（位置：[功能需求] / [用户使用流程] / [UI 布局]）—— 原因：避免后续设计和开发误以为只需要一个泛化视频按钮。
- 🔴 将视频 AI 能力描述从泛化多模态改为 Track 级多参考生成（位置：[AI 能力需求]）—— 原因：AI 能力必须对应真实输入输出。
- 🔴 明确首尾帧不是当前主流程，只作为兼容或后续增强模式（位置：[技术说明] / [明确不做的事]）—— 原因：当前资产管线没有首尾帧，强行引入会增加不必要复杂度。

---

## [v1.0] - 2026-04-28
- 初始版本（基于现有 Web 项目、Toonflow-app 和竞品调研整理）
