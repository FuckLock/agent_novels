# Product Spec — Toonflow Web

**生成日期**：2026-04-28
**最后更新**：2026-05-14
**版本**：v3.5

---

## 产品概述

**一句话定义**：Toonflow Web 是面向网文改编与短剧创作者的 **电影级 AI 短剧生产管线**——"一部小说 + 一支 Agent 队伍 + 一条工业流水线 = 一套可交付后期的高质量短剧粗剪包"。我们不做工具集合，做产线本身；不比"能不能生成"，比"生成出来能不能看、能不能继续交付"。

**北极星指标**：最终服务目标是接近电影制作标准的可发布短剧成片；当前核心闭环的直接交付物是 **高质量 RoughCut + AudioSubtitlePlan + EpisodeDeliveryPackage**，交给剪辑、配音、字幕或外部后期继续完成发布母版。所有功能、流程、UI、模型选择都以"能否稳定提升最终成片质量"为衡量标准——能稳定提升成片质量的优先做，不影响成片质量的延后或砍掉。

**目标用户**：网文改编创作者与短剧团队——他们想用 AI 把小说做成 50-100 集可发布短剧，但被三件事卡住：① 跨集角色/场景/道具一致性崩坏（脸变了、服装变了、空间变了）；② 单镜头质量不可控（构图飘、动作错、嘴型乱）；③ 生产流程碎片化（剧本工具→提示词工具→视频工具→剪辑工具来回切，没有连续上下文）。Toonflow 把这三件事作为头号目标解决。

**核心价值**：把 AI 短剧从"碎片工具拼凑"升级为"电影级生产管线"。用户的角色从"操作员"变成"导演 + 制片人"——Agent 队伍负责执行（A 导演分析 → B 资产塑造 → C 分镜规划 → D 视频生成 → E 粗剪交付），用户只在关键节点判断质量、调整方向、锁定结果。完整调色、混音、字幕精修和发布母版可交给外部后期或后续能力。

**差异化定位**：市面赛道分四类：(1) **一键全自动型**（VIVA Short、即梦+剪映、万兴剧厂）——黑盒模板化、3 步出片，但同质化严重、镜头质量受限于通用模型；(2) **专业可控型**（Runway、LTX Studio）——工具属性强、用户每步参与，但成片跨镜连续性差；(3) **节点画布型**（LibTV、Higgsfield Canvas）——极致灵活、Agent 友好，但门槛高、缺导演工艺沉淀；(4) **本地开源型**（LocalMiniDrama）——数据本机但功能单薄。**Toonflow 的差异化锚定在"成片质量"本身**，靠四件武器：

1. **导演级 Skill 工业化沉淀**：10 方位朝向 / 台词时长公式 / 10 项连贯性校验 / 5 层框架 / 去人化 / 柔化词 / 嘴型口型 / 时间轴——这些是真实导演经验落成代码，竞品有 Agent 协议但没有 Skill 内容。
2. **跨集 IP 资产持续性**：B 阶段强制"复用/变体/新增"标记，让 50 集短剧角色/场景/道具/服装跨集不漂移——行业唯一。
3. **全自动管线 + 关键节点用户确认**：`~sd auto` 一条指令跑完 A→C3，用户在每阶段产物处当导演而非操作员；策略选择默认系统决策，用户只在 Retake 时介入。
4. **网文改编垂直**：6 参数初始化（totalEpisodes / episodeDuration / chapterRange / platform / style / paywall）+ 网文专用 Skill 包（章节切分、爽点识别、付费点设计、女频/男频/古言/玄幻模板）。

**我们不和黑盒生成器比"快"，比"能看"。**

**输入来源**：
- 现有 Web 项目：`web/app` 页面、API Routes、数据层、旧版 `web/PRD.md`、`docs/PRD-asset-canvas.md`。
- Toonflow-app：`web/Toonflow-app/README.md`、后端路由、Agent/Skill/供应商/记忆/事件图谱相关代码。
- 竞品观察：VIVA Short、NovelVids、Cutflow、ShortPlay Studio、StoryIntoVideo 等产品均覆盖剧本、分镜、图像或视频链路。
- 视频模型参考：Seedance 2.0 官方说明支持文本、图像、音频、视频混合输入；第三方 API 文档也显示多参考输入与异步任务轮询是主流接入方式。

**竞品参考链接**：
- [VIVA Short](https://vivashort.com/)
- [NovelVids](https://novelvids.com/)
- [Cutflow](https://www.cutflow.so/)
- [ShortPlay Studio](https://www.shortplaystudio.com/)
- [StoryIntoVideo](https://storyintovideo.com/)
- [Runway Image to Video Prompting Guide](https://help.runwayml.com/hc/en-us/articles/48324313115155-Image-to-Video-Prompting-Guide)
- [LTX Studio](https://ltx.studio/)
- [Krea Video Models](https://docs.krea.ai/user-guide/features/video)
- [Seedance 2.0 官方发布](https://seed.bytedance.com/en/blog/seedance-2-0-official-launch)
- [Runware Seedance 2.0 API 文档](https://runware.ai/docs/models/bytedance-seedance-2-0)
- [Replicate Seedance 2.0 模型说明](https://replicate.com/bytedance/seedance-2.0)

## 重设计结论

这次应按“重新做产品”处理，而不是给现有 Web 页面补功能。现有 Web、CLI Seedance pipeline 和 Toonflow-app 都只能作为需求来源与经验参考，不能反向要求新产品兼容旧页面结构、旧交互路径或旧实现细节。

**产品内核（边界澄清）**：Toonflow Web 不是 AI 聊天生成器，不是无限画布素材管理器，也不只是"Track 粗剪导出工具"。所有功能和 UI 都服务于"**生产线稳定输出高质量可交付粗剪，并降低最终成片质量风险**"这个唯一目标。具体的产品承诺、目标用户、北极星指标和交付物见 [产品概述]，本段只做边界澄清，避免与产品概述重复表达。

**核心闭环**必须完整成立：

**默认路径**：制作链路中的 A→C3 默认通过 `~sd auto` 一键全自动完成（A 导演分析 → B 资产塑造 → C1 导演规划 → C2 分镜表 → C3 分镜提示词），用户只在 D 阶段视频生成提交前确认一次。HTTP 进入、小说/剧本导入、文本生成和项目初始化仍是前置项目准备流程；手动逐步路径作为介入和调试模式保留，不是主推使用方式。

1. 用户通过 HTTP 进入项目工作台。
2. 用户导入小说或剧本，生成故事骨架、改编策略和分集剧本。
3. 用户塑造角色、场景、道具、服装/妆造等可复用视觉资产。
4. 系统生成导演分析、导演规划、分镜表和 Track 计划。
5. 用户在 D 阶段按 Track 控制时间段、制作目标、视觉锚帧、关键帧和视频 take；生成策略由系统决策，用户只在 Retake 时覆盖。
6. 用户在 E 阶段审核 take、锁定结果、生成粗剪、音频/字幕计划和导出清单。

**核心闭环验收基线**：

这不是开发 Phase，也不是一期范围拆分；它定义“产品是否已经成立”的最低通过线。`dev-planner` 可以分期实现，但任何阶段性演示都不能把未通过的项伪装成完整产品。

| 基线项 | 必须通过的验收口径 |
|--------|--------------------|
| **成片质量（头号验收口径）** | 单集 RoughCut 必须通过以下硬指标，才能进入 EpisodeDeliveryPackage：① **角色一致性**：主要角色跨 Track 脸/服装/发型一致率 ≥ 95%；② **构图正确**：镜头景别/朝向与分镜表偏差不超过 1 档；③ **动作可读**：动作意图被观众理解的比例 ≥ 90%；④ **嘴型状态**：有台词镜头的开闭口状态与对白/旁白意图不冲突，无明显反向口型；⑤ **跨镜连续性**：相邻 Track 空间、光线、人物站位无明显跳跃；⑥ **合规通过**：不出现质量门禁标记 compliance_failed 的内容。任一指标未通过且未人工豁免，不算"可交付后期"。|
| HTTP 入口与数据目录 | 服务能通过 `localhost` 打开；后端工作空间初始化完成；用户可在设置或环境检查中看到 `dataRoot`、数据库、Artifact 仓库和 RuntimeCheck 状态，但不直接看到 Workspace 对象名 |
| 文本输入与剧本版本 | 用户能新建项目，导入小说原文或已有剧本文本，生成或创建 ScriptVersion，并能编辑、对比、回滚和锁定 |
| 资产版本闭环 | 用户能从剧本/导演分析提取角色、场景、道具、服装/妆造，上传或生成 AssetVersion，并选择可被 Track 精确引用的 canonicalVersion |
| 跨集 IP 资产持续性 | B 阶段强制对每个资产标记"复用/变体/新增"；同一角色在多集间通过 assetVersionId 精确引用同一 canonicalVersion；跨集一致性可在"跨集 IP 资产时间线"页查看 |
| A/C/D 制作链路 | 单集能生成 DirectorAnalysis、DirectorPlan、Storyboard、PromptPack、TrackPlan；TrackSegment 可编辑；每个 Track 有明确生成策略和依赖清单 |
| 全自动管线 | `~sd auto` 可一条指令跑完 A→C3，自动管线运行视图实时展示各阶段进度、当前在跑哪一步、可暂停/恢复；关键节点（A 完成 / B 完成 / C3 完成 / D 提交前）允许用户介入 |
| 视频 take 闭环 | Track 依赖 ready 后能提交视频任务；Task 可轮询/恢复；Take 保存血缘、成本和问题类型；用户能审核并锁定 take |
| E 粗剪交付 | 系统只用 locked take 组装 RoughCut，生成 AudioSubtitlePlan、缺口列表和 EpisodeDeliveryPackage，不把 D 阶段 take 误当成成片，也不把 RoughCut 伪装成最终发布母版 |
| 治理底线 | 非 localhost 访问必须有访问控制；密钥不进入项目包；敏感操作写 AuditLog；批量生成前有成本估算和止损提示 |

**头号验收维度是"最终成片质量风险是否被显著降低"**——其它基线只是保证系统能跑通，但"能跑通"不等于"能看、能交付"。一切阶段性演示如果 RoughCut 质量指标不通过，不算产品成立。

**成片质量测量口径**：

- **评估对象**：以当前 Episode 的 locked take 组装出的 RoughCut 为准；剧本版本、Storyboard、TrackPlan、PromptPack、AssetVersion 和 ProductionFrame 是质量评估基准。
- **系统初筛**：QualityGate 先输出每个 Track / TrackSegment 的角色一致、构图、动作、嘴型状态、连续性和合规检查结果，并记录置信度、失败原因和建议处理方式。
- **人工确认**：角色一致性、动作可读和嘴型状态允许采用"AI 初筛 + 人工抽检"组合，但最终锁定 RoughCut 前必须有通过、驳回或人工豁免记录。
- **指标计算**：角色一致性按主要角色出现次数计算通过率；动作可读按需要动作表达的 TrackSegment 计算通过率；构图、嘴型状态和连续性按 Track 逐项检查。分母、样本范围和豁免项必须写入 QualityGate，不能只写总体分数。
- **豁免规则**：人工豁免必须记录 Operator、原因、影响指标、影响范围和时间。waived 只能表示"允许继续制作"，不能伪装成自动质量通过。

**质量评估协议（最小可执行版）**：

| 指标 | 评估单位 | 样本规则 | 通过口径 |
|------|----------|----------|----------|
| 角色一致性 | 主要角色在 locked take 中的出现次数 | 主要角色定义为当前 Episode 中出现 ≥2 个 Track、或被标记为 continuity_critical 的角色；每个出现 Track 至少取首/中/尾 3 帧，不足 3 帧则全取 | 主要角色脸、服装、发型与引用 AssetVersion 不冲突；通过次数 / 有效出现次数 ≥ 95% |
| 构图正确 | Track | 每个 locked take 必查；以 Storyboard / TrackPlan 的景别、朝向、构图目标为基准 | 景别或朝向偏差不超过 1 档；关键主体未被错误裁切或遮挡 |
| 动作可读 | 带明确动作目标的 TrackSegment | 只统计 TrackSegment 中写有动作目标的段落；无动作目标的段落不进入分母 | AI 初筛 + 人工确认均认为动作意图可被观众理解；通过段落 / 有动作目标段落 ≥ 90% |
| 嘴型状态 | 带台词、说话、闭嘴旁白或内心独白标记的 TrackSegment | 无台词/无嘴型意图段落不进入分母；有说话意图至少抽取对应时间段中点帧 | 开闭口状态与对白/旁白意图不冲突，无明显反向口型；当前阶段不要求精确音素级同步 |
| 跨镜连续性 | 相邻 Track 对 | 同一场景、连续动作或 `continue_from_previous` 策略的相邻 Track 必查；跨场景切换不进入分母 | 人物站位、光线方向、服装、道具和空间关系无明显跳跃 |
| 合规通过 | locked take / RoughCut | 所有 locked take 和 RoughCut 导出前必查 | 不存在 compliance_failed；授权未知可提示但不等同合规失败 |

QualityGate 必须保存 `metric`、`scope`、`denominator`、`passedCount`、`failedItems`、`confidence`、`samplingRule`、`reviewSource`、`operatorDecision` 和 `waiverReason`。P0 实现允许采用"AI 初筛 + 人工确认"组合，不要求一次性实现全自动视觉判定；但不能没有结构化记录。

Agent 记忆、章节事件图谱、可编程供应商、无限画布、完整 TTS/混音/字幕精修属于增强能力；它们不能阻塞上述核心闭环成立。

**核心对象模型**：

下表的"UI 暴露"列说明每个对象在用户界面里的可见性——✅ 直接暴露（用户语言）/ ⚠️ 半暴露（仅元信息或翻译后名称）/ ❌ 不暴露（仅后端使用）。**详细的 UI 用户语言映射见 [UI 布局] "后端对象 → 用户可见概念映射" 章节**。

| 对象 | UI 暴露 | 说明 |
|------|---------|------|
| Workspace | ❌ | 后端工作空间，保存数据根目录、数据库、访问配置、模型配置和任务队列 |
| Project | ✅ | 项目空间，保存项目配置、画风、比例、章节、剧本、资产和制作结果 |
| SourceDocument | ✅ | 导入的小说原文、剧本原稿或外部文本来源 |
| Chapter | ✅ | 从 SourceDocument 解析出的章节或文本分段，是改编和召回的基础单位 |
| StoryOutline | ✅ | 全局故事骨架和分集故事骨架 |
| AdaptationPlan | ✅ | 全局改编策略、分集改编策略和短剧化规则 |
| Episode | ✅ | 单集制作单位，承载剧本、分镜、Track、视频 take 和粗剪结果 |
| ScriptVersion | ✅ | 分集剧本的具体版本，记录生成、人工编辑、审核、锁定和回滚 |
| Asset | ✅ | 可复用视觉资产，包括角色、场景、道具、服装/妆造；跨 Track 或跨集复用 |
| AssetVersion | ✅ | 资产的具体提示词、参考图和版本记录，用于区分候选图、主参考图、弃用图和归档图 |
| DirectorAnalysis | ✅ | A 阶段导演分析，连接剧本、视觉资产需求和制作风险 |
| DirectorPlan | ✅ | C1 阶段导演规划，描述单集节奏、镜头策略、资产使用和连续性要求 |
| Storyboard | ✅ | C2 阶段分镜表，描述镜头、画面、角色、台词、时长和参考资产 |
| PromptPack | ✅ | C3 阶段分镜提示词与视频提示词包，可读、可版本化、可追溯 |
| TrackPlan | ✅ | D 阶段 Track 计划，承接 Storyboard/PromptPack 并拆出 Track 与 TrackSegment |
| Track | ✅ | 视频生成单位，通常 4-15 秒，最终对应一个锁定短视频片段 |
| TrackSegment | ✅ | Track 内控制单位，描述 0-2s、2-6s 等时间段的动作、镜头、台词和情绪 |
| ProductionFrame | ⚠️ | Track 生产图，包括视觉锚帧、尾帧、中间关键帧和承接帧；不进入可复用资产库 |
| Take | ✅ | 单次视频生成结果，记录输入、模型、参数、成本、问题和重试血缘 |
| RoughCut | ✅ | 单集粗剪结果，按锁定 Track 组装并关联音频/字幕计划 |
| AudioSubtitlePlan | ✅ | E 阶段音频与字幕计划，记录旁白、台词、嘴型、音效、字幕时间段和后续 TTS/字幕导出入口 |
| ExportPackage | ✅ | 导出包基类，分为 `EpisodeDeliveryPackage`（单集粗剪交付包）和 `ProjectMigrationPackage`（项目迁移/备份包）；两者共享 manifest、Artifact 清单、hash、缺失检查和敏感配置排除规则 |
| UsageRecord | ⚠️ | 模型调用、媒体处理、重试、导出等成本和用量记录 |
| QualityGate | ✅ | 锁定剧本、资产、关键帧、take 或粗剪前的质量检查结果和人工豁免记录 |
| SkillVersion | ❌ | ScriptAgent、ProductionAgent 等使用的提示词/工具说明版本 |
| AgentRun | ❌ | 单次 Agent 或 AI 生成运行记录，关联输入对象、SkillVersion、ModelConfig、Task、UsageRecord 和输出对象 |
| ModelConfig | ⚠️ | 语言、图像、视频模型配置和能力声明 |
| ProviderConfig | ⚠️ | 模型供应商或私有网关配置，引用密钥但不直接暴露密钥值 |
| Task | ⚠️ | 图片、视频、审核、导出等异步任务记录 |
| Artifact | ❌ | 文本、图片、视频、音频、导出包等大文件产物 |
| LegacyImport | ❌ | 从旧 `novels/` 目录导入或映射的数据来源记录 |
| AccessCredential | ❌ | 本机、局域网或私有服务器访问凭据，不等同于云端多用户账号（仅私有服务器模式暴露） |
| SecretRef | ⚠️ | 模型密钥、访问令牌、供应商凭据等敏感配置引用，只保存引用和元数据，不进入项目包 |
| Operator | ❌ | 操作人身份（仅私有服务器模式暴露）。localhost 可使用默认本机身份；私有服务器可绑定轻量账号或访问令牌 |
| AuditLog | ❌ | 关键操作审计记录（本地模式简化为操作记录）。包括删除、解锁、豁免、导出、导入和模型密钥变更 |
| RuntimeCheck | ❌ | 服务启动与生成前的环境检查结果（设置页"环境检查"汇总展示） |
| SchemaMigration | ❌ | 后端数据库和数据产物结构版本记录 |

**后端化数据结论**：

- `novels/` 不应继续作为新产品的业务边界。它是旧项目数据目录和迁移来源，不是重做版的产品概念。
- 重做版需要一个明确的 Toonflow Backend。前端只通过后端 API 读写文本、资产、制作管线、Track、take、任务、审计和 Artifact 对象，不能直接读写文件夹。
- 后端数据采用“结构化数据库 + 文件产物仓库”的混合模式：Project、Episode、ScriptVersion、AssetVersion、DirectorAnalysis、DirectorPlan、Storyboard、PromptPack、TrackPlan、Track、Take、Task、模型配置、状态机和血缘记录进入 SQLite 或等价本地数据库；章节原文、Markdown、图片、视频、粗剪、导出包等大文件作为 Artifact 保存在可配置数据根目录。
- 数据根目录应配置为 `dataRoot`，而不是硬编码成 `novels/`。为了迁移旧数据，`dataRoot` 可以临时指向现有 `novels/`，但新建项目应进入新的后端工作空间结构。
- 旧 `novels/{projectName}` 项目应通过导入/迁移流程进入新后端：扫描、预览、映射对象、生成 projectId、导入文件产物、建立索引。默认不得原地改写旧目录。
- 后端化不是云 SaaS 化。第一性目标仍是本地优先和私有化部署，只是把文件夹工具升级为有 API、有状态机、有任务队列、有数据契约的完整项目。

**缺陷审视与修正**：

当前自审发现的主要风险不是功能不够，而是约束不够。如果开发时不补，做出来会"能跑但很快脏掉"。"修正优先级"列指明该缺陷影响成片质量或生产稳定性的严重程度——P0 直接影响成片成立 / P1 影响生产稳定性 / P2 工程完整度补强。

| 缺陷 | 风险 | 修正 | 修正优先级 |
|------|------|------|-----------|
| 数据版本缺失 | 数据库、Artifact 路径和旧导入映射一变，旧项目可能打不开 | 引入 schemaVersion、artifactManifest 和 SchemaMigration | P0 |
| 下游失效规则缺失 | 剧本或资产修改后，旧 Track/take 还显示可用，成片内容前后不一致 | 建立依赖图；上游变更后标记下游 stale/needs_regeneration | P0 |
| 任务队列过于笼统 | 长视频、图片批量、抽帧、导出任务无法暂停、取消、恢复或重试 | Task 必须支持 queued/running/paused/cancelled/retrying/succeeded/failed 与依赖 DAG | P1 |
| 并发编辑缺失 | HTTP 访问、多标签页或局域网访问时，两个操作互相覆盖 | 对 Project/Episode/Track 使用 revision、乐观锁和编辑冲突提示 | P1 |
| 访问安全过粗 | 局域网/私有服务器访问会暴露小说、模型密钥和生成素材 | 本机可免登录；非 localhost 必须启用访问凭据；模型密钥不得进入 ProjectMigrationPackage | P1（本地 P2 / 私有服务器 P0） |
| Artifact 生命周期缺失 | 图片、视频 take 和导出包越积越多，删记录不删文件，或删文件导致数据库悬空 | Artifact 需要 hash、size、mime、owner、引用计数、缺失检测、清理和归档 | P1 |
| 锁定产物规则缺失 | 已锁定 take 或粗剪被上游重生成覆盖 | locked 对象只读；如需修改必须复制为新版本或显式解锁 | P0 |
| 导出/备份边界不清 | 项目迁移到另一台机器时素材丢失，或把密钥一起导出 | ProjectMigrationPackage 必须包含 manifest、数据库切片、Artifact 清单和缺失检查，默认排除密钥 | P2 |
| 资产版本缺失 | 角色/场景/道具/服装多次生成后，Track 如果引用"最新图"会导致旧镜头复现不了 | 引入 AssetVersion、主参考图 canonicalVersion 和精确 assetVersionId 引用 | P0 |
| 任务幂等与远端恢复缺失 | 页面刷新、重复点击或 Worker 重启可能重复提交视频任务，造成重复扣费或结果丢失 | Task 保存 idempotencyKey、providerJobId、attemptNo 和远端任务恢复/对账状态 | P1 |
| 软删除缺失 | 用户误删项目、资产、take 或粗剪后难以恢复，也可能留下大文件垃圾 | 关键对象先进入回收站；硬删除需要二次确认并走 Artifact 清理 | P2 |
| 素材来源缺失 | 上传图、抽帧图、生成图和外部素材来源不明，后续商用、迁移或审计有风险 | Artifact/AssetVersion 记录 source、license、model、prompt、importPath、createdBy | P1 |
| 导出一致性缺失 | 导出项目时数据库和文件产物可能处于不同时间点，恢复后出现悬空引用 | 导出使用一致性快照，manifest 记录 DB revision、Artifact hash 和运行中任务处理方式 | P2 |
| 质量门禁缺失 | 视频能生成但不一定能进入成片，锁定动作太随意会把问题带到 E 阶段 | 引入 QualityGate，锁定关键产物前必须有检查结果、问题清单或人工豁免 | P0 |
| 文本阶段对象缺失 | 小说、章节、骨架、策略和剧本版本如果只落成文件，后续依赖分析会断 | 引入 SourceDocument、Chapter、StoryOutline、AdaptationPlan、ScriptVersion | P0 |
| 审计主体缺失 | 软删除、解锁、豁免、导出敏感配置时不知道是谁操作，局域网访问尤其危险 | 引入 Operator 和 AuditLog；localhost 可用默认本机身份 | P2（本地）/ P1（私有服务器） |
| HTTP 文件安全缺失 | 浏览器访问素材时可能出现路径穿越、越权下载、超大上传或恶意文件预览 | 文件访问必须走 signed URL/受控 API、MIME 校验、大小限制和路径白名单 | P1（本地 P2 / 非本地 P0） |
| 运行环境检查缺失 | ffmpeg、磁盘空间、数据目录权限或供应商连接不正常时，用户到 D/E 阶段才失败 | 引入 RuntimeCheck 和启动/任务前预检 | P0 |
| 需求范围继续膨胀 | 记忆、事件图谱、供应商脚本、无限画布都写进去后，核心闭环可能被拖死 | 建立核心闭环门槛：不影响 A-E 主链路的能力不得阻塞剧本到 Track 粗剪 | 元约束（不打优先级） |
| A/C 阶段对象缺失 | 导演分析、导演规划、分镜表和提示词包如果只是散文件，D 阶段无法稳定追溯 Track 来源 | 引入 DirectorAnalysis、DirectorPlan、Storyboard、PromptPack、TrackPlan | P0 |
| Task 状态枚举缺失 | 技术说明要求暂停、取消、恢复、重试，但状态表没有对应枚举，后续实现会各写各的 | 新增 Task 状态和任务恢复状态，并把 Task 与 Track/Take 状态分开 | P1 |
| 生成状态和依赖有效性混用 | take 已生成但上游剧本或资产已变更时，不能简单把 take 改成 failed 或 generated | 新增依赖有效性状态：current/stale/needs_regeneration/blocked_by_missing_input | P1 |
| 私有服务器传输安全缺失 | 只写访问凭据不够，公网或反代下如果没有 HTTPS，会泄露小说、密钥和素材 | private_server 或公网访问必须走 HTTPS；局域网访问默认不开放，开启时给出风险提示 | P0（私有服务器）/ N/A（本地） |
| E 阶段对象缺失 | 音频/字幕、粗剪导出、剪辑工程清单如果只是 JSON 文件，后续接 TTS、字幕和外部剪辑会重做 | 引入 AudioSubtitlePlan 和 ExportPackage | P0 |
| 成本记录缺失 | 只有生成前估算，没有生成后实际用量和失败成本，无法判断哪种策略烧钱 | 引入 UsageRecord，记录估算、实际、失败、重试和供应商账单映射 | P1 |
| 供应商配置边界不清 | ModelConfig 同时承担模型能力、供应商网关和密钥，容易把敏感信息带进 ProjectMigrationPackage | 引入 ProviderConfig；ModelConfig 只引用供应商和能力，密钥以 SecretRef 管理 | P1 |
| 密钥引用对象缺失 | ProviderConfig 写了 SecretRef，但对象模型没有定义，开发时可能又把密钥塞回数据库或导出包 | 引入 SecretRef，并规定只保存引用、作用域、过期时间和审计信息 | P1（本地 P1 / 非本地 P0） |
| 资产生成仍有"最新图"口吻 | 批量生成资产图片如果只保存路径和展示最新结果，会绕开 AssetVersion | 资产图片生成必须创建 AssetVersion，用户再选择 canonicalVersion | P0 |
| 辅助导出绕过 ExportPackage | 剧本导出如果直接写本地文本文件，会绕开 manifest、Artifact 和审计 | 所有面向用户的导出都通过 ExportPackage 或 Artifact 导出记录 | P2 |
| AI 生成血缘缺 Agent 层 | 只记录模型和 Task，不记录 Skill/Agent 输入输出，后续无法复现一次剧本、分镜或提示词生成 | 引入 SkillVersion 和 AgentRun，所有 AI 产物都要关联生成运行 | P0 |
| 用户文本提示注入风险 | 小说原文、剧本和用户对话可能诱导 Agent 修改设置、泄露密钥或越权读文件 | AgentRun 必须有工具权限边界，导入文本只能作为内容上下文，不得成为系统指令 | P0 |

**重设计原则**：

- 先定对象模型和数据契约，再定页面。页面可以重画，数据对象不能反复推翻。
- 每个生成结果都必须可追溯、可重试、可替换、可锁定。黑盒覆盖式生成不适合这个产品。
- A-E 管线是底层状态机，UI 可以是节点图、列表或将来的画布，但不能让 UI 形态替代流程状态。
- HTTP Web 是主入口；Electron 只能是壳，不得形成另一套业务逻辑。
- Backend 是产品的一部分，不是实现细节。没有后端数据契约，前端重做只会继续变成页面拼装。
- 数据一致性优先于生成便利。任何会改动上游对象的操作，都必须明确影响哪些下游 Track、take、粗剪和导出结果。
- 稳定引用优先于“最新结果”。Track、take、粗剪和导出必须引用确定版本的资产、关键帧和提示词，不能依赖会漂移的当前最新状态。
- 核心闭环优先于能力堆叠。Agent 记忆、事件图谱、可编程供应商和无限画布不能阻塞“小说/剧本 → 资产 → Track → take → 粗剪”的主链路。
- 成本可追溯：任何调用模型或触发媒体处理的动作，必须能在任务前估算、任务后记录实际用量。**这是生产稳定性支撑，不作为产品对外讲故事的差异化卖点。**
- Toonflow-app 的登录、Socket.IO、记忆、事件图谱、无限画布、供应商系统是参考能力，不是全部照搬清单。
- 完整非线编、云端多租户协作、公开素材市场、全自动发布、完整配音混音不是核心闭环的一部分。

## 应用场景

- **一键全自动短剧生产（核心默认场景）**：创作者选定某集后点击"一键自动制作"或输入 `~sd auto [集号]`，系统按 A→C3 顺序自动跑导演分析、资产塑造、导演规划、分镜表和提示词包，每阶段产物经 director / compliance / art-direction / prompt-review Agent 自审；创作者在自动管线运行视图实时查看进度并按需暂停/介入，只在 D 阶段视频提交前介入一次。这是产品默认推荐的使用方式，体现"用户当导演不当操作员"。
- **网文改编试制**：独立创作者导入一批小说章节，通过剧本 Agent 生成故事骨架、改编策略和分集剧本，用于验证这个 IP 是否适合短剧化。
- **文本版本管理**：创作者多次生成或手动修改骨架、改编策略和分集剧本后，需要保留版本、对比差异、回滚和锁定，避免后续制作引用一个会被覆盖的剧本文本。
- **短剧团队批量生产**：内容团队为多部小说建立项目，按集管理剧本、资产和 Seedance 制作管线，减少制作人员在文件夹和 AI 平台之间来回切换。
- **视觉资产统一塑造**：美术或制片负责人从大纲和导演分析中提取角色、场景、道具、服装/妆造，批量润色提示词并生成参考图，保证同一项目内资产命名和视觉锚点一致。
- **资产版本与主参考管理**：美术或制片负责人为同一个角色、场景、道具、服装/妆造保留多张候选图，选择一张或多张主参考图，并让后续 Track 精确引用这些版本，而不是引用会变化的“最新图”。
- **分镜到视频任务准备**：导演或分镜师基于已完成剧本生成导演分析、导演规划、分镜表和分镜提示词，再进入视频任务阶段。
- **Track 级视频片段生成**：系统按 C2 分镜表把单集拆成多个 4-15 秒的 Track，创作者可在制作工作台展开 Track 调整时间段或拆分；每个 Track 使用场景、人物、道具参考图和叙事提示词生成短视频片段，而不是先制作首尾帧。
- **Track 内镜头段控制**：创作者不直接把 15 秒全部交给一段长 prompt，而是在每个 Track 内检查 0-2s、2-6s、6-10s 等时间段的画面目标、人物动作、镜头、台词和参考资产，再让系统编译成最终视频提示词。
- **Track 生成策略决策**：每个 Track 在生成前必须先确定生成策略，包括多参考直出、仅首帧、首尾帧、多关键帧、承接上一段尾帧等。首次生成时由系统按 Track 内容、制作目标和模型能力矩阵自动决策；用户只在 Retake 高级区覆盖。
- **策略驱动的视频制作**：系统决策出的 Track 生成策略会改变后续制作步骤。`start_frame` 需要先生成或上传首帧，`first_last_frame` 需要准备首帧和尾帧，`multi_keyframe` 需要准备多个视觉状态帧，`continue_from_previous` 需要从上一段视频抽取尾帧作为下一段首帧。
- **视觉锚帧制作**：系统按 Track 起点视觉状态、场景图、人物图、道具图和画风约束生成视觉锚帧；创作者审核、重生成、上传替换或锁定锚帧。锚帧不是角色资产图，也不是场景资产图，而是"这一段视频真正从哪里开始"的画面控制图。
- **Take 审核与单集粗剪交付**：创作者不是只生成一堆 T01/T02/T03 文件，还需要审核每个 Track、选择最佳 take、锁定结果、按顺序组装单集粗剪，并导出给后续剪辑、配音或发布流程。
- **任务中断恢复**：创作者在视频生成期间刷新页面、关闭浏览器或重启后端后，系统需要恢复本地任务和远端供应商任务的对应关系，避免重复提交、重复扣费或丢失已完成结果。
- **音频与字幕准备**：创作者需要知道每个 Track 的旁白、台词、嘴型、音效和字幕归属。即使当前核心闭环不做完整配音，也必须把音频/字幕计划作为 Track 元数据保存，否则后面做 TTS、口型或字幕会重写一遍。
- **单集交付包导出**：创作者完成单集粗剪后，需要导出粗剪视频、锁定 take 列表、字幕/音频计划、素材清单和剪辑工程清单，形成 EpisodeDeliveryPackage 交给剪辑、配音或后期继续处理。整项目迁移另走 ProjectMigrationPackage。
- **成本复盘**：创作者可在任务中心查看 UsageRecord 汇总，判断哪些策略实际烧钱、哪些重试更高频，用于优化后续生产。**作为生产稳定性辅助，不是核心使用场景。**
- **HTTP Web 直接访问**：创作者或团队成员可以通过浏览器打开 `http://localhost:{port}`、局域网地址或私有服务器地址访问工作台，而不是只能通过桌面壳或命令行使用。
- **启动与生成前预检**：创作者打开工作台或准备批量生成前，系统检查数据目录、数据库、磁盘空间、ffmpeg、模型连接、视频预览和访问地址是否可用，把环境问题提前暴露。
- **后端项目数据仓库**：创作者的数据不再散落在一个被前端直接读写的 `novels/` 文件夹里，而是由后端管理项目、资产、Track、任务、生成血缘和文件产物。旧 `novels/` 可被导入，但不应继续定义新产品结构。
- **变更影响检查**：创作者修改剧本、资产、Track 计划或模型配置后，系统需要提示哪些下游产物会过期，包括锚帧、关键帧、take、粗剪和导出结果，避免拿旧视频当新结果使用。
- **项目迁移与恢复**：创作者可以导出一个 ProjectMigrationPackage，包含数据库切片、Artifact 清单、可选素材文件和 manifest；导入时系统检查缺失文件、版本兼容和模型密钥缺口。
- **误删恢复与清理**：创作者删除项目、资产、Track、take 或导出包时，系统先进入回收站和缺失检查流程，确认无引用后再清理大文件产物。
- **敏感操作审计**：创作者或团队成员进行删除、解锁、人工豁免、导出 ProjectMigrationPackage、导出敏感配置或修改模型密钥时，系统记录操作人、时间、对象和原因。
- **私有模型接入与调试**：技术型团队在设置中心配置语言、图像、视频模型，必要时对齐 Toonflow-app 的可编程供应商能力，让团队可以接入自有模型网关。

## 功能需求

**优先级分级标准**：所有功能按"是否直接影响成片质量"分级，不按"是否新颖"或"是否被竞品占了"分级。

- **[P0]**：直接决定能否产出高质量可交付粗剪，并显著降低最终成片质量风险。砍掉则产品不成立。
- **[P1]**：增强成片质量或保证生产稳定性。砍掉粗剪可做但质量不稳。
- **[P2]**：次要工程能力，与成片质量无直接关系。可后置实现。

**核心功能**（去掉就不成立的功能）

- **管理项目** [P0]：用户点击"新建项目"并填写名称、小说类型、影片画风、画面比例和简介 → 系统在本地项目数据目录创建项目结构 → 用户获得一个可持续写入章节、剧本、资产和制作结果的项目空间。
- **通过 HTTP 访问工作台** [P0]：用户启动 Toonflow Web 服务 → 系统监听配置的 host/port 并提供 Web UI、API Routes、素材预览和生成任务接口 → 用户通过浏览器地址直接访问项目工作台。支持本机 `localhost`、局域网 IP 和私有服务器域名访问；非本机访问必须启用访问控制。
- **执行运行环境预检** [P0]：用户首次启动、切换数据目录或准备批量生成 → 系统检查数据目录读写、数据库迁移、磁盘空间、ffmpeg/媒体处理能力、模型连接、base URL、视频范围请求和任务队列 → 不通过时给出可操作修复提示。核心预检是 A-E 闭环前置条件；高级诊断和历史记录可后置。
- **查看数据目录与存储状态** [P0]：用户首次启动服务或进入设置 → 系统检查后端数据库、数据根目录、素材仓库、任务队列和访问配置 → 用户可以查看当前 `dataRoot`、数据库状态、Artifact 仓库、存储占用和最近一次 RuntimeCheck。备份/导入入口可后置，但当前数据目录和存储健康必须可见。
- **导入旧 novels 项目** [P2]：用户选择现有 `novels/` 目录或其中某个旧项目 → 系统扫描章节、配置、脚本、资产、review、seedance 产物和任务记录 → 用户确认映射关系后导入为新的 Project/Episode/Asset/Track/Artifact 记录。默认只读扫描，不直接覆盖旧目录。**不作为顶级页面，挂在设置→数据→导入子菜单。**
- **检查上游变更影响** [P1]：用户修改章节、剧本、资产、TrackSegment、生成策略或模型配置 → 系统计算依赖影响并把相关锚帧、关键帧、take、粗剪、导出结果标记为 stale 或 needs_regeneration → 用户确认后再继续生成或解锁重做。
- **导入和管理小说原文** [P0]：用户粘贴或上传 `.txt` 原文 → 系统保存 SourceDocument，按"第 X 章"自动解析 Chapter 并允许确认 → 章节进入项目并可继续编辑、删除、查看摘要。
- **生成故事骨架** [P0]：用户在剧本 Agent 中点击生成或输入意图 → 系统读取项目描述和章节上下文，调用语言模型生成全局故事骨架与分集骨架 → 结果保存为 StoryOutline 并展示在工作台右侧。 [⚡ AI 增强]
- **生成改编策略** [P0]：用户在故事骨架后继续执行 → 系统调用语言模型生成全局改编策略与分集改编策略 → 结果保存为 AdaptationPlan 并可编辑。 [⚡ AI 增强]
- **生成分集剧本** [P0]：用户选择生成剧本或继续下一步 → 系统基于章节、骨架、改编策略和项目配置生成分集剧本 → 生成新的 ScriptVersion，进入卡片列表并可查看、编辑、导出和审核。 [⚡ AI 增强]
- **管理剧本版本与详情** [P0]：用户进入"剧本管理" → 系统以卡片展示集数、标题、字数、场景数、当前版本和内容预览 → 用户可搜索、批量选择、导出、删除、对比版本、回滚，并在弹窗中编辑单集剧本。
- **关联剧本资产** [P0]：用户打开剧本详情 → 系统自动解析角色、场景、道具、服装/妆造并结合大纲补全关联资产 → 用户可手动增删资产标签并保存。 [⚡ AI 增强]
- **管理视觉资产** [P0]：用户进入"塑造" → 系统展示角色、场景、道具、服装/妆造资产卡片，支持搜索、类型筛选、批量选择、手动新增、从大纲提取、详情编辑和删除。
- **管理资产版本与主参考图** [P0]：用户打开资产详情 → 系统展示该资产的提示词版本、上传图、生成图、弃用图和主参考图 → 用户选择 canonicalVersion、锁定满意版本或归档失败版本。后续 Track 必须引用具体 AssetVersion。**这是跨集 IP 一致性的核心机制。**
- **跨集 IP 资产时间线** [P0] 🆕：用户在项目级查看每个角色/场景/道具的跨集出现情况——某个角色在 EP01/03/05 出现，使用了 av-01 / av-04 两个 AssetVersion，其中 av-04 是 canonical；某个场景在 EP02/04 出现，跨集复用率 100% → 用户能一眼看出 IP 是否在跨集间一致、哪里需要补变体、哪里有断层。**这是行业唯一的功能，竞品都只做单集生成。**
- **上传和导入参考素材** [P1]：用户为角色、场景、道具、服装/妆造或 Track 生产图上传本地图片/视频帧 → 系统保存 Artifact、来源信息、hash 和授权备注 → 用户可把素材设为资产版本、视觉锚帧或关键帧候选。
- **批量润色资产提示词** [P0]：用户选中资产并选择语言模型 → 系统基于资产描述、美术风格 Skill 和视觉锚点生成图像提示词 → 生成新的 AssetVersion 候选提示词并展示进度。 [⚡ AI 增强]
- **批量生成资产图片** [P0]：用户选中已具备提示词的 AssetVersion 并选择图像模型/分辨率 → 系统提交图片生成任务、轮询状态、保存 Artifact、错误信息和 UsageRecord → 生成新的 AssetVersion 图片候选；用户再选择 canonicalVersion。 [⚡ AI 增强]
- **执行制作管线** [P0]：用户进入"制作工作台"选择集数 → 系统展示 A 导演分析、B 资产管理、C1 导演规划、C2 分镜表、C3 分镜提示词、D Track 视频生成、E 粗剪交付七个节点 → 用户可通过右侧 Production Agent 对话或快捷指令推进管线。 [⚡ AI 增强]
- **执行一键全自动生产 `~sd auto`** [P0] 🆕：用户在制作工作台或剧本管理点击"一键全自动制作"或输入指令 `~sd auto [集号]` → 系统按 A→B→C1→C2→C3 顺序自动跑完前 5 阶段，每阶段产物通过后端写入 Artifact 和数据库记录、自动审核（director/compliance/art-direction/prompt-review Agent）；遇到 stage1_blocked / stage2_blocked / skill_failed 自动暂停并提示用户介入 → C3 完成后生成 D 阶段提交前确认页。D 阶段视频生成必须由用户确认后进入路径 5，不属于 `~sd auto` 自动范围。 [⚡ AI 增强]
- **自动管线运行视图** [P0] 🆕：用户启动 `~sd auto` 后进入此视图 → 系统大屏展示 A→C3 各阶段实时进度、当前在跑哪一步、各阶段产物缩略图、预计完成时间、累计耗时、Agent 调用次数和 Token 用量 → 用户可暂停、恢复、跳过某阶段、查看产物、人工介入、终止任务。**这是产品差异化的核心 UI**，不是某个 Tab 内的子视图，是项目级独立大屏。
- **生成 Track 级视频片段** [P0]：用户在 D 节点选择视频模型并点击生成 → 系统读取 TrackPlan 对象和对应 Artifact 中的 T01/T02/T03 等 Track，收集每个 Track 的参考资产版本、生成策略、关键帧/承接帧和最终提示词 → 系统按策略提交视频任务、轮询状态并保存每个 Track 的短视频 take。 [⚡ AI 增强]
- **控制 Track 内镜头段** [P0]：用户展开 T01/T02 等 Track → 系统展示该 Track 内的秒级时间段、画面目标、角色动作、镜头/景别、台词/嘴型、情绪、参考资产和风险提示 → 用户可编辑时间段、让 AI 优化描述、拆分 Track 或锁定已满意段落 → 系统把结构化时间段编译为最终 Track 提示词。 [⚡ AI 增强]
- **Track 生成策略（系统决策为主）** [P0]：**用户首次生成时不选策略——系统根据 Track 内容（时长、动作复杂度、镜头变化、角色数量、制作目标、连续性要求）自动决策最适合的策略**（multi_reference_text / start_frame / first_last_frame / multi_keyframe / continue_from_previous）。系统在 Track 旁显示"使用策略 X，理由 Y"。**只有当用户对 take 不满意发起 Retake 时**，UI 才暴露"换策略"选项让用户覆盖。系统不允许静默降级到不兼容的策略。 [⚡ AI 增强]
- **准备策略所需视觉输入** [P0]：系统决策策略后 → 自动生成策略依赖清单，包括所需参考资产、首帧、尾帧、中间关键帧、上一段尾帧和模型能力 → 系统自动生成、抽取或提示用户补齐缺失素材 → 所有依赖 ready 后 Track 才能进入视频生成。 [⚡ AI 增强]
- **生成 Track 视觉锚帧** [P0]：系统选择 `start_frame` / `first_last_frame` / `multi_keyframe` 策略后 → 系统基于 Track 起点视觉状态、场景图、人物图、道具图和画风约束生成首帧候选图 → 用户选择、重生成、上传替换或锁定锚帧 → 锚帧作为视频生成的起始画面输入。 [⚡ AI 增强]
- **审核和锁定 Track take** [P0]：系统为每个 Track 保存多次生成结果 → 用户预览后选择接受、重试（可按问题类型重试：动作不对/角色漂移/构图不对/嘴型不对/时长不对/道具缺失/合规失败）、替换、锁定或标记问题 → 系统记录选中 take、失败原因、重试提示词和模型参数。 [⚡ AI 增强]
- **执行质量门禁** [P0]：用户准备锁定资产主参考图、关键帧、Track take 或粗剪 → 系统展示角色一致、构图、动作、道具、时长、嘴型状态、连续性和合规检查结果 → 用户通过、驳回或人工豁免后才能进入下一阶段。**这是"高质量可交付粗剪"的硬门禁——任一项失败且未豁免，不允许进入下一阶段。** [⚡ AI 增强]
- **组装单集粗剪** [P0]：用户进入 E 粗剪交付 → 系统按 Track 顺序读取已锁定视频，生成 RoughCut、单集预览时间线、缺口列表、EpisodeDeliveryPackage 和导出清单 → 用户可导出粗剪视频、剪辑工程清单或素材包。核心需求不做完整非线编软件。
- **生成音频与字幕计划** [P0]：系统从 Track 时间段、台词、旁白和嘴型规范生成 AudioSubtitlePlan → 用户可查看每段是否需要闭嘴旁白、张嘴对话、内心独白、音效或背景音乐 → 后续可接 TTS、配音、口型或字幕导出。 [⚡ AI 增强]
- **估算并记录生成成本** [P1]：用户批量生成 Track、锚帧或关键帧前 → 系统根据模型、时长、策略、参考素材和重试次数估算耗时/额度/失败风险 → 任务结束后写入 UsageRecord，记录实际用量、失败成本、重试次数和估算偏差。**功能保留作为生产稳定性支撑，但不作为产品对外讲故事的差异化卖点。**
- **导出和恢复项目迁移包** [P2]：用户在项目或设置中选择迁移/备份导出 → 系统生成 ProjectMigrationPackage、manifest、数据库切片、Artifact 清单和可选素材包 → 导入时检查版本、缺失文件、hash、模型配置缺口和安全风险。它不同于 E 阶段 P0 的 EpisodeDeliveryPackage。
- **恢复和对账生成任务** [P1]：用户回到任务中心或制作工作台 → 系统根据 Task、providerJobId 和 Artifact 状态恢复远端任务结果，标记 orphaned、reconciled 或 failed，避免重复提交同一个视频任务。
- **使用回收站和安全删除** [P2]：用户删除关键对象 → 系统先软删除并展示引用影响 → 用户可恢复；硬删除前系统列出会清理的 Artifact、数据库记录和不可恢复影响。
- **查看审计日志** [P2]：用户进入设置或对象详情 → 系统展示关键对象的创建、修改、删除、解锁、豁免、导入、导出和敏感配置变更记录 → 用户可按对象、操作人、时间和操作类型筛选。**本地单机模式下简化为操作记录，不强制 Operator 身份。**
- **查看管线产出** [P0]：用户点击已完成或进行中的管线节点 → 系统展示对应对象和可读 Artifact，包括 DirectorAnalysis、DirectorPlan、Storyboard、PromptPack、TrackPlan、资产卡片、视频任务队列和 Track 视频结果 → 用户能定位当前制作进度和缺口。
- **配置 AI 模型和供应商** [P1]：用户进入设置页 → 系统先管理 ProviderConfig，再按语言、图像、视频三类管理 ModelConfig，支持新增、编辑、删除、启用/禁用、测试连接 → 后续 Agent 和生成任务可选择指定模型。**UI 上用"模型供应商 / 语言模型 / 图像模型 / 视频模型"等用户语言，不直接暴露 ProviderConfig / ModelConfig 对象名。**
- **选择生成模型** [P1]：用户在剧本 Agent 或制作工作台发送消息/执行任务前选择模型 → 系统把选中的语言、图像、视频模型 ID 传给后端 → 不同阶段可使用不同模型组合。
- **多视频供应商能力矩阵** [P1] 🆕：系统维护"策略 × 视频模型"能力矩阵（每个供应商对 multi_reference_text / start_frame / first_last_frame / multi_keyframe / continue_from_previous 的支持情况，以及最大参考图数、最大时长、比例、分辨率约束）→ 策略决策和任务提交必须读取这张矩阵；不兼容时不能静默降级，必须提示换模型、人工覆盖策略或拆分 Track。

**辅助功能**（有了更好但去掉不死的）

- **保存对话历史** [P2]：系统保存剧本 Agent 的会话历史，并在切换 Tab 或刷新后恢复近期上下文。
- **审核生成结果** [P1]：用户对故事骨架、改编策略或单集剧本发起审核 → 系统按维度给出评分、问题和修改建议 → 用户可根据审核结果执行修复。 [⚡ AI 增强]
- **修复生成结果** [P1]：用户在审核失败或不满意时点击修复 → 系统读取最近审核建议并调用对应 Agent 生成修订内容 → 覆盖或更新目标产物。 [⚡ AI 增强]
- **检测后台任务** [P2]：制作工作台轮询跨集运行状态 → 当其他集有任务运行时显示提示并允许切换查看。
- **读取和编辑 Skill** [P1]：系统应把 ScriptAgent、ProductionAgent、Director、StoryboardArtist 等 Agent 使用的核心 Skill 作为 SkillVersion 管理，允许在设置或管理页面查看、编辑、保存和回滚。**Skill 是产品的核心资产（10 方位朝向/台词时长公式/10 项连贯性校验等导演工艺），但运维入口不暴露给普通用户。**
- **网文专用 Skill 包** [P1] 🆕：在 Skill 系统内提供网文改编专用 Skill 模板——章节切分（按章/按情节单元）、爽点识别（强反差/打脸/逆袭）、付费点设计、女频/男频/古言/玄幻类型化模板。用户创建新项目时可选择类型，系统自动加载对应 Skill 包。 [⚡ AI 增强]
- **管理 Agent 记忆** [P2]：系统应提供 Agent 记忆查看、确认、清空能力，用于处理跨会话创作连续性。 [⚡ AI 增强]
- **配置可编程供应商** [P2]：系统应支持供应商代码/模型列表/输入参数管理，让私有网关或新模型无需改源码即可接入。**降级为 P2 是因为该能力被 LibTV 等竞品占了，不作为差异化主轴。**
- **管理章节事件图谱** [P2]：系统应从小说章节提取事件和角色信息，并在剧本改编时按事件图谱召回上下文。 [⚡ AI 增强]
- **导出内容** [P2]：用户导出选中剧本或全部剧本 → 系统生成 Artifact 导出记录，必要时归入 EpisodeDeliveryPackage，便于交付给外部制作环节。

### 核心功能验收标准

以下验收标准用于后续 `dev-planner`、`dev-builder` 和 `code-review` 判断是否完成。它们描述“做到什么算通过”，不规定开发顺序。

| 功能 | 验收标准 |
|------|----------|
| 管理项目 | 创建后生成 Project 记录和项目 Artifact 根索引；刷新后仍可见；重名、空名和非法字符有明确错误 |
| 通过 HTTP 访问工作台 | `localhost` 可访问 Web UI、API、素材预览和任务接口；非 localhost 必须先通过访问控制；base URL 改变后资源链接仍有效 |
| 执行运行环境预检 | RuntimeCheck 覆盖 dataRoot、数据库、磁盘、ffmpeg、模型连接、视频范围请求和任务队列；失败项给出可执行修复建议 |
| 查看数据目录与存储状态 | 设置页能查看 dataRoot、数据库位置、Artifact 仓库、存储占用和最近一次 RuntimeCheck；UI 使用"数据目录 / 存储状态 / 环境检查"等用户语言，不暴露 Workspace 对象名 |
| 导入旧 novels 项目 | 扫描只读；用户能预览映射；确认后创建新 Project/Episode/Asset/Track/Artifact；旧目录无覆盖、无删除 |
| 检查上游变更影响 | 保存上游对象前展示受影响下游清单；确认后把相关对象标记 stale 或 needs_regeneration；locked 对象不被覆盖 |
| 导入和管理小说原文 | 支持 `.txt` 和粘贴文本；章节解析结果可人工调整；解析失败时允许手动分段；章节编辑产生 revision |
| 生成故事骨架 | 输出 StoryOutline 版本，关联输入章节版本、ModelConfig、SkillVersion 和 AgentRun；结果可编辑、回滚、锁定 |
| 生成改编策略 | 输出 AdaptationPlan 版本，关联 StoryOutline 和章节版本；结果可编辑、回滚、锁定 |
| 生成分集剧本 | 为目标集创建 ScriptVersion；可查看、编辑、导出、审核、回滚；锁定前必须经过文本质量检查或人工豁免 |
| 管理剧本版本与详情 | 支持搜索、批量选择、版本对比、回滚、编辑、删除和导出；回滚会触发下游影响检查 |
| 关联剧本资产 | 系统给出资产候选和置信度；用户可增删改；保存后生成确定的 ScriptVersion 到 Asset 引用 |
| 管理视觉资产 | 资产支持新增、搜索、筛选、详情编辑、批量选择和软删除；同名同类资产需要去重提示 |
| 管理资产版本与主参考图 | 每次上传、生成、替换都形成 AssetVersion；canonicalVersion 必须由用户确认；Track 只能引用确定版本 |
| 跨集 IP 资产时间线 | 项目级时间线按集数横轴展示所有资产，纵轴按资产名称排列；自动检测同一资产 canonical 跨集不一致或视觉漂移并标红；点资产可跳转塑造详情、点节点可跳转对应集 D 节点；支持按类型/集数/一致性状态筛选；跨集复用次数、变体次数、新增次数清晰可查 |
| 执行一键全自动生产 `~sd auto` | A→C3 五阶段自动顺序执行，每阶段自动调对应 Skill/Agent，并把产物写入对应对象记录和 Artifact 仓库路径（资产进入 `dataRoot/projects/{projectId}/assets/...`，单集制作产物进入 `dataRoot/projects/{projectId}/episodes/{episodeId}/...`）；任一阶段 blocked/failed 自动暂停展示原因和回填入口；C3 完成后**必须**用户确认才提交 D 阶段（视频生成最烧钱）；启动前完成预算、Token 用量和图片任务成本估算，超阈值时阻塞二次确认 |
| 自动管线运行视图 | 项目级独立大屏；管线流水线大图覆盖 A→C3 全阶段状态、产物缩略图、Agent 调用次数、Token 用量；Pause/Resume/Stop/Skip 可用；右侧人工介入面板按 blocked 原因提示修复建议；底部 Agent 日志流式滚动；不混在 Tab 中 |
| 多视频供应商能力矩阵 | 系统维护"策略 × 视频模型"能力矩阵（覆盖 5 策略 + 最大参考图数 + 最大时长 + 比例 + 分辨率约束）；Track 策略决策必须读取矩阵；不兼容时不能静默降级，必须提示换模型、Retake 高级区覆盖策略或拆分 Track；矩阵可在设置页查看和更新 |
| 上传和导入参考素材 | Artifact 记录 hash、mime、size、source、license、createdBy 和敏感标记；超大小/MIME 不合法时拒绝 |
| 批量润色资产提示词 | 每个选中资产产生 AssetVersion 候选提示词；进度、失败原因、AgentRun 和 UsageRecord 可查 |
| 批量生成资产图片 | 每个任务产生 Task、Artifact 和 AssetVersion 候选图；失败不阻塞其他任务；并发、重试和成本记录可查 |
| 执行制作管线 | A/B/C1/C2/C3/D/E 节点状态与对象状态一致；Production Agent 能检查前置条件并写入 AgentRun |
| 生成 Track 级视频片段 | 仅在策略依赖 ready 后提交任务；Take 保存输入素材、策略、模型、参数、prompt、成本、providerJobId 和父 take |
| 控制 Track 内镜头段 | TrackSegment 可编辑、拆分、锁定；系统能把结构化时间段编译成最终 motion prompt 并保留原始结构 |
| Track 生成策略决策 | 系统给出决策理由和供应商兼容性；用户仅在 Retake 高级区覆盖策略，覆盖必须被记录；不兼容时不能静默降级 |
| 准备策略所需视觉输入 | 依赖清单展示缺失项；缺依赖时禁止提交视频；系统能生成、抽取、复用或提示用户上传所需素材 |
| 生成 Track 视觉锚帧 | 锚帧候选可预览、重生成、上传替换和锁定；锁定后作为 ProductionFrame 被 Track 精确引用 |
| 审核和锁定 Track take | Take 列表支持接受、重试、替换、标记问题和锁定；只有 locked take 能进入 RoughCut |
| 执行质量门禁 | 覆盖 StoryOutline、AdaptationPlan、ScriptVersion、AssetVersion、ProductionFrame、Take 和 RoughCut；**RoughCut 锁定前必须按 [重设计结论] 成片质量测量口径完成 6 项指标检查（角色一致性 ≥ 95%、构图正确、动作可读 ≥ 90%、嘴型状态、跨镜连续、合规通过），分项指标和分母必须写入 QualityGate**；失败不能静默进入下一阶段，豁免必须记录 Operator、原因和时间 |
| 组装单集粗剪 | RoughCut 只读取 locked take；未锁定 Track 显示缺口；导出前检查缺失 Artifact 和上游 stale 状态；可生成 EpisodeDeliveryPackage |
| 生成音频与字幕计划 | AudioSubtitlePlan 按 TrackSegment 生成时间段、旁白、对白、嘴型、音效和字幕字段；与 RoughCut 版本关联 |
| 估算并记录生成成本 | 批量提交前展示估算；超过预算或重试上限需二次确认；任务结束后写入实际 UsageRecord |
| 导出和恢复项目迁移包 | ProjectMigrationPackage 使用一致性快照；manifest 包含 schemaVersion、DB revision、Artifact hash、缺失项和敏感配置排除项；默认排除 SecretRef |
| 恢复和对账生成任务 | 重启或刷新后能用 providerJobId/requestHash 对账；重复提交等价任务时进入 duplicate_blocked 而不是再次扣费 |
| 使用回收站和安全删除 | 关键对象先软删除；可恢复；硬删除前展示引用影响、清理文件大小和不可恢复影响 |
| 查看操作记录 | 可按对象、Operator、时间和操作类型筛选；删除、解锁、豁免、导出、密钥和访问配置变更必须有 AuditLog 记录；本地模式 UI 简化为"操作记录"列表，私有服务器模式启用完整 Operator 审计 |
| 查看管线产出 | 每个节点能展示对应对象、Artifact、版本、状态、缺口和 stale 原因 |
| 配置 AI 模型和供应商 | ProviderConfig、SecretRef、ModelConfig 分层保存；连接测试和能力探测可见；密钥不出现在前端响应和项目包 |
| 选择生成模型 | 每次 AgentRun/Task 记录所选模型 ID；禁用模型不出现在选择器；不同阶段可选择不同模型组合 |

### 辅助功能验收标准

| 功能 | 验收标准 |
|------|----------|
| 保存对话历史 | 刷新、切 Tab 后能恢复最近上下文；历史关联 Project/Episode/AgentRun |
| 审核生成结果 | 输出评分、问题、建议和是否可锁定；审核结果写入 QualityGate 或 Review Artifact |
| 修复生成结果 | 修复基于最近审核建议创建新版本，不默认覆盖 locked 版本 |
| 检测后台任务 | 跨集任务状态可见；用户能跳转到对应任务或制作节点 |
| 读取和编辑 Skill | SkillVersion 可查看、编辑、保存、回滚；新生成内容记录使用的 SkillVersion |
| 网文专用 Skill 包 | 网文章节切分、爽点识别、付费点设计 Skill 可用；女频/男频/古言/玄幻类型模板支持在新建项目时选择；用户改类型时自动加载对应 Skill 包；Skill 包变更不污染已有项目，仅影响新建项目和显式启用的旧项目 |
| 管理 Agent 记忆 | 记忆条目需人工确认后才进入长期召回；可清空并写 AuditLog |
| 配置可编程供应商 | 私有网关配置可测试、禁用和回滚；代码/参数变更不泄露 SecretRef |
| 管理章节事件图谱 | 事件抽取结果可查看、修正、确认；剧本生成时能引用确认过的事件 |
| 导出内容 | 剧本导出走 Artifact 导出记录，必要时归入 EpisodeDeliveryPackage，不绕过 manifest 和审计 |

## UI 布局

### 整体布局

产品采用固定侧栏 + 项目内容区的 Web 工作台布局。全局左侧为窄侧栏导航，右侧为页面主体；项目详情页顶部显示返回入口、项目名称、当前管线阶段进度和 Tab 图标导航。

**项目内的页面分两类**：
- **生产主线（Tab 形式）**：小说原文 → 剧本 Agent → 剧本管理 → 塑造 → 制作工作台 → Take/粗剪交付（按 A→E 顺序排列，体现"流水线"感）。
- **项目级独立大屏**（不挂 Tab，从项目首页或快捷入口进入）：**自动管线运行视图**、**跨集 IP 资产时间线**。这两个是产品差异化的核心页面，不应混在 Tab 中。

设置页独立于项目，集中管理模型、供应商、Skill、数据目录等系统配置。

### 区域内容

- **全局侧栏**：项目列表入口、设置入口、任务中心入口；保持窄栏，避免抢占创作区空间。**侧栏图标必须配合文字 label（不用单字缩写），让首次进入用户能识别每个入口语义。**
- **项目列表页**：项目卡片网格、新建项目按钮、空状态、删除确认弹窗；卡片展示项目名称、类型、简介、**流水线进度条（显示当前在 A/B/C/D/E 哪个阶段）**、章节数、剧本数、Track 数和创建时间。流水线进度让用户在卡片层就看到生产状态，不用进项目才发现。
- **自动管线运行视图（新页，P0 核心差异化）🆕**：用户启动 `~sd auto` 后进入的项目级独立大屏。**这是路径 0 和路径 5 共享的视图，按阶段切换 5 种显示模式**：
  - **模式 1（A→C3 跑通模式）**：路径 0 启动后进入。顶部状态区显示当前任务名、累计耗时、预计剩余时间、当前阶段；中部管线流水线大图（A→B→C1→C2→C3 水平排列）；底部 Agent 日志流式滚动；右侧人工介入面板（blocked 时显示原因 + "回填后继续"按钮）。
  - **模式 2（C3 完成确认模式）**：A→C3 跑通后切换。展示各阶段产物缩略图和验收摘要，引导用户进入路径 5（D 提交确认页）。
  - **模式 3（D 阶段提交前确认模式）**：路径 5 入口页。展示所有 Track 的策略决策、依赖检查结果、预计 Token / 视频秒数 / 视频成本、可选模型、并发上限设置；用户确认后进入模式 4。
  - **模式 4（D 阶段任务模式）**：D 阶段视频生成执行中。顶部切换为 D 任务进度统计（已完成 / 运行中 / 失败 / 等待）；中部由管线流水线大图切换为 **Track 任务网格**（每 Track 卡片显示 providerJobId、状态、耗时、缩略图）；底部日志变为 Task 事件流（提交 / 重试 / 完成 / 失败）；右侧人工介入面板显示 blocked Track 的修复建议。
  - **模式 5（D 完成进入路径 4 引导模式）**：D 阶段全部完成后切换。展示成功/失败/blocked Track 统计 + 一键跳转 Take/粗剪交付 Tab 按钮，引导进入路径 4。
  - **共用控件**：所有模式下顶部都有 Pause/Resume/Stop/Skip 按钮（模式 4 下还支持单 Track 终止）；底部右下角显示项目级 RuntimeCheck 状态徽章。
- **跨集 IP 资产时间线（新页，P0 核心差异化）🆕**：项目级独立大屏，展示该项目所有资产的跨集复用关系。
  - 顶部筛选区：按资产类型（角色 / 场景 / 道具 / 服装）筛选、按集数范围筛选、按一致性状态筛选（一致 / 有变体 / 缺失）。
  - 主区时间线：横轴为集数（EP01→EPN），纵轴为资产名称。每个资产用一条横线表示，线上有节点标记该资产在哪些集出现、使用的 AssetVersion ID、是否为 canonical 版本。
  - 资产卡片悬停展示：缩略图、所有 AssetVersion 历史、当前 canonical、复用次数、变体次数、新增次数（来自 B 阶段标记）。
  - 不一致警报：系统自动检测"同一资产跨集 canonical 不一致"或"明显视觉漂移"，标红提示。
  - 用户操作：点资产可跳转塑造页详情；点节点可跳转对应集的 D 节点。
- **小说原文 Tab**：章节表格、上传/粘贴原文弹窗、章节解析确认、章节编辑弹窗、删除确认。
- **剧本 Agent Tab**：左侧聊天面板，右侧"当前产物"主区（故事骨架 / 改编策略 / 当前集剧本根据上下文动态切换，不并列三栏）+ 顶部小型"版本与质量门禁"摘要条。**避免把聊天、产物、质量门禁三栏并列堆在一屏。**
- **剧本管理 Tab**：顶部搜索和批量操作区，中部剧本卡片网格（**卡片上明确显示该集已进入 A/B/C/D/E 哪个阶段、是否已开始制作**），详情弹窗展示剧本标题、当前 ScriptVersion、版本历史、差异对比、全文、关联资产、保存/取消操作。
- **塑造 Tab**：左侧批量操作面板，右侧资产卡片网格；包含类型筛选、搜索、模型选择、提示词润色、图片生成、素材上传、提取资产、新增资产、详情抽屉。资产详情必须展示版本画廊、主参考图、来源信息、锁定/归档状态、被哪些 Track 引用、**以及跨集复用情况（"该资产在 EP01/03/05 复用"链接到跨集 IP 时间线）**。
- **制作工作台 Tab**：左侧约 60% 为集数选择器、管线节点图和节点详情展开区；右侧约 40% 为 Production Agent 对话面板和阶段模型选择器。
  - **首要入口是"一键全自动生产"按钮**（启动 `~sd auto`，跳转到自动管线运行视图）；手动逐阶段执行作为次要入口。
  - D 节点详情需要展示 Track 列表、Track 内时间段、参考资产版本、目标时长、**系统决策的生成策略（不让用户首次选）**、策略依赖清单、关键帧/承接帧、视频任务状态、失败原因、质量门禁和视频预览。
  - E 节点详情需要展示锁定 take、粗剪时间线、音频/字幕计划、缺口列表和导出入口。
  - Track 行必须可展开，不能只做成 `T01 + 参考图 + 生成按钮` 的黑盒列表。
- **Take/粗剪交付 Tab**：左侧深色 Take 审核面板（视频预览 + 多 take 候选 + 按问题类型重试按钮），右侧 E 粗剪交付（粗剪时间线 + AudioSubtitlePlan + 缺口列表 + 质量门禁 + 导出入口）。
- **设置页**：**用用户语言组织**——"模型供应商 / 语言模型 / 图像模型 / 视频模型 / 模型密钥 / 环境检查 / 操作记录"，而不是 ProviderConfig / SecretRef / RuntimeCheck / AuditLog 等工程对象名。后端对象与 UI 名称的映射见下文映射表。
- **任务中心**：本地 Task、远端 providerJobId、重试次数、恢复状态、按问题类型重试入口、duplicate_blocked 提醒。
- **访问入口**：服务启动后应展示当前可访问地址，包括本机地址、可选局域网地址和端口；设置页可展示当前服务地址、访问模式和安全提示。
- **降级页面（挂在设置子菜单内，不作顶级 Tab）**：旧 novels 导入向导、回收站、ProjectMigrationPackage 导出检查。这些是低频运维入口，不占主导航。

### 后端对象 → 用户可见概念映射

**目的**：Spec 列了 37 个核心对象（Workspace / Project / SourceDocument / ... / SchemaMigration），但 UI 不应该把所有对象名直接暴露给用户。**这张映射表告诉设计师哪些对象有对应的用户可见入口、哪些只是后端实现。**

| 后端对象 | UI 是否暴露 | UI 用户语言 / 入口 |
|---------|------------|------------------|
| Project / Episode | ✅ 暴露 | "项目 / 集" |
| SourceDocument / Chapter | ✅ 暴露 | "原文 / 章节" |
| StoryOutline / AdaptationPlan / ScriptVersion | ✅ 暴露 | "故事骨架 / 改编策略 / 剧本版本" |
| Asset / AssetVersion | ✅ 暴露 | "资产 / 版本"（主参考图 = canonical） |
| DirectorAnalysis / DirectorPlan / Storyboard / PromptPack / TrackPlan | ✅ 暴露 | "导演分析 / 导演规划 / 分镜表 / 提示词包 / Track 计划" |
| Track / TrackSegment / Take / RoughCut | ✅ 暴露 | "镜头 / 镜头段 / 视频版本 / 粗剪" |
| ProductionFrame | ⚠️ 半暴露 | "锚帧 / 关键帧"，但不强调对象名 |
| QualityGate | ✅ 暴露 | "质量检查" |
| AudioSubtitlePlan | ✅ 暴露 | "音频字幕计划" |
| ExportPackage | ✅ 暴露 | "单集交付包 / 项目迁移包" |
| ProviderConfig / ModelConfig | ⚠️ 半暴露 | "模型供应商 / 模型"，不显示对象名 |
| SecretRef | ⚠️ 半暴露 | "密钥"，只显示作用域、过期时间、最近使用，不显示明文 |
| AgentRun / SkillVersion | ❌ 不暴露 | 仅在调试视图、操作记录里出现，不进主流程 UI |
| Task | ⚠️ 半暴露 | "任务"（任务中心），状态用用户语言收敛 |
| Artifact | ❌ 不暴露 | 用户看到的是"图片/视频/文件"，不知道 Artifact 概念 |
| UsageRecord | ⚠️ 半暴露 | "成本记录"，仅在任务中心和导出包内出现 |
| Workspace | ❌ 不暴露 | 单工作空间时根本不需要这个概念 |
| LegacyImport | ❌ 不暴露 | 用户看到的是"导入旧项目"动作，不知道 LegacyImport 对象 |
| AccessCredential / Operator / AuditLog | ❌ 默认不暴露（本地模式） | 仅在私有服务器模式下作为"访问设置 / 操作日志"出现 |
| RuntimeCheck / SchemaMigration | ❌ 不暴露 | 仅在设置页"环境检查"汇总展示，不暴露对象 |

### 后端状态 → UI 用户语言映射

**目的**：Spec 定义了 38 套状态枚举，但 UI 应把主流程状态收敛到少量用户能理解的语言；设置、导出、安全和预算类状态可作为辅助标识展示。此表是实现映射要求，不是示例。

| UI 主状态（用户看到的） | 后端枚举（开发实现） | 含义 |
|------------------------|---------------------|------|
| **待规划** | draft / unchecked / pending / queued / unset / none / suggested / missing / scanned | 还没开始，需要用户或系统决策下一步 |
| **准备中** | preparing / preparing_inputs / generating / running / tool_waiting / reconnecting / checking / retrying / reconciling / waiting | 系统在准备素材、执行生成、检查或对账 |
| **可确认** | reviewed / passed / accepted / ready / success / succeeded / generated / ready_for_preview / preview_generated / current / within_budget / reconciled / mapped | 产物或检查结果可用，等待用户确认、锁定或进入下一步 |
| **已确认** | locked / canonical / completed / exported / imported / active / user_owned / licensed | 用户已确认、流程已完成或产物已可作为下游引用 |
| **失败/中止** | failed / blocked / orphaned / duplicate_blocked / blocked_by_missing_input / blocked_by_budget / connection_failed / timeout / cancelled / rejected / hard_deleted / compliance_failed / model_error | 出问题、被拦截、被取消或需要用户介入 |
| **过期/警告** | stale / needs_regeneration / expired / warning / capability_unknown / over_estimate / exceeded / disputed / unknown / restricted | 上游变更、风险、预算、授权或能力不确定，需要提示但不一定阻塞 |

| 辅助标识 | 后端枚举（开发实现） | 展示规则 |
|---------|---------------------|----------|
| **已暂停** | paused | 任务中心和 D 阶段批量任务中显示为可恢复状态，不归为失败 |
| **已停用** | disabled | 设置页模型/供应商状态，不进入主流程状态徽章 |
| **已启用** | enabled | 设置页模型/供应商状态，不进入主流程状态徽章 |
| **需人工豁免** | waived | 显示为"已豁免"辅助标识，必须附 Operator、原因和影响范围 |
| **预算风险** | over_estimate / exceeded / blocked_by_budget | 批量生成、D 提交确认和任务中心展示，超过阻塞阈值时必须二次确认 |
| **授权风险** | unknown / restricted / generated_provider_terms | 导出 EpisodeDeliveryPackage、ProjectMigrationPackage 或素材包前提示 |

合规失败（compliance_failed）、重复拦截（duplicate_blocked）、能力未知（capability_unknown）等具体状态必须在 tooltip 或详情页保留原始原因，不能只显示一个泛化徽章后丢失诊断信息。

### Track 生成策略 → UI 用户语言映射

**目的**：策略对象用英文 ID（与代码层、AgentRun、AuditLog、API 接口一致），但 UI 显示给用户时应用中文用户语言。这张表让 dev-builder、code-review 和 design-maker 知道每个策略 ID 对应的 UI 名称。

| 策略 ID（代码层） | UI 用户语言 | 系统决策典型场景 |
|------------------|-------------|----------------|
| `multi_reference_text` | 多参考直出 | 草稿预览、时长 < 4s 单空间单角色 |
| `start_frame` | 仅首帧 | 正式制作默认（4-8s 单空间） |
| `first_last_frame` | 首尾帧 | 含明确动作落点或情绪转折（拔剑/转身/破碎） |
| `multi_keyframe` | 多关键帧 | Track 时长 > 8s 且含 ≥ 2 个明确视觉状态变化 |
| `continue_from_previous` | 承接上一段尾帧 | 与上一段 Track 视觉连续（同场景延续动作） |

Retake 高级区显示策略选择器时，用 UI 用户语言；策略对象、AgentRun、AuditLog、API 接口都用英文 ID。

### 控件规范

- **项目与剧本列表**：使用卡片网格，卡片点击进入详情，删除等破坏性操作必须二次确认。
- **批量操作**：使用复选框、多选集合、批量操作按钮和进度条；批量任务必须显示总数、完成数、成功数、失败数。
- **模型选择**：使用下拉选择器；语言模型、图像模型、视频模型分开选择；默认选择最近使用或第一个启用模型。
- **生成状态**：UI 可以把 Task 状态映射成 pending/running/partial/completed/failed 等用户可读状态，但底层不得再创建另一套任务状态枚举；运行中任务需要轮询或流式反馈。
- **Track 生成策略（系统决策为主）**：每个 Track 都必须记录生成策略，但**用户首次生成时不看到策略选择器**——系统按"策略 × 模型能力 × Track 内容"矩阵自动决策（multi_reference_text / start_frame / first_last_frame / multi_keyframe / continue_from_previous）。Track 行只显示"系统使用了策略 X"+ 简短理由。**只有当用户对生成结果不满意发起 Retake 时**，UI 才暴露"换策略"高级选项。首尾帧、多关键帧等都不是用户主动选的入口，是系统判断后的执行细节。
- **Track 控制**：Track 行使用展开/收起结构；展开后以时间段表格或紧凑卡片展示 0-2s、2-6s、6-10s 等段落，并提供编辑、AI 优化、拆分 Track、锁定段落、生成、重试、预览操作。**默认不显示策略选择器**，策略由系统决策；策略覆盖按钮放在 Retake 弹窗的"高级"区。
- **生成策略覆盖（仅 Retake 时出现）**：用户对 take 不满意点 Retake → 弹出窗口默认是"系统优化提示词重试"，"高级"区才展示策略选择器 / 所需素材清单 / 关键帧/承接帧预览 / 供应商兼容提示，方便资深用户手动覆盖。
- **制作目标控制**：每个 Track 区分 `draft_preview` 和 `production_final`。草稿预览允许多参考直出；正式制作原则上需要视觉锚帧，除非用户明确接受多参考直出的画面漂移风险。
- **策略依赖清单**：在生成按钮前展示该 Track 当前还缺什么，例如首帧未生成、尾帧未确认、上一段视频未完成、视频模型不支持首尾帧、参考资产缺图。缺依赖时禁止直接提交视频任务。
- **Take 管理**：每个 Track 的生成结果以 take 列表展示，包含缩略图、时长、策略、模型、状态、评分/备注、接受、重试、锁定和删除入口；只有 locked take 进入 E 阶段粗剪。
- **成本提示**：高级用户可见的批量提交前预估面板——展示预计视频秒数、图片张数、模型调用次数、可能等待时间和已设置重试上限；这是生产稳定性辅助，不放进主推流程。
- **成本复盘**：任务中心和 E 节点提供 UsageRecord 汇总（估算成本、实际成本、失败成本、重试次数、供应商任务 ID 和策略维度统计），创作者可选查看，不强制对外讲故事。
- **粗剪交付**：E 节点使用时间线列表，不做复杂剪辑轨道；支持按 Track 顺序预览、缺口检查、导出粗剪、AudioSubtitlePlan、EpisodeDeliveryPackage 和导出素材清单。
- **内容编辑**：Markdown/纯文本内容采用只读预览 + 编辑模式切换；关闭有未保存内容时必须提示。
- **资产分类**：角色、场景、道具、服装/妆造使用类型筛选与标签；同名同类资产去重，不同类型允许同名但必须明确类型。
- **资产版本**：资产卡片展示当前主参考图；详情页展示所有 AssetVersion，支持设为主参考、锁定、归档、上传替换、查看来源和查看引用。Track 选择参考资产时必须选择确定版本，不能只选择资产名称。
- **质量门禁**：锁定资产、关键帧、take 或粗剪前展示检查项、AI 建议、人工备注和豁免按钮；豁免必须记录操作者、原因和时间。**按 [重设计结论] 成片质量测量口径展示分项指标分母、AI 初筛结果、人工抽检状态和豁免理由**——不是给一个总体分数蒙混过关。
- **任务中心**：展示本地 Task、远端 providerJobId、重试次数、恢复状态和预计成本；重复点击生成时应提示已有等价任务，而不是直接提交新任务。
- **回收站**：删除 Project、Episode、Asset、Track、Take、RoughCut、Artifact 索引时先进入回收站；硬删除前展示引用影响和将被清理的文件大小。
- **审计日志**：本地模式下简化为操作记录列表，私有服务器模式才启用完整 Operator 审计；关键操作以时间线展示，包含 Operator、操作类型、对象、原因、结果和关联 Task；普通浏览日志不能代替审计日志。

## 用户使用流程

**路径关系**（明确路径之间的衔接和触发时机）：

- **路径 1（剧本文本生成）** = `~sd auto` 的**前置准备**：剧本未就绪时必走。这是文本阶段的剧本 Agent 流程，跑完才有可供制作的剧本。
- **路径 0（一键全自动 A→C3）** = **核心使用方式**：剧本就绪后默认走，只把制作链路 A→C3 自动化。
- **路径 5（D 阶段视频生成确认与批量提交）** = **路径 0 和路径 2 的后续必经**：A→C3 完成后（无论自动还是手动）都进入 D 提交确认页，用户确认成本、模型、策略依赖和并发后，系统才批量提交 Track 视频任务。**D 阶段不是 `~sd auto` 的自动范围**，必须用户确认（视频生成最烧钱）。
- **路径 4（Take 审核与单集粗剪交付）** = **路径 5 的后续必经**：D 阶段完成后，用户审 take、锁定、组装粗剪、导出 EpisodeDeliveryPackage。这是直接影响成片质量的关键环节。
- **路径 2（制作工作台手动逐步 A→C3）** = **路径 0 的替代或介入**：不信任自动管线时手动逐步执行 A→C3，或路径 0 blocked 后人工介入。**A→C3 跑完后同样必须进入路径 5 / 路径 4**，不能在路径 2 内部完成 D/E 阶段。
- **路径 3（模型与供应商配置）** = **横切**：任何阶段穿插使用。

多数用户的主链路是 **路径 1 → 路径 0 → 路径 5 → 路径 4**；路径 2 替代路径 0 后仍接 **路径 5 → 路径 4**；路径 3 是配置工具，不属于主链路。

**Tab ↔ 路径对应**（设计师和开发参考）：
- 剧本 Agent Tab + 剧本管理 Tab ↔ 路径 1
- 自动管线运行视图（项目级独立大屏）↔ 路径 0（自动模式）和路径 5（D 阶段任务模式）共享，按阶段切换显示状态
- 制作工作台 Tab ↔ 路径 2（手动逐步）和路径 5 的"D 阶段提交前确认"入口
- Take/粗剪交付 Tab ↔ 路径 4
- 设置页 ↔ 路径 3

### 路径 0：一键全自动生产（核心差异化路径）🆕

1. 用户在剧本管理或制作工作台选择某集 → 点击"一键全自动制作"按钮（或输入指令 `~sd auto [集号]`）。
2. 系统弹出"启动确认"弹窗：显示预计运行阶段（A→C3）、预计耗时、预计 Token 用量、预计图片任务数、图片生成成本区间、当前剧本版本、关联资产数量；超过预算或图片生成阈值时必须二次确认，用户确认后启动。
3. 浏览器跳转到 **"自动管线运行视图"** 大屏。
4. 系统按 A→B→C1→C2→C3 顺序自动执行：
   - **A 导演分析**：director Agent 生成导演分析；director-skill 自审 + compliance-review 自审；通过后写入后端 Artifact 和数据库记录，进入 B。
   - **B 资产管理**：seedance-asset Skill 执行 B1（提取）→ B2（提示词）→ B3（生图）→ B4（衍生）；**强制对每个资产做"复用/变体/新增"跨集标记**；art-direction-review 自审 + compliance-review 自审。
   - **C1 导演规划**：seedance-director-plan Skill 生成六维度规划。
   - **C2 分镜表**：seedance-storyboard-table Skill 生成 12 列分镜表 + 10 项连贯性校验；必要时回调 B4 补衍生资产。
   - **C3 分镜提示词**：storyboard-artist Agent 生成 Track 级提示词；prompt-review 自审 + compliance-review 自审。
5. 实时展示：管线大图各阶段状态高亮、产物缩略图、Agent 调用日志流式滚动、Token 用量累计、预计剩余时间。
6. 任一阶段失败（stage1_blocked / stage2_blocked / skill_failed）→ 系统自动暂停 → 右侧人工介入面板显示原因、修复建议、"回填后继续"按钮 → 用户处理后继续。
7. 用户可随时点 Pause（暂停后下个 Agent 调用结束时停止）、Resume（恢复）、Stop（终止整个任务）、Skip（跳过某阶段，慎用）。
8. C3 完成后 → 系统**不自动跑 D**（视频生成最烧钱，必须用户确认）→ 生成"D 阶段提交前确认"界面：展示所有 Track 的策略决策、依赖检查结果、预计 Token / 视频秒数 / 视频成本、可选模型 → 用户确认后转入**路径 5**。
9. 全程产物可在自动管线运行视图的"产物预览"区点开查看，不影响管线运行。

**关键约束**：
- 自动管线只跑 A→C3，**D 阶段必须用户确认并转入路径 5 才能开始**（避免误烧视频额度）。
- B3 图片生成同样会产生实际成本；自动管线必须在启动前估算，并在超过预算或阈值时阻塞确认，不能只拦 D 阶段视频成本。
- 每阶段产物通过后端写入对应对象记录和 Artifact 仓库路径：B 阶段资产进入 `dataRoot/projects/{projectId}/assets/...`，单集制作产物进入 `dataRoot/projects/{projectId}/episodes/{episodeId}/...`。旧 `novels/` 路径只允许出现在 LegacyImport 来源映射中。
- 各 Agent 自审失败时不静默继续，必须用户介入。
- 所有 AgentRun 自动记录用于追溯。

### 路径 5：D 阶段视频生成确认与批量提交

路径 5 从路径 0 的 C3 完成页或路径 2 的手动 C3 完成状态进入。它不是 `~sd auto` 的自动范围，但属于核心主链路。

1. 用户进入 D 阶段提交前确认页，检查所有 Track 的策略决策、依赖清单、模型兼容性、预计视频秒数、预计成本、并发上限和重试上限。
2. 用户确认后，系统按依赖关系提交 Track 视频任务：`continue_from_previous` 策略的 Track 等待前置 Track 完成；其他 Track 在并发上限内并行执行。
3. 系统在 D 阶段任务视图逐 Track 显示任务进度、providerJobId、耗时、失败原因和 UsageRecord 估算。
4. 视频任务失败的 Track 可按问题类型自动重试 ≤ 2 次；连续失败超阈值进入 blocked，等待用户介入、换模型、拆分 Track 或在 Retake 高级区覆盖策略。
5. 用户可暂停整个 D 批次，也可终止单个 Track；所有取消、重试、失败和恢复都必须写入 Task 与 AuditLog。
6. D 阶段完成后，系统通知用户进入**路径 4**（Take/粗剪交付 Tab）审 take、锁定、组装粗剪、导出 EpisodeDeliveryPackage。

**关键约束**：
- D 阶段必须用户确认才能开始（视频生成最烧钱），不允许任何路径绕过提交确认页直接进入 D。
- 并发上限默认为 3（与 Task 队列层一致），用户可在 D 提交前确认页临时调整；超过供应商速率限制时系统强制下调并发并提示。
- 单 Track 视频任务失败自动按问题类型重试 ≤ 2 次（动作不对 / 角色漂移 / 构图不对 / 嘴型不对 / 时长不对 / 道具缺失 / 合规失败 7 类），每次重试 prompt 略有变体；连续 ≥ 3 次失败进入 blocked，等待人工介入。
- 用户单独终止某 Track 时，依赖它的下游 Track（`continue_from_previous` 策略链上的下一个 Track）自动进入 blocked，提示用户先重新生成上游或改策略。
- 整批暂停时，已提交但未返回结果的远端视频任务必须由 Task 层做 providerJobId 对账：成功保存为 take；超时或丢失标 orphaned，等待用户对账或重试；不允许"暂停期间扣费但本地无记录"。
- D 阶段所有取消、重试、失败和恢复都必须写入 Task 与 AuditLog；UsageRecord 记录实际成本，失败和重试单独列出，便于成本复盘。

### 路径 1：首次从小说生成剧本

1. 用户启动 Toonflow Web 服务，并在浏览器打开 `http://localhost:{port}` 或已配置的私有访问地址。
2. 用户进入项目列表页，点击“新建项目”。
3. 用户填写项目名称、小说类型、影片画风、画面比例和简介，创建项目。
4. 用户进入项目，打开“小说原文”，粘贴或上传小说章节。
5. 系统解析章节，用户确认后保存。
6. 用户进入“剧本 Agent”，选择语言模型。
7. 用户触发生成故事骨架，系统生成并保存全局与分集骨架。
8. 用户继续生成改编策略，系统保存全局与分集改编策略。
9. 用户继续生成分集剧本，系统创建新的 ScriptVersion 和对应可读 Artifact。
10. 用户进入“剧本管理”，按卡片检查、编辑、审核、对比版本和导出剧本。
11. **剧本就绪后**：用户可选择**路径 0（一键全自动制作，默认推荐）**或**路径 2（手动逐步执行 A→C3）**进入制作链路。路径 1 到此结束，进入制作主链路。

### 路径 2：制作工作台手动逐步（A→C3）

路径 2 是路径 0 的替代或介入方式。**它只覆盖 A→C3 制作链路的手动逐步执行**——D 阶段必须接入路径 5、Take 审核与粗剪交付必须接入路径 4，不能在路径 2 内部完成。

1. 用户在"剧本管理"确认某集剧本可用。
2. 用户点击"开始制作"进入"制作工作台"。
3. 用户选择目标集数，系统检测 A-E 管线状态。
4. 用户通过 Production Agent 输入"开始导演分析"或点击快捷入口。
5. 系统生成 A 阶段导演分析，并从导演分析/大纲/剧本中提取资产。
6. 用户进入"塑造"检查 B 阶段资产，批量润色提示词并生成图片；可手动调整资产"复用/变体/新增"跨集标记。
7. 用户回到"制作工作台"，逐步触发 C1 导演规划、C2 分镜表（含 10 项连贯性校验）、C3 分镜提示词；每阶段产物用户审阅、可编辑或回滚。
8. **C3 完成后接入路径 5**：进入 D 阶段提交前确认页，按路径 5 流程执行视频生成。
9. **D 阶段完成后接入路径 4**：Take 审核、锁定、组装单集粗剪、导出 EpisodeDeliveryPackage。

**关键约束**：
- 路径 2 只负责 A→C3 手动逐步，**严禁在路径 2 内部完成 D/E 阶段**（避免与路径 5/4 职责重复或不一致）。
- 每阶段产物必须用户显式确认（点击"通过"或在该阶段对象上锁定）才能进入下一阶段——这是手动模式区别于路径 0 的核心约束。
- 跨阶段回退：用户可在任一阶段回到上一阶段（如 C2 回到 B），但触发下游 stale/needs_regeneration 标记。
- 路径 2 中各 Agent 仍然有自审能力（director-skill / art-direction-review / prompt-review / compliance-review），不通过时同样阻塞用户进入下一阶段，但**用户可显式豁免**（写 AuditLog），不像路径 0 必须通过自审才能跑下一阶段。

### 路径 4：Take 审核与单集粗剪交付（核心成片质量环节）🆕

D 阶段完成后进入此路径。这是直接决定单集是否可交付的关键环节，不能跳过。

1. 用户收到 D 阶段完成通知，进入 Take/粗剪交付 Tab。
2. 系统展示该集每个 Track 的多个 take 候选；左侧深色 take 审核面板逐 Track 浏览，右侧 E 粗剪交付预览。
3. **逐 Track 审 take**：用户预览每个 take，可执行接受、按问题类型重试（动作不对 / 角色漂移 / 构图不对 / 嘴型不对 / 时长不对 / 道具缺失 / 合规失败 7 类）、替换、标记问题。
4. **Retake 时可覆盖策略**：用户对所有 take 都不满意时点击 Retake 高级区，UI 暴露策略选择器、依赖清单、关键帧/承接帧预览和供应商兼容提示；用户可手动覆盖系统决策策略，系统记录覆盖原因和参数。
5. **锁定 take**：用户对 take 满意时点击锁定。锁定前系统执行 QualityGate 检查（按 [重设计结论] 成片质量测量口径 6 项指标）；不通过则展示分项失败原因，用户可继续重试或人工豁免（豁免必须记录 Operator、原因和影响指标）。
6. 全部 Track 锁定后进入 E 粗剪交付。系统按 Track 顺序读取 locked take，生成 RoughCut、单集预览时间线、缺口列表（未锁定 Track 标注为缺口）。
7. **RoughCut 级质量门禁**：系统再次执行整集级 6 项指标检查（跨 Track 一致性、连续性更关注）；不通过则提示用户回到对应 Track 重做或豁免。
8. 系统按 TrackSegment 时间段、台词、旁白和嘴型规范生成 AudioSubtitlePlan。
9. 用户可生成粗剪预览视频、查看缺口列表、检查素材清单。
10. **导出 EpisodeDeliveryPackage**：单集粗剪导出走 EpisodeDeliveryPackage——包含 RoughCut 视频、AudioSubtitlePlan、locked take 清单、素材清单、剪辑工程清单、manifest（schemaVersion / DB revision / Artifact hash / 缺失项检查 / 敏感配置排除）；默认排除 SecretRef。交付包供剪辑、配音、字幕和外部后期继续处理。

**关键约束**：
- 只有 locked take 进入 RoughCut；未锁定 Track 在粗剪中显示为缺口。
- 锁定前必须经过 QualityGate；豁免必须记录原因。
- E 阶段不做完整非线编、调色、混音和字幕精修——只做"粗剪交付"。完整成片由外部后期完成。
- 整项目迁移/备份走 ProjectMigrationPackage，不在路径 4 范围内（属 P2 设置页能力）。

### 路径 3：配置与调试模型

1. 用户进入设置页。
2. 用户新增语言模型、图像模型、视频模型，填写模型 ID、名称、供应商、API 信息和默认参数。
3. 用户启用目标模型并测试连接。
4. 用户回到剧本 Agent、塑造或制作工作台，在模型选择器中选择模型。
5. 系统把模型 ID 传给后端生成任务。
6. 如果生成失败，用户回到设置页调整配置或切换模型重试。

## AI 能力需求

| 能力类型 | 用途说明 | 应用位置（对应功能名） |
|---------|---------|---------|
| 文本理解与生成 | 阅读项目描述、章节和已有产物，生成故事骨架 | 生成故事骨架：用户触发生成时 |
| 文本理解与生成 | 基于骨架和章节生成改编策略 | 生成改编策略：故事骨架完成后 |
| 文本理解与生成 | 生成结构化分集剧本、台词、场景和动作描写 | 生成分集剧本：改编策略完成后 |
| 文本差异分析 | 对比 ScriptVersion、StoryOutline 和 AdaptationPlan 的版本差异，提示下游影响 | 管理剧本版本与详情：用户保存、回滚或锁定文本产物时 |
| 文本理解与生成 | 从剧本文本解析角色、场景、道具、服装/妆造候选并做去重 | 关联剧本资产：打开剧本详情或保存前 |
| 文本理解与生成 | 基于资产描述、美术风格和视觉锚点生成图像提示词 | 批量润色资产提示词：用户选中资产后 |
| 图像生成 | 根据资产提示词生成角色、场景、道具、服装/妆造参考图 | 批量生成资产图片：提示词就绪后 |
| 图像/文本理解 | 对比资产版本，判断角色身份、服装、场景风格、道具形态是否适合作为主参考图 | 管理资产版本与主参考图：用户准备锁定 canonicalVersion 时 |
| 文本理解与生成 | 生成导演分析、导演规划、分镜表和分镜提示词 | 执行制作管线：A/C1/C2/C3 阶段 |
| 工具调用 | 根据用户意图选择生成、修复、查看进度、执行管线阶段等动作 | 执行制作管线、生成故事骨架、生成改编策略、生成分集剧本 |
| Agent 运行编排 | 记录一次 AgentRun 的输入对象、SkillVersion、模型、工具调用、输出对象和失败原因 | 所有 AI 生成、修复、审核和管线推进动作 |
| 安全边界判断 | 区分用户内容、小说文本、系统 Skill 和工具权限，阻止提示注入触发越权操作 | AgentRun 执行前和工具调用前 |
| 文本理解与生成 | 将 Track 内时间段结构编译为连续叙事视频提示词，并在生成前检测动作过密、时长过长、角色/道具缺引用等问题 | 控制 Track 内镜头段：用户编辑或确认 Track 时 |
| 文本理解与生成 | 根据 Track 时长、动作复杂度、镜头变化、角色数量、制作目标和连续性要求自动决策生成策略 | Track 生成策略决策：用户进入 D 节点或编辑 Track 时 |
| 文本理解与生成 | 根据系统决策策略生成依赖清单和关键帧提示词，判断哪些素材必须先准备 | 准备策略所需视觉输入：系统决策 Track 策略后 |
| 图像生成 | 基于 Track 起点、终点或中间动作生成首帧、尾帧和关键帧图片 | Track 生成策略决策：策略需要关键帧时 |
| 图像生成 | 将场景、人物、道具参考资产组合为具体 Track 起始画面，生成视觉锚帧候选图 | 生成 Track 视觉锚帧：Track 进入正式制作时 |
| 视频帧抽取 | 从已生成视频中抽取尾帧作为下一段 Track 的首帧 | 准备策略所需视觉输入：策略为 `continue_from_previous` 时 |
| 多模态 | 将 Track 级叙事提示词、人物/场景/道具参考图、关键帧/承接帧按系统决策策略组合，生成 4-15 秒短视频片段 | 生成 Track 级视频片段：用户在 D 节点触发时 |
| 文本理解与生成 | 审核 Track 视频是否满足角色一致、动作可读、构图正确、嘴型状态、时长和合规要求，并生成重试建议 | 审核和锁定 Track take：视频生成完成后 |
| 多模态审核 | 根据质量门禁清单检查资产主图、关键帧、take 和粗剪，并输出通过、失败或建议人工豁免 | 执行质量门禁：用户锁定关键产物前 |
| 文本理解与生成 | 生成音频/字幕计划，包括旁白、台词、嘴型、音效和字幕时间段 | 生成音频与字幕计划：E 粗剪交付前 |
| 结构化生成 | 根据 Track 台词、旁白、嘴型和时间段生成 AudioSubtitlePlan | 生成音频与字幕计划：用户进入 E 节点或锁定 RoughCut 后 |
| 嵌入向量 | 建立 Agent 记忆、章节事件和长期项目上下文召回 | 管理 Agent 记忆、管理章节事件图谱 |
| 文本理解与生成 | 审核故事骨架、改编策略和剧本质量并输出建议 | 审核生成结果：用户发起审核时 |
| 文本理解与生成 | 根据审核建议重写或修补目标内容 | 修复生成结果：用户确认修复时 |

## 系统与后端能力需求

这些能力是确定性工程能力，不应拆成 LLM 任务。AI 可以辅助生成说明、建议或摘要，但核心执行必须由后端服务、任务队列、文件仓库和数据库完成。

| 能力类型 | 用途说明 | 应用位置（对应功能名） |
|---------|---------|---------|
| 媒体处理 | 按锁定 Track 顺序拼接 RoughCut，生成预览视频、素材清单和导出清单 | 组装单集粗剪：用户进入 E 节点时 |
| 结构化解析与导入映射 | 扫描旧 `novels/` 项目目录，把章节、剧本、资产、review 和 seedance 产物映射到新对象模型 | 导入旧 novels 项目：用户执行导入时 |
| 依赖分析 | 根据对象引用和版本号判断上游修改会影响哪些下游产物 | 检查上游变更影响：用户保存关键对象前 |
| 项目打包与校验 | 生成 ProjectMigrationPackage manifest、Artifact 清单、hash 校验和导入兼容报告 | 导出和恢复项目迁移包：用户迁移或备份项目时 |
| 任务对账 | 根据 providerJobId、任务参数和本地 Artifact 判断远端生成任务是否完成、丢失、重复或需要恢复 | 恢复和对账生成任务：用户返回任务中心或后端重启后 |
| 环境诊断 | 检查数据目录、数据库、ffmpeg、磁盘空间、模型连接、base URL 和视频范围请求是否可用 | 执行运行环境预检：启动、设置变更或批量生成前 |
| 成本聚合 | 按 UsageRecord 维度聚合估算成本、实际成本、失败成本、重试成本和供应商账单对账状态 | 任务中心、E 粗剪交付节点和成本复盘 |

## 技术方向

| 维度 | 选择 | 理由 |
|------|------|------|
| 产品类型 | Web | 重做版必须支持通过 HTTP 地址直接访问；现有 Web 项目是参考来源，不是兼容目标。Electron 只能作为可选桌面壳，不能替代 Web HTTP 入口。 |
| 后端需求 | 完整 | 后端是产品一等组成部分，需要对象 API、文件产物仓库、AI 调用、SSE/任务轮询、批量任务、模型配置、审核结果、制作状态、导入迁移和访问控制。 |
| 推荐技术栈方向 | Next.js + TypeScript + 后端数据服务 + SQLite/本地数据库 + 文件产物仓库 | Next.js 提供 SSR + API Routes 一体化部署能力；TypeScript 让 37 对象模型可类型化；SQLite 适合本地优先单机部署；文件产物仓库与数据库分离便于备份和迁移。 |
| 数据存储 | 混合后端存储 | 结构化元数据和状态进入数据库；章节、Markdown、图片、视频、粗剪、导出包作为 Artifact 保存在可配置数据根目录；旧 `novels/` 作为迁移来源。 |
| 部署方式 | HTTP Web 服务优先 / 私有服务器 / 可选 Electron 桌面壳 | 创作数据、模型配置和素材生成更适合本地或私有化环境；第一入口应是 `localhost` 或私有 HTTP 地址，桌面壳只是降低普通用户启动门槛。 |

## 技术说明

- 重做版后端必须拥有明确数据边界：前端通过 API 访问项目对象和文件产物，不能直接依赖 `novels/` 目录结构。现有 `path.join(process.cwd(), '..', 'novels')` 规则仅适用于旧 Web 兼容和 legacy import，不应成为新对象模型的长期约束。
- 后端应提供 Workspace / Project / SourceDocument / Chapter / StoryOutline / AdaptationPlan / Episode / ScriptVersion / Asset / AssetVersion / DirectorAnalysis / DirectorPlan / Storyboard / PromptPack / TrackPlan / Track / TrackSegment / ProductionFrame / Take / RoughCut / AudioSubtitlePlan / ExportPackage / UsageRecord / QualityGate / SkillVersion / AgentRun / ProviderConfig / ModelConfig / Task / Artifact / LegacyImport / AccessCredential / SecretRef / Operator / AuditLog / RuntimeCheck / SchemaMigration 的统一 API；页面、Agent、任务队列和导出流程都通过这些 API 协作。
- 后端数据根目录必须可配置，建议用 `TOONFLOW_DATA_DIR` 或设置页配置项表示；默认可使用仓库外的本地应用数据目录。不要把新项目强绑定在 repo 根目录的 `novels/`。
- 数据库保存结构化状态：项目列表、文本来源、章节、故事骨架、改编策略、剧本版本、单集、资产索引、资产版本、导演分析、导演规划、分镜表、提示词包、TrackPlan、Track/Segment、生成策略、QualityGate、take 血缘、RoughCut、AudioSubtitlePlan、ExportPackage、UsageRecord、SkillVersion、AgentRun、任务状态、供应商配置、模型配置、访问配置、SecretRef 元数据、导入来源、审计记录、运行预检和粗剪状态。
- 文件产物仓库保存大文件和可读产物：章节原文、Markdown、提示词、图片、视频、音频、字幕、导出包。数据库只保存路径、hash、mime、大小、来源和关联对象。
- Asset 不能直接指向一张会被覆盖的“最新图”。资产的每次上传、生成、抽帧或替换都应形成 AssetVersion；Track、ProductionFrame、Take 和 RoughCut 必须引用确定的 assetVersionId 或 productionFrameId。
- AssetVersion 和 Artifact 必须记录素材来源：generated、uploaded、extracted、imported、reused，以及 model、prompt、sourcePath/importPath、license、createdBy 和敏感标记。缺少来源的外部素材在导出时应提示风险。
- 后端必须保存 `schemaVersion` 和 migration 记录。任何数据库结构、Artifact 路径规则或对象字段变更，都需要可检测、可迁移、可回滚或给出明确失败提示。
- 对象需要 revision/version 字段。保存 Project、Episode、Asset、Track、TrackSegment 等关键对象时必须做乐观锁校验，避免多标签页或局域网访问互相覆盖。
- 上游对象变更必须触发下游失效检查：SourceDocument/Chapter 影响 StoryOutline、AdaptationPlan、ScriptVersion 和资产提取；ScriptVersion 影响剧本审核、资产关联、DirectorAnalysis、DirectorPlan、Storyboard、PromptPack 和 TrackPlan；AssetVersion 影响锚帧、关键帧和 take；TrackSegment 影响 prompt、关键帧、take 和粗剪；模型配置影响后续任务但不应自动改写已锁定 take。
- SkillVersion 或 Agent 策略变更不会自动让已有内容失效，但新生成内容必须记录使用的 SkillVersion。用户重新生成时，界面应提示本次使用的 SkillVersion 是否不同于上次。
- locked 对象默认只读。已锁定 take、锚帧、关键帧、粗剪和导出结果不能被新生成结果覆盖；需要修改时必须显式解锁或复制为新版本。
- 删除、解锁、人工豁免、导出 ProjectMigrationPackage、导出敏感配置、修改模型密钥、修改访问凭据等操作必须写入 AuditLog。AuditLog 至少包含 Operator、操作类型、对象 ID、前后状态、原因、时间和结果。
- Operator 不是默认云账号体系。localhost 可以使用默认本机身份；局域网或私有服务器访问必须能把访问凭据映射到可审计身份；不做完整多租户权限也不能让敏感操作匿名。
- 旧 `novels/` 导入必须是显式流程：扫描 → 预览 → 映射 → 导入 → 校验。默认不原地迁移、不删除、不覆盖；用户确认后才可建立只读引用或复制到新数据根目录。
- 后端需要提供备份与恢复的产品能力：至少能导出 ProjectMigrationPackage、导出数据索引、检测缺失 Artifact，并在设置页提示数据根目录位置和存储占用。导出必须使用一致性快照，manifest 至少记录导出时的 DB revision、schemaVersion、Artifact hash、缺失文件、运行中任务和敏感配置排除项。
- 关键对象删除采用软删除优先。Project、Episode、Asset、AssetVersion、Track、Take、RoughCut 和 Artifact 索引应先进入回收站；硬删除必须二次确认，并由 Artifact 生命周期管理清理孤儿文件。
- HTTP Web 访问是一等需求。系统必须能以 Web 服务方式启动，并通过 `http://localhost:{port}` 访问；可配置 host、port、base URL 和是否允许局域网访问。
- 支持访问形态分三类：本机访问 `localhost`、局域网访问 `http://{lan-ip}:{port}`、私有服务器/反向代理访问。非 localhost 访问必须至少提供密码、访问令牌或等价访问控制，避免把本地小说、模型密钥和生成素材裸露到网络。
- 默认 host 应绑定 localhost。局域网访问必须由用户显式开启并显示风险提示；private_server、公网域名或跨网段反向代理访问必须使用 HTTPS，除非用户明确处于受控内网并接受风险。
- 访问凭据与模型密钥必须独立于项目数据保存。ProjectMigrationPackage 默认不包含模型密钥、访问令牌和供应商密钥；如用户选择导出敏感配置，必须二次确认。
- ProviderConfig 只保存供应商名称、base URL、能力探测结果、模型同步规则和 SecretRef，不直接保存密钥明文。ModelConfig 引用 ProviderConfig，并声明语言/图像/视频能力、限制和默认参数。
- SecretRef 只保存密钥引用、用途、作用域、创建时间、过期时间和审计信息，不保存可导出的密钥明文。ProjectMigrationPackage、普通日志、前端响应和审计日志都不得泄露密钥值。
- HTTP 文件访问必须防路径穿越和越权下载。所有 Artifact 预览、下载和上传都应通过受控 API 或短期 signed URL，禁止把任意本地路径直接暴露给浏览器。
- 上传素材必须限制 MIME、扩展名、文件大小、分辨率和总存储占用；视频/图片预览要基于后端识别结果，不信任浏览器上传的 Content-Type。
- 局域网和私有服务器模式必须考虑 CSRF/CORS、会话过期、访问令牌撤销、反向代理头和 base URL 配置；localhost 开发便利不能复制到非本机访问。
- RuntimeCheck 必须在服务启动、切换 dataRoot、模型配置保存、批量图片/视频生成、粗剪导出前执行对应检查。至少检查数据目录可写、数据库迁移状态、剩余磁盘、ffmpeg/媒体处理工具、视频范围请求、模型连接、供应商能力和任务队列健康。
- Electron 或桌面壳只能封装 HTTP Web 服务，不应形成另一套业务逻辑、数据路径或 API。浏览器访问和桌面壳访问必须共享同一套 Web UI、API Routes 和数据层。
- 图片、视频、粗剪和导出文件必须能通过 HTTP 预览或下载；视频预览应支持大文件和断点/范围请求能力，避免长视频预览只能整文件下载。
- API、SSE、任务轮询、素材预览和下载路径必须在配置 base URL 后仍可工作，避免部署到子路径、反向代理或局域网地址后资源链接失效。
- 默认不得直接改动 `novels/` 业务数据，除非用户通过界面操作触发创建、编辑、删除等明确动作。
- AI 模型分为语言、图像、视频三类；剧本 Agent 使用语言模型，塑造使用语言模型与图像模型，制作工作台使用语言、图像、视频模型组合。
- 视频生成不只有一条主路径，而是按 Track 选择生成策略。系统必须把策略作为 Track 数据的一部分保存，不能只保存最终 prompt。
- Track 是视频 API 的生成单位，Track 内时间段/镜头段是用户的控制单位。产品不能把 10-15 秒视频全部压缩成不可编辑长 prompt；复杂 Track 必须能展开、检查、编辑和拆分。
- 单个 Track 建议只承载一个主要动作、一个情绪转折或一个空间连续段；超过 10 秒、包含多次动作转折、跨场景或参考资产过多时，系统应提示拆分为多个 Track。
- Track 内时间段通常为 2-4 段，每段至少包含画面目标、角色动作、镜头/景别、台词/嘴型、情绪、参考资产。最终提交给视频模型前，由系统编译成连续叙事提示词，而不是把表格字段原样丢给模型。
- C2 Storyboard 和 C3 PromptPack 不能只输出最终长 prompt，还必须为 D 阶段 TrackPlan 提供 Track 起点视觉状态、终点视觉状态、关键中间状态、参考资产引用和连续性要求。否则 D 阶段无法合理生成首帧、尾帧或多关键帧。
- D 阶段拆成策略驱动子流程：D0 Track 检查与拆分，D1 **策略系统决策**（不是用户确认），D2 策略依赖准备，D3 视频任务提交（用户确认后），D4 预览审核与重试。`start_frame` 等策略改变的是 D2 准备步骤，不要求重写 A/B/C 全链路。
- **策略由系统决策而非用户选**：D1 阶段系统读取 Track 内容（时长、动作复杂度、镜头变化、角色数量、连续性要求）+ 模型能力矩阵，自动判断最适合的策略，并在 UI 上显示"使用策略 X，理由 Y"。**用户首次生成时不参与策略选择**——这避免把"AI 不听话"的痛点翻译成"用户操作累"。只有当用户对生成结果不满意发起 Retake 时，UI 才在"高级"区暴露策略覆盖选项。
- 系统策略决策规则（按优先级匹配，命中即停止）：
  1. Track 与上一段 Track 视觉连续（同场景延续动作）→ `continue_from_previous`
  2. Track 时长 > 8 秒 且 包含 ≥ 2 个明确视觉状态变化 → `multi_keyframe`
  3. Track 包含明确的动作落点或情绪转折（拔剑前后、转身前后、道具出现/破碎前后）→ `first_last_frame`
  4. Track 时长 4-8 秒 且 单空间单角色 且 动作简单 → 默认 `start_frame`（正式制作的基础策略）
  5. Track 时长 < 4 秒 且 用户标注为草稿预览 → `multi_reference_text`（仅作为草稿快速试跑）
  - 决策结果如果与所选视频模型能力不兼容 → 系统提示"该 Track 推荐策略 X，但当前视频模型 Y 不支持，建议换模型或拆分 Track"，**不静默降级**。
- 合理性原则：正式制作不应只依赖"场景图 + 人物图 + 道具图 + prompt"直出，因为多张参考图只能提供身份和风格，不能稳定定义这一段视频的具体构图、人物站位和道具空间关系。正式制作的基础控制物应是视觉锚帧。
- `multi_reference_text` 仅用于草稿预览、快速试跑或模型能力验证，**不作为正式成片的策略**。系统决策时只在草稿模式或时长极短场景才选它；用户在 Retake 高级区主动覆盖到它，UI 必须显示"该策略可能导致构图漂移、角色站位不稳定"的警告。
- Track 生成策略详细说明（用于系统决策参考和 Retake 时的可选清单）：
  - `multi_reference_text`：适合草稿预览、快速试跑、单一空间、动作简单且对首尾构图不敏感的 Track，输入为场景/人物/道具参考图 + 分段 motion prompt。
  - `start_frame`：适合大多数正式制作 Track，用来锁定开场构图、人物站位、服装细节和场景关系。制作过程必须先生成、上传或复用一张视觉锚帧，确认后再提交视频任务；锚帧应记录来源、提示词、关联资产和锁定状态。
  - `first_last_frame`：适合必须锁定动作结果或情绪落点的 Track，例如拔剑前后、转身前后、道具出现/破碎前后。制作过程必须先准备首帧和尾帧，并检查两帧之间的人物身份、服装、场景、镜头方向是否一致。
  - `multi_keyframe`：适合 Track 内有两个以上明确视觉状态变化的片段。制作过程必须先生成多个关键帧并排序；如果关键帧超过 3 个，系统应优先建议拆分 Track。
  - `continue_from_previous`：适合两个 Track 需要视觉连续时，使用上一段视频尾帧作为下一段首帧，再叠加人物/场景/道具参考图和 motion prompt。制作过程必须依赖上一段 Track 完成；上一段失败或未生成时，当前 Track 不能进入 ready。
- 不得默认为每个 Track 生成首尾帧。全量首尾帧会显著增加图片生成成本、审核成本和人工确认成本，还可能因为首尾帧风格/构图差异过大导致视频过渡异常。
- 首尾帧不是“高级模式”，只是生成策略之一。需求验收按“每个 Track 有明确策略、策略可解释、策略所需素材可生成或补齐、供应商不兼容时可处理”验收。
- “支持多种视频模式”不表示用户要手工维护多套流程；产品主流程承诺的是 Track 策略选择、素材补齐、供应商能力适配和结果回写。
- 策略素材分两类：项目资产图和 Track 生产图。角色、场景、道具、服装/妆造参考图属于项目资产，可跨集复用；首帧、尾帧、中间关键帧、承接帧属于 Track 生产图，只服务于具体 Track，不应污染角色/场景/道具/服装资产库。
- 视觉锚帧的生成不应提前塞进 B 资产管理阶段。B 阶段只生成可复用资产；视觉锚帧必须在 D2 阶段按具体 Track 的起点视觉状态生成，避免为所有可能镜头预生成大量无用图片。
- 视频模型配置必须声明支持能力，例如是否支持多参考图、首帧、首尾帧、多关键帧、视频延展、最大参考图数量、最大时长、比例和分辨率。策略决策和任务提交必须读取这些能力。
- D 阶段输出不等于成片。每个 Track 可能有多个 take，只有用户锁定的 take 才能进入 E 阶段；未锁定 Track 在粗剪中显示为缺口。
- E 阶段是单集粗剪交付，不是完整非线编软件。它只负责按 Track 顺序组装粗剪、检查缺口、生成音频/字幕计划和导出素材清单；复杂剪辑、调色、混音、字幕精修可以交给外部剪辑软件。
- Track take 必须记录生成血缘：模型、供应商、参数、seed、输入参考图、视觉锚帧、关键帧、最终提示词、生成时间、成本估算、错误信息和父 take。没有血缘记录，重试和问题定位会变成猜谜。
- 每次模型调用和媒体处理都必须写入 UsageRecord。UsageRecord 至少记录 Task、ProviderConfig、ModelConfig、对象 ID、估算用量、实际用量、失败原因、重试次数、providerJobId、计费单位和时间。
- **成本分析是数据汇总能力，不是 AI 能力**：按 UsageRecord 维度（项目、单集、Track、模型、策略）聚合输出实际成本、失败成本、重试成本和供应商账单对账状态。后端按维度做 SQL 聚合即可，不需要 LLM 参与。前端在任务中心和 E 节点提供该汇总视图。
- 每次 AI 生成、审核、修复、提示词编译和工具调用都必须写入 AgentRun。AgentRun 至少记录输入对象版本、用户指令、SkillVersion、ModelConfig、Task、工具调用、输出对象、错误、UsageRecord 和审计引用。
- 用户导入的小说、剧本、网页内容和对话消息都只能作为内容上下文，不得覆盖系统 Skill、工具权限、访问控制或密钥策略。AgentRun 执行工具前必须经过 allowlist 和对象权限校验。
- Track 重试不应只有“重新生成”。系统应支持按问题类型重试：动作不对、角色漂移、构图不对、嘴型不对、时长不对、道具缺失、合规失败。每种问题应生成不同的修复提示，必要时建议用户在 Retake 高级区人工覆盖策略。
- 锁定关键产物前必须经过 QualityGate。至少覆盖资产主参考图、视觉锚帧/关键帧、Track take 和 RoughCut；检查不通过时不能静默进入下一阶段，人工豁免必须记录原因。
- 音频/字幕是独立轨道，不应硬塞进 D 视频生成。D 阶段可以记录台词和嘴型意图；E 阶段生成音频/字幕计划；真正 TTS、口型、音效和混音可作为后续能力接入。若视频模型支持同步音频，也必须在模型能力里显式声明，不默认所有模型都支持。
- AudioSubtitlePlan 是 RoughCut 的一等关联对象，不只是导出的 JSON。后续接 TTS、字幕、口型或音效时，都应基于同一个 AudioSubtitlePlan 版本继续生成。
- ExportPackage 必须记录包含哪些 Artifact、manifest、目标格式、生成时间、hash、缺失项、敏感配置排除项和导入兼容结果。导出粗剪和导出素材包归入 EpisodeDeliveryPackage；项目迁移、备份和恢复归入 ProjectMigrationPackage。
- 批量生成前必须有成本和队列提示。策略越重，调用次数越多：`start_frame` 至少多一次图片生成，`first_last_frame` 至少多两次关键帧准备，`multi_keyframe` 成本随关键帧数量增加，`continue_from_previous` 会产生前后 Track 依赖阻塞。
- Task 不是简单状态字段，必须支持依赖 DAG、取消、暂停、恢复、重试、超时、并发限制和失败清理。`continue_from_previous`、批量关键帧、视频 take、粗剪导出都必须能表达前置依赖。
- Task 必须支持幂等和远端任务对账。提交图片/视频任务时保存 idempotencyKey、providerJobId、requestHash、attemptNo、costEstimate 和 providerStatus；页面刷新、重复点击、Worker 重启后先恢复或对账已有任务，不得默认再次扣费提交。
- Task 状态不能混用 Track/Take 状态。Task 表达执行过程，Track/Take 表达业务结果，依赖有效性表达上游是否变化；例如一个 locked take 可以同时是 generated/locked，但 dependencyValidity 为 stale。
- Artifact 必须有生命周期管理：hash 去重、缩略图/代理文件、引用计数、缺失检测、孤儿清理、归档和恢复。删除 take 或项目记录时不能留下不可见的大文件垃圾。
- 无限画布不是当前主流程的必要条件。当前 Web 先用“管线节点图 + 节点详情 + Agent 对话”串起流程；无限画布可作为后续更自由的生产视图，用来组织剧本、角色、场景、道具、服装/妆造、Track 和视频结果节点，但不能替代底层数据状态机。
- 批量生成任务需要可轮询状态，失败项不能阻断整批任务。
- 生成结果必须写入可审计的数据库记录和 Artifact：骨架、改编策略、剧本、导演分析、分镜表、提示词、资产记录、任务记录和审核结果都应有对象 ID、版本、路径或 Artifact 引用。
- Toonflow-app 的完整能力包含登录、SQLite、Socket.IO、Agent 记忆、可编程供应商和无限画布；当前 Web 已实现其中一部分，后续产品需求应以“补齐必要能力”为准，不要照搬所有复杂度。
- 重做版不以旧页面、旧 API、旧组件结构为兼容目标。可以复用有价值的数据结构、提示词和文件产物，但产品信息架构应围绕 Project / Episode / Asset / Track / Take / RoughCut 重新设计。
- 后端化以后，`novels/` 这个名称不应出现在新产品用户心智里。用户看到的是"项目 / 集 / 资产 / 数据目录 / 存储状态"等产品语言；Workspace 只作为后端对象存在，不作为用户入口。`novels/` 只出现在导入旧项目或开发兼容说明中。
- **两套审核机制并存（区分清楚）**：
  - **自动审核（属 ~sd auto 内部步骤）**：路径 0 跑各阶段时由对应 Review Skill / Agent 自动执行——A 阶段 director-skill 自审 + compliance-review；B 阶段 art-direction-review + compliance-review；C2 阶段执行 10 项连贯性校验（无独立 Agent）；C3 阶段 prompt-review + compliance-review。失败时 blocked，触发用户在自动管线运行视图介入面板回填。**这套审核是 ~sd auto 内部机制，用户不主动触发，UI 不暴露独立操作**。
  - **人工审核（属辅助功能）**：用户对 StoryOutline / AdaptationPlan / ScriptVersion 等文本产物主动发起的额外评分审核（[辅助功能] "审核生成结果"），输出评分 + 问题 + 修改建议；用户可基于结果调用"修复生成结果"重新生成。**这套审核是用户主动行为，UI 有显式入口**。
  - 两套审核都写入 QualityGate（自动审核记 `source=agent_self_review`，人工审核记 `source=manual_review`），但生命周期、UI 入口、触发时机完全不同。dev-builder 实现时不能合并成同一套。

## 补充说明

### 页面清单

| 页面/区域 | 主要职责 |
|----------|----------|
| 项目列表 | 创建、查看、删除短剧项目 |
| 自动管线运行视图 | 展示 `~sd auto` A→C3 实时进度、产物预览、Agent 日志、成本估算和人工介入 |
| 跨集 IP 资产时间线 | 展示角色、场景、道具和服装/妆造跨集复用、变体、新增和一致性风险 |
| 小说原文 | 导入、解析、编辑、删除章节 |
| 剧本 Agent | 通过对话和按钮生成骨架、改编策略、剧本 |
| 剧本管理 | 检索、批量选择、导出、审核、编辑分集剧本和关联资产 |
| 塑造 | 管理角色/场景/道具/服装资产，批量提示词润色和图片生成 |
| 制作工作台 | 以管线节点和 Agent 对话推进导演分析、资产、分镜和视频阶段 |
| Take/粗剪交付 | 审核 Track take、执行质量门禁、锁定 take、组装 RoughCut、生成 AudioSubtitlePlan 和导出 EpisodeDeliveryPackage |
| 任务中心 | 查看生成任务、远端任务对账、失败恢复和成本记录 |
| 设置 | 管理模型、供应商、Skill、记忆、运行环境、访问凭据、审计日志和系统配置 |

### 输入来源与解析边界

| 输入类型 | 核心处理规则 |
|----------|--------------|
| 小说 `.txt` / 粘贴文本 | 保存为 SourceDocument；自动识别“第 X 章”、`Chapter N`、数字标题等章节边界；解析结果必须可人工合并、拆分、重命名 |
| 已有分集剧本文本 | 可直接创建 ScriptVersion，并把 StoryOutline/AdaptationPlan 标记为 external/manual 来源；不强迫用户重新走小说改编链路 |
| 旧 `novels/` 项目 | 只通过 LegacyImport 扫描、预览、映射和导入；默认不得原地修改旧目录 |
| 外部图片/视频帧 | 保存为 Artifact；必须记录来源、license、hash、mime、size 和 createdBy；缺少授权信息时导出前提示风险 |
| PDF / DOCX / 网页链接 | 不属于核心闭环强制输入；后续可作为 SourceDocument 扩展，但必须先转为受控 Artifact 和纯文本快照 |

章节或剧本解析失败时，系统不得静默丢弃内容；必须保留原文 Artifact，并给用户手动分段或重新解析入口。

### 权限与访问矩阵

Toonflow Web 不默认做云端多租户 SaaS，但私有访问必须能把敏感操作映射到可审计身份。轻量角色只服务本地/私有部署下的访问控制，不引入组织、订阅和复杂租户模型。

| 操作类型 | localhost 默认身份 | 非 localhost 访问 | 审计要求 |
|----------|--------------------|-------------------|----------|
| 查看项目、章节、资产和产物 | 允许 | viewer/editor/owner 允许 | 普通浏览不强制写 AuditLog |
| 编辑章节、剧本、资产、TrackSegment | 允许 | editor/owner 允许 | 关键对象保存记录 revision 和 Operator |
| 提交 AI/媒体生成任务 | 允许 | editor/owner 允许 | 必须写 Task、AgentRun、UsageRecord |
| 锁定、解锁、人工豁免 | 允许 | owner 或具备制片权限的 editor | 必须写 AuditLog，含原因 |
| 删除、恢复、硬删除 | 允许但二次确认 | owner 允许；硬删除必须二次确认 | 必须写 AuditLog 和 Artifact 清理记录 |
| 导出 ProjectMigrationPackage 或素材包 | 允许 | owner/editor 允许；含敏感配置仅 owner | 必须写 AuditLog；默认排除 SecretRef |
| 修改模型密钥、访问凭据、dataRoot | 允许但二次确认 | owner 允许 | 必须写 AuditLog，不记录密钥明文 |

### 素材版权与合规边界

| 风险项 | 产品要求 |
|--------|----------|
| 外部素材授权不明 | Artifact/AssetVersion 的 license 默认为 unknown；导出 EpisodeDeliveryPackage、ProjectMigrationPackage 或素材包前必须提示 |
| 用户上传侵权或敏感素材 | 系统记录来源和创建者；质量门禁可标记 compliance_failed；用户需人工确认或替换 |
| AI 生成素材商用限制 | ProviderConfig/ModelConfig 应记录供应商条款摘要或链接；导出时提示使用者自行确认商用授权 |
| 角色相似度和 IP 风险 | QualityGate 允许记录“疑似撞脸/撞 IP/风格过近”问题；不把 AI 判断当法律结论 |
| 水印、EXIF 和敏感元数据 | 上传和导出前应提供检测结果；默认不把本地绝对路径、密钥或隐私元数据写入导出包 |

### 成本预算与止损规则

| 项 | 规则 |
|----|------|
| 预算层级 | Project、Episode、Batch Task 可设置可选预算上限；未设置时仍需展示估算成本 |
| 超额确认 | 估算超过预算、单次生成超过阈值或重试会明显增加成本时，必须二次确认 |
| 重试止损 | 每个 Task 和 Track 可设置最大重试次数；连续失败达到阈值后进入 blocked，需要用户改策略、换模型或人工确认 |
| 失败成本 | UsageRecord 必须区分成功成本、失败成本、重试成本和供应商账单对账状态 |
| 批量生成 | 提交前展示任务数、预计图片数、视频秒数、等待时间、并发限制和取消入口 |

### 状态枚举

| 选项 | 可选值 | 说明 |
|------|--------|------|
| 管线节点状态 | pending / running / partial / completed / failed / stale | 用于制作工作台 A-E 节点 |
| 文本产物状态 | draft / generating / reviewed / locked / stale / archived | 用于 SourceDocument、StoryOutline、AdaptationPlan 和 ScriptVersion |
| 制作产物状态 | draft / generating / reviewed / locked / stale / archived | 用于 DirectorAnalysis、DirectorPlan、Storyboard、PromptPack 和 TrackPlan |
| 资产类型 | character / scene / prop / costume | 分别代表角色、场景、道具、服装/妆造 |
| 资产图片状态 | pending / generating / success / failed | 用于资产卡片图片生成结果 |
| 资产提示词状态 | pending / generating / completed / failed | 用于批量润色进度 |
| 模型类型 | language / image / video | 设置页固定三类，不让用户自定义类型 |
| 模型状态 | enabled / disabled | 只有启用模型出现在任务选择器中 |
| 供应商状态 | enabled / disabled / connection_failed / capability_unknown | 用于 ProviderConfig 连接、能力同步和任务提交判断 |
| 成本记录状态 | estimated / actual / reconciled / disputed | 用于 UsageRecord 估算、实际用量和供应商账单对账 |
| HTTP 访问模式 | localhost / lan / private_server | 分别代表本机访问、局域网访问和私有服务器访问 |
| Operator 角色 | owner / editor / viewer | 用于私有访问下的轻量权限控制，不等同云端多租户 |
| 后端存储模式 | local_hybrid / private_server | `local_hybrid` 表示本地数据库 + 文件产物仓库；`private_server` 表示私有服务器部署 |
| Legacy 导入状态 | scanned / mapped / imported / failed | 用于旧 `novels/` 项目导入流程 |
| 删除状态 | active / trashed / hard_deleted | 用于关键对象软删除、恢复和最终清理 |
| 资产版本状态 | candidate / canonical / locked / rejected / archived | 用于判断资产图是否可被 Track 引用 |
| 质量门禁状态 | unchecked / passed / failed / waived | 用于锁定资产、关键帧、take 和粗剪前的检查 |
| Task 状态 | queued / running / paused / cancelled / retrying / succeeded / failed / timeout / reconciling | 用于异步任务执行过程，不等同于 Track/Take 业务状态 |
| 任务恢复状态 | normal / reconnecting / orphaned / reconciled / duplicate_blocked | 用于远端生成任务恢复、对账和重复提交拦截 |
| 依赖有效性状态 | current / stale / needs_regeneration / blocked_by_missing_input | 用于表示上游对象变更或输入缺失对下游产物的影响 |
| RuntimeCheck 状态 | unchecked / checking / passed / warning / failed | 用于启动、设置变更、批量生成和导出前的环境预检 |
| Artifact 来源 | generated / uploaded / extracted / imported / reused | 用于记录素材和产物来源 |
| Artifact 授权状态 | unknown / user_owned / generated_provider_terms / licensed / restricted | 用于导出前提示素材版权与商用风险 |
| AgentRun 状态 | queued / running / tool_waiting / succeeded / failed / cancelled | 用于记录 Agent 执行过程，不替代 Task 状态 |
| SkillVersion 状态 | draft / active / archived | 用于提示词版本管理和回滚 |
| 预算状态 | unset / within_budget / over_estimate / exceeded / blocked_by_budget | 用于批量生成前估算、执行中止损和成本复盘 |
| Track 制作目标 | draft_preview / production_final | 草稿预览允许多参考直出；正式制作原则上需要视觉锚帧或承接帧 |
| Track 控制状态 | draft / needs_review / preparing_inputs / ready / generating / generated / locked / failed | 用于 D 节点判断 Track 是否已完成时间段检查、策略素材准备、可生成、已生成或已锁定 |
| Track 生成策略 | multi_reference_text / start_frame / first_last_frame / multi_keyframe / continue_from_previous | 用于 D 节点记录该 Track 的视频生成方法 |
| Track take 状态 | generated / accepted / rejected / locked / failed | 用于判断单次视频生成结果是否被采用 |
| Track 问题类型 | character_drift / composition_error / action_error / mouth_state_error / prop_missing / duration_error / compliance_failed / model_error | 用于分类重试原因和修复建议 |
| Track 关键帧状态 | none / suggested / generating / ready / locked / rejected / failed | 用于判断关键帧是否需要生成、确认、锁定或重试 |
| Track 策略依赖状态 | missing / waiting / generating / ready / blocked / failed | 用于判断参考资产、关键帧、承接帧和供应商能力是否已满足 |
| Track 生产图来源 | generated / uploaded / extracted / reused | 用于记录视觉锚帧、尾帧、中间关键帧和承接帧的来源 |
| 粗剪交付状态 | incomplete / ready_for_preview / preview_generated / exported | 用于 E 阶段判断粗剪是否可预览或已导出 |
| ExportPackage 状态 | preparing / ready / failed / imported / expired | 用于导出包生成、恢复和迁移 |
| Agent 意图 | chat / generate_skeleton / generate_adaptation / generate_script / fix_skeleton / fix_adaptation / fix_script / check_progress | 剧本 Agent 的主要意图 |
| 制作阶段 | A / B / C1 / C2 / C3 / D / E | 制作管线节点 |

### 数据产物

| 产物 | 说明 |
|------|------|
| Backend database | 保存 Workspace、Project、SourceDocument、Chapter、StoryOutline、AdaptationPlan、Episode、ScriptVersion、Asset、AssetVersion、DirectorAnalysis、DirectorPlan、Storyboard、PromptPack、TrackPlan、Track、TrackSegment、ProductionFrame、Take、RoughCut、AudioSubtitlePlan、ExportPackage、UsageRecord、QualityGate、SkillVersion、AgentRun、Task、ProviderConfig、ModelConfig、Artifact、LegacyImport、AccessCredential、SecretRef、Operator、AuditLog、RuntimeCheck、SchemaMigration 索引和状态 |
| `dataRoot/projects/{projectId}/source/...` | SourceDocument 原文、导入原始文件和来源记录 |
| `dataRoot/projects/{projectId}/chapters/{chapterId}.md` | Chapter 可读导出和人工编辑快照 |
| `dataRoot/projects/{projectId}/story/outline-{versionId}.md` | StoryOutline 可读版本 |
| `dataRoot/projects/{projectId}/story/adaptation-{versionId}.md` | AdaptationPlan 可读版本 |
| `dataRoot/projects/{projectId}/skills/{skillVersionId}.md` | SkillVersion 可读快照 |
| `dataRoot/projects/{projectId}/agent-runs/{agentRunId}.json` | AgentRun 输入、工具调用、输出和错误快照 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/scripts/{scriptVersionId}.md` | ScriptVersion 分集剧本 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/director-analysis/{analysisVersionId}.md` | DirectorAnalysis 可读版本 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/director-plan/{planVersionId}.json` | DirectorPlan 结构化版本 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/storyboards/{storyboardVersionId}.json` | Storyboard 分镜表结构化版本 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/prompt-packs/{promptPackVersionId}.md` | PromptPack 可读版本 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/track-plans/{trackPlanVersionId}.json` | TrackPlan 与 Track 内时间段控制数据 |
| `dataRoot/projects/{projectId}/assets/{type}/{assetId}/versions/...` | 角色、场景、道具、服装/妆造的 AssetVersion 图片、提示词、来源和主参考记录 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/frames/{trackId}/start.png` | Track 视觉锚帧 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/frames/{trackId}/previous-tail.png` | 从上一段视频抽取的尾帧 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/keyframes/{trackId}/...` | Track 首帧、尾帧或多关键帧 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/exports/prompts-readable.md` | PromptPack 和 TrackPlan 的可读导出，不作为主数据源 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/videos/takes/{trackId}/{takeId}.mp4` | Track 单次生成 take |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/videos/selected-takes.json` | 每个 Track 当前接受或锁定的 take 导出 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/audio-subtitle-plans/{planVersionId}.json` | AudioSubtitlePlan 音频与字幕计划 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/rough-cuts/{roughCutId}.json` | RoughCut 单集粗剪清单 |
| `dataRoot/projects/{projectId}/episodes/{episodeId}/exports/rough-cut.mp4` | 可选粗剪预览视频 |
| `dataRoot/imports/legacy-novels/{importId}.json` | 旧 `novels/` 导入映射、校验结果和来源路径 |
| `dataRoot/exports/{exportPackageId}/manifest.json` | ExportPackage manifest，记录 schemaVersion、DB revision、Artifact hash、缺失文件、目标格式和敏感配置排除项 |
| `dataRoot/usage/usage-{date}.jsonl` | 可选 UsageRecord 导出；数据库仍是主记录 |
| `dataRoot/trash/...` | 软删除对象的恢复索引和待清理 Artifact 记录 |
| `dataRoot/audit/audit-{date}.jsonl` | 可选审计日志导出；数据库仍是主记录 |
| `dataRoot/runtime-checks/{checkId}.json` | RuntimeCheck 结果快照，便于排查环境问题 |

### 明确不做的事

- 不把当前 Web 直接改成 Toonflow-app 的完整克隆。那样只会把复杂度搬过来，产品边界反而更乱。
- 不把现有 Web 的页面结构、旧 Tab 路由或旧 API 兼容作为重做目标。旧实现只能作为参考，不应绑架新产品结构。
- 不把 `novels/` 继续作为新产品的核心数据模型或用户可见概念。它只能作为 legacy import 来源或兼容层。
- 不让前端页面直接读写项目文件。所有项目数据和文件产物必须经过后端 API、权限检查、状态校验和任务记录。
- 不把本地绝对路径、模型密钥、访问令牌或供应商凭据暴露给浏览器、项目包或普通导出文件。
- 不让 Track、Take 或 RoughCut 引用“当前最新资产图”这种浮动状态。所有生产结果必须引用确定版本，避免以后复现不了。
- 不把首尾帧生成作为每个 Track 的强制前置步骤。你的系统现在依赖场景、人物、道具参考图和提示词拼合成 10 秒左右视频，首尾帧只用于策略确实需要锁定起止画面的 Track，不用于普通 Track 批量强制生产。
- 不把首帧、尾帧或关键帧混入角色/场景/道具/服装资产库。它们是 Track 生产过程中的临时或半成品控制图，生命周期和复用范围不同。
- 不在 E 阶段做完整非线编、调色、混音和字幕精修。核心闭环只做粗剪交付和导出，别把自己写成 Premiere。
- 不默认视频模型会生成可用对白、音效或口型。音频/字幕必须先作为独立计划保存，再按模型能力选择是否生成。
- 不把产品做成只能通过 Electron 打开的桌面应用。必须保留 HTTP Web 直接访问能力。
- 不默认开放公网访问。HTTP 访问优先面向本机、局域网和私有服务器；如果暴露到公网，必须先补齐认证、密钥保护和访问日志。
- 不把无限画布作为重做版的核心入口。它可以是后续生产视图，但第一性流程仍然是 A-E 管线和对象状态。
- 不让 Agent 记忆、事件图谱、可编程供应商或无限画布成为主链路阻塞项。它们可以增强效果，但不能决定剧本到粗剪是否能跑通。
- 不在产品需求文档中拆开发 Phase；阶段拆分属于 `dev-planner`。
- 不在产品需求文档中规定精确颜色、字号、圆角等视觉细节；视觉方向属于 `design-brief-builder` 和 `design-maker`。
- 不默认做云端多租户 SaaS。当前需求以本地优先和私有化为主，登录和权限只在对齐 Toonflow-app 或私有部署时引入。
- **不让用户在 Track 首次生成时选策略**。策略由系统按"策略 × 模型能力 × Track 内容"矩阵自动决策；用户只在对生成结果不满意发起 Retake 时，在"高级"区才看到策略覆盖选项。把"AI 不听话"翻译成"用户操作累"是反产品的设计。
- **不把 37 个核心对象全部暴露给 UI**。后端对象模型保留完整（这是真实架构），但 UI 只展示"项目 / 集 / 章节 / 剧本 / 资产 / 镜头 / 视频版本 / 粗剪 / 单集交付包 / 项目迁移包"等用户语言；ProviderConfig / SecretRef / RuntimeCheck / SchemaMigration 等工程对象不进主流程 UI，参见 [UI 布局] 的"后端对象 → 用户可见概念"映射表。
- **不把 38 套状态枚举全部暴露给 UI**。主流程状态收敛到 6 个用户语言（待规划 / 准备中 / 可确认 / 已确认 / 失败或中止 / 过期或警告），设置、预算、授权和豁免类状态作为辅助标识展示；参见 [UI 布局] 的"后端状态 → UI 用户语言"映射表。
- **不把 Legacy Import / 回收站 / ProjectMigrationPackage 导出检查作为顶级页面**。这些是低频运维入口，挂在设置子菜单下；它们不影响成片质量，不该占主导航重量。
- **不把"成本透明化"作为对外讲产品故事的差异化卖点**。UsageRecord、预算估算、止损规则等功能保留作为生产稳定性支撑，但不放进产品定位、不放进 hero 区文案；产品的对外故事只围绕"成片质量"展开。
- **不为追求功能完整度牺牲成片质量**。Spec 中任何一条规则、任何一个对象、任何一个状态，如果与"产出电影级成片"目标无关或冲突，都必须降级或砍掉。北极星指标永远是最终成片质量。
