# Bug 报告：剧本管理

生成时间：2026-04-03
验证版本：当前 master 分支

## 概要

| 类型 | 🔴 阻断 | 🟡 一般 | 🟢 建议 |
|------|---------|---------|---------|
| 编译 | 0 | 3 | 0 |
| UI | 0 | 3 | 2 |
| 交互 | 0 | 0 | 1 |
| 总计 | 0 | 6 | 3 |

## 编译验证

✅ 编译通过，0 错误

### 警告列表
🟡 ScriptsTab.tsx:6 — `Modal` 导入未使用（@typescript-eslint/no-unused-vars）
🟡 ScriptsTab.tsx:33 — `scripts` 逻辑表达式可能导致 useCallback 依赖每次渲染变化（react-hooks/exhaustive-deps）
🟡 OverviewTab.tsx:13 / ChaptersTab.tsx:13 — `name` 定义但未使用

## 原型图审核记录

已读取原型图：
- image-20260323下午83217585.png（主页面：Tab + 关联资产 + 空态）
- image-20260401下午30352261.png（剧本内容行号显示）
- image-20260401下午30408820.png（分镜图网格）
- image-20260401下午30425733.png（更多分镜 + 视频配置）

## 布局强制检查

- 整体布局：原型图为单列纵向排列 → 实际为单列纵向排列 → ✅
- 集数 Tab：原型图有圆角药丸 Tab（第1集/第2集）→ 实际有圆角 Tab → ✅
- 统计栏：原型图有「剧本统计 X集」+ 右侧「一键导出剧本」按钮 → 实际有统计但缺少导出按钮 → 🟡
- 关联资产：原型图有缩略图网格 + 右上「批量生成」按钮 → 实际有缩略图网格但缺少批量生成按钮 → 🟢（Phase 2 功能）
- 剧本内容：原型图有行号 + 深色标题栏 + 右上「生成剧本」按钮和字数统计 → 实际有行号 + 深色标题栏 + 字数统计，缺少「生成剧本」按钮 → 🟢（Phase 2 功能）
- 分镜图区：原型图有紫色标题栏 + 4列网格 + 每卡片含片段标签/图片/标题/描述 → 实际有但分镜卡片缺少标题 → 🟡
- 视频配置区：原型图有「视频配置」区域展示视频生成状态卡片 → 实际缺失 → 🟡
- 空状态：原型图有图标+文案 → 实际有 → ✅

## 问题详情

### BUG-001 🟡 未使用的 Modal 导入
- **类型**：编译
- **位置**：ScriptsTab.tsx:6
- **描述**：导入了 Modal 组件但未使用
- **修复**：删除该导入

### BUG-002 🟡 useCallback 依赖警告
- **类型**：编译
- **位置**：ScriptsTab.tsx:33,63
- **描述**：`scripts` 在 useCallback 依赖中可能每次渲染变化
- **修复**：用 useMemo 包裹 scripts 初始化，或在 useCallback 中使用 scripts.length 替代

### BUG-003 🟡 分镜卡片缺少标题
- **类型**：UI
- **位置**：ScriptsTab.tsx:320
- **描述**：原型图中每个分镜卡片有标题（如"广场眺望"、"焦急等待"），当前只显示 fragmentContent 描述
- **期望**：每个 shot 显示 fragmentContent 作为标题（粗体），prompt 前30字作为描述
- **修复**：调整卡片布局，将 fragmentContent 显示为标题

### BUG-004 🟡 缺少「一键导出剧本」按钮
- **类型**：UI
- **位置**：统计栏右侧
- **描述**：原型图统计栏右侧有「一键导出剧本」按钮
- **修复**：在统计栏右侧添加导出按钮（触发下载当前集剧本 txt）

### BUG-005 🟡 缺少视频配置区域
- **类型**：UI
- **位置**：分镜图区域下方
- **描述**：原型图第4张显示「视频配置」区域，展示已生成视频的状态卡片
- **修复**：新增视频配置区域，读取 videos/ 目录数据展示

### BUG-006 🟡 其他文件未使用变量警告
- **类型**：编译
- **位置**：OverviewTab.tsx:13, ChaptersTab.tsx:13
- **描述**：`name` 参数定义但未使用
- **修复**：在解构时加下划线前缀 `_name`

### BUG-007 🟢 关联资产缺少「批量生成」按钮
- **类型**：UI
- **位置**：关联资产区右上角
- **描述**：原型图有「批量生成」按钮，属于 Phase 2 功能
- **修复**：可添加禁用按钮占位

### BUG-008 🟢 剧本内容缺少「生成剧本」按钮
- **类型**：UI
- **位置**：剧本内容标题栏右侧
- **描述**：原型图有「生成剧本」按钮，属于 Phase 2 功能
- **修复**：可添加禁用按钮占位

### BUG-009 🟢 编辑后保存无编辑注意事项提示
- **类型**：交互
- **位置**：编辑模式上方
- **描述**：原型图在编辑模式时上方有「编辑后重点注意事项」提示区
- **修复**：可在编辑模式顶部添加提示文字
